# 01-插件管理

## BL-PLUGIN-001 插件 CRUD 与调试执行

- 模块：插件管理
- 优先级：P0
- 状态：已完成
- 当前阶段：阶段一
- 价值：安全测试人员可以创建和管理漏洞利用插件资产。
- 关联模块文档：docs/modules/01-插件管理模块.md
- 关联计划：
- 关联代码：`app_cybersparker/views/expload/plugin_manage.py`, `app_cybersparker/views/expload/exp_debug.py`, `app_cybersparker/models.py:24`
- 验收状态：已验收

### 范围
- 插件列表/新增/编辑/删除/详情/启停
- 插件文件上传 + 在线编辑（poc_content）
- 调试执行（即时编译 + importlib 加载 + 结果返回）
- 插件类型 12 种 + 能力标记 5 种

### 不做
- 插件版本管理
- 插件导入/导出
- 插件市场同步

### 验收条件
- [ ] `POST /expload/add` 创建插件，DB 有对应 `EXP` 记录，`EXP_plugin/` 有对应文件
- [ ] `POST /expload/edit` 更新插件，`EXP` 记录和文件同步更新
- [ ] `POST /expload/delete` 删除插件，DB 记录和文件均删除
- [ ] `POST /expload/useStatus` 切换启停，`EXP.use` 字段正确变更
- [ ] `POST /expload/execute` 调试执行 Python3 插件，返回有效 JSON 结果
- [ ] `POST /expload/save` 在线编辑保存插件，`poc_content` 写入 DB

### 依赖
- Django 4.1 ORM
- `importlib` 动态加载

### 状态记录
- 2026-05-13：基于代码审查确认功能已实现，待验收。
- 2026-05-13：阶段一验收审计通过。代码契约验证：Django 系统检查 0 issues，模型可导入，CRUD 视图可导入，URL 解析通过。验收条件精化：Nuclei YAML 调试执行 JSON 结构明确为 `status` + `output` 字段。

---

## BL-PLUGIN-002 Nuclei YAML 运行时支持

- 模块：插件管理
- 优先级：P0
- 状态：已完成
- 当前阶段：阶段一
- 价值：支持 Nuclei YAML 模板格式，扩展插件生态。
- 关联模块文档：docs/modules/01-插件管理模块.md
- 关联计划：plans/2026-04-25-nuclei-yaml-runtime-support.md
- 关联代码：`app_cybersparker/views/expload/task_manage/nuclei_runtime_engine.py`, `app_cybersparker/views/expload/task_manage/poc_runtime_resolver.py`, `app_cybersparker/models.py:45-52`
- 验收状态：已验收

### 范围
- `plugin_language=2`（nuclei_yaml）插件的创建和存储
- Nuclei YAML 解析引擎（HTTP 请求模板、匹配器、提取器）
- 与现有执行链路（单任务/批量任务/调试）的统一接入

### 不做
- 完整 Nuclei YAML 规范兼容（仅支持常用指令子集）
- Nuclei 模板社区同步

### 验收条件
- [ ] 创建 `plugin_language=nuclei_yaml` 的插件，写入 DB 和文件
- [ ] 调试执行 nuclei_yaml 插件，返回 JSON 包含 `status`(success/fail) 和 `output` 字段
- [ ] 单任务/批量任务可调用 nuclei_yaml 插件并产出结果

### 依赖
- `EXP.plugin_language` 字段（migration `0005`）
- `poc_runtime_resolver.py` 统一解析入口

### 状态记录
- 2026-05-13：基于代码审查确认功能已实现，待验收。
- 2026-05-13：阶段一验收审计通过。代码契约验证：Django 系统检查 0 issues，模型可导入，CRUD 视图可导入，URL 解析通过。验收条件精化：Nuclei YAML 调试执行 JSON 结构明确为 `status` + `output` 字段。

---

## BL-PLUGIN-003 插件列表批量删除

- 模块：插件管理
- 优先级：P1
- 状态：已完成
- 当前阶段：阶段一（增强）
- 价值：安全测试人员可以批量删除不再需要的漏洞利用插件。
- 关联模块文档：docs/modules/01-插件管理模块.md
- 关联计划：plans/2026-05-13-expload-batch-delete.md
- 关联代码：`app_cybersparker/views/expload/plugin_manage.py:341-357`, `cybersparker/urls.py:19`
- 验收状态：已验收

### 范围
- 列表页复选框选中多项后，批量删除按钮可用
- 后端接收 `uids[]` 列表，逐个删除 EXP 记录和关联文件
- 复用已有删除确认弹窗

### 不做
- 不提供撤销/回收站功能
- 不提供全选-当前页/全选-全部功能

### 验收条件
- [ ] 勾选 ≥2 项 → 点击"batch delete" → 确认弹窗显示选中数量 → 确认后页面刷新 → 选中项消失
- [ ] 未勾选任何项时点击"batch delete" → 提示"请至少选择一项"
- [ ] Django 系统检查 0 issues，`/expload/batch_delete` URL 解析通过

### 依赖
- 已有 `expload_delete` 单条删除逻辑
- 已有列表复选框 UI（`#check-all`, `.check-item`）

### 状态记录
- 2026-05-13：Mode D-lite 快速实现，自检通过（Django check 0 issues，URL 解析通过）。
- 2026-05-20：批量删除参数兼容修复完成。后端同时兼容 `uids[]` / `uids`，回归测试覆盖当前页面实际提交格式；`python manage.py check` 与目标测试通过。

---

## BL-PLUGIN-004 CodeMirror Tab→4空格 强制缩进

- 模块：插件管理
- 优先级：P2
- 状态：已完成
- 当前阶段：阶段一（增强）
- 价值：从代码层面杜绝 POC 编辑时 tab/space 混用，避免 Python TabError。
- 关联模块文档：docs/modules/01-插件管理模块.md
- 关联计划：plans/2026-05-13-codemirror-tab-to-spaces.md
- 关联代码：`app_cybersparker/templates/project/expload/exp_debug.html:326-401`
- 验收状态：已验收

### 范围
- tips 编辑器（`editor_tips`）：补全 `indentUnit: 4, tabSize: 4, indentWithTabs: false`
- 主编辑器（`editor`）：在 `extraKeys` 中显式绑定 Tab 键，直接插入 4 空格
- 两个编辑器统一使用 `cm.replaceSelection(spaces, "end", "+input")` 处理 Tab

### 不做
- 不修改 CodeMirror 库文件本身
- 不改变 Shift-Tab（减少缩进）和 Ctrl-/（切换注释）行为

### 验收条件
- [ ] Django 系统检查 0 issues
- [ ] 模板可正常加载，两个编辑器均配置 `indentWithTabs: false` + 显式 Tab 到 空格
- [ ] 浏览器端验证：在编辑器中按 Tab 输入 4 空格，不会出现 tab 字符

### 依赖
- CodeMirror 5.x（`static/project2/expload/codemirror/`）

### 状态记录
- 2026-05-13：Mode D-lite 实现。两个 CodeMirror 实例统一配置，`extraKeys.Tab` 显式插入 4 空格。Django check 通过。

---

## BL-PLUGIN-005 EXP 模型 severity + Tag 表 + 迁移

- 模块：插件管理
- 优先级：P0
- 状态：已完成
- 当前阶段：阶段六
- 价值：每个 EXP 有危害等级和标签，任务可以按这两个维度筛选要运行的漏洞验证模板。
- 关联模块文档：docs/modules/01-插件管理模块.md
- 关联计划：plans/2026-05-27-EXP-severity-tags分类与任务过滤.md
- 关联代码：`app_cybersparker/models.py`（EXP、Tag）
- 验收状态：已验收

### 范围
- EXP 表新增 `severity` 字段（CharField，choices: critical/high/medium/low/info，允许空，db_index=True）
- 新建 `Tag` 模型（`name` CharField unique，db_index=True）
- EXP 与 Tag 多对多关联（`tags = ManyToManyField("Tag", blank=True)`）
- 中间表自动产生 `(exp_id, tag_id)` 索引
- 迁移：0043 建 Tag + severity + M2M（三项合并在一个迁移文件中）

### 不做
- 不增加 severity 枚举值
- 不做 Tag 的 CRUD 管理页面（通过 Django Admin 或命令行操作）
- 不做标签层级（父子标签）

### 验收条件
- [ ] `python manage.py makemigrations` 生成 0043（Tag + severity + M2M）
- [ ] `python manage.py migrate` 成功执行
- [ ] `EXP.objects.create(severity="high")` 写入成功
- [ ] `Tag.objects.get_or_create(name="xss")` 返回 (tag, True/False)
- [ ] `exp.tags.add(tag)` 中间表写入成功
- [ ] `EXP.objects.filter(severity="info")` 走索引（EXPLAIN 确认）
- [ ] `EXP.objects.filter(tags__name="xss")` 走 M2M JOIN 索引
- [ ] `python manage.py check` 0 issues

### 依赖
- EXP 模型（`app_cybersparker/models.py`）

### 状态记录
- 2026-05-27：Mode B0 需求澄清 + 方案审计通过，创建 backlog item。

---

## BL-PLUGIN-006 EXP 表单 + 后台列表页 severity/tags 显示

- 模块：插件管理
- 优先级：P1
- 状态：已完成
- 当前阶段：阶段六
- 价值：用户能在后台看到每个 EXP 的 severity 和 tags，python3 插件可手动设置。
- 关联模块文档：docs/modules/01-插件管理模块.md
- 关联计划：plans/2026-05-27-EXP-severity-tags分类与任务过滤.md
- 关联代码：
  - `app_cybersparker/views/expload/plugin_manage.py`（EXP 列表/编辑视图）
  - `app_cybersparker/templates/project/expload/`（EXP 列表/编辑模板）
- 验收状态：已验收

### 范围
- EXP 列表页：新增 severity 列（颜色标记）、tags 列（标签样式）
- EXP 编辑表单：
  - severity：下拉选择（5 个值 + "未设置"）
  - tags：多选输入
  - 所有类型 EXP（python3/nuclei_yaml）均可编辑 severity 和 tags
- 列表页筛选：可按 severity 下拉筛选、按 tag 名称搜索

### 不做
- 不做 Tag 的新增/编辑/删除页面
- 不修改 EXP 列表页的其他列

### 验收条件
- [ ] 列表页每行显示 severity 徽章（颜色区分 5 级 + 空）
- [ ] 列表页每行显示 tags 标签列表
- [ ] 编辑表单：下拉可选中任一 severity 值，保存后 DB 更新，重新打开回显已选值
- [ ] 编辑表单：多选可添加/移除 tags，保存后 DB 更新，重新打开回显已选 tags
- [ ] 列表页：可按 severity 下拉筛选、按 tag 名称搜索
- [ ] `python manage.py check` 0 issues

### 依赖
- BL-PLUGIN-005（EXP.severity + Tag 模型已存在）

### 状态记录
- 2026-05-27：Mode B 生成，创建 backlog item。
- 2026-05-27：实现完成。列表页 severity 徽章 + tags 标签列；编辑表单 severity 下拉 + tags 多选；所有类型 EXP 均可编辑；JS 编辑回显修复；列表页 severity 下拉筛选 + tag 搜索。审计后修复 3 个前端缺陷。

---

## BL-PLUGIN-007 import_nuclei_templates 自动解析 severity + tags + 已有数据补全

- 模块：插件管理
- 优先级：P1
- 状态：已完成
- 当前阶段：阶段六
- 价值：支持库中仅保留当前引擎真正能执行的 Nuclei 模板，并让后续导入自动跳过不支持协议模板。
- 关联模块文档：docs/modules/01-插件管理模块.md
- 关联计划：plans/2026-05-27-EXP-severity-tags分类与任务过滤.md，plans/2026-05-29-nuclei-unsupported-template-cleanup.md
- 关联代码：
  - `app_cybersparker/management/commands/import_nuclei_templates.py`
  - `app_cybersparker/management/commands/cleanup_nuclei_unsupported_templates.py`
- 验收状态：已验收

### 范围
- 首次导入时：`yaml.safe_load(content)` → 提取 `info.severity` + `info.tags` → `Tag.objects.get_or_create()` → `exp.tags.add()` + `exp.save(update_fields=['severity'])`
- 导入前先识别模板顶层协议；带 `code/javascript/headless/file/dns/ssl/websocket/whois` 任一协议块的模板直接跳过，不写入 `EXP`
- 新增管理命令 `python manage.py backfill_exp_metadata`：遍历所有 nuclei_yaml EXP，读 YAML 文件回填 severity + tags。进度条输出，失败记录输出日志。
- 新增管理命令 `python manage.py cleanup_nuclei_unsupported_templates`：清理历史库中不支持协议模板、删除对应 YAML 文件，并同步清理批量任务 `EXP` 字段里的悬空模板 id
- 回填策略：`update_fields=['severity']` 逐条保存，`exp.tags.add(*tags)` 逐条关联

### 不做
- 不重新解析 Python3 EXP 文件
- 不实现 `code/javascript/headless/file/dns/ssl/websocket/whois` 这些协议的运行时执行器

### 验收条件
- [ ] 新导入一个支持协议的 Nuclei 模板后，EXP 记录的 severity 和 tags 有正确值
- [ ] 新导入一个不支持协议模板时，数据库不会新增对应 `EXP`
- [ ] `python manage.py cleanup_nuclei_unsupported_templates` 能删除历史不支持协议模板，并同步删除 YAML 文件与批量任务悬空引用
- [ ] `python manage.py backfill_exp_metadata` 成功执行，输出完成/跳过/失败计数
- [ ] `python manage.py check` 0 issues

### 依赖
- BL-PLUGIN-005（EXP.severity + Tag 模型已存在）

### 状态记录
- 2026-05-27：Mode B 生成，创建 backlog item。
- 2026-05-29：补做 Nuclei 协议支持边界治理。确认当前运行时只支持 `http/requests` 与 `tcp/network`；`code/javascript/headless/file/dns/ssl/websocket/whois` 一律视为不支持协议。`import_nuclei_templates` 导入时已直接跳过这类模板；新增 `cleanup_nuclei_unsupported_templates` 清理历史库中的同类模板、对应 YAML 文件和批量任务悬空引用；协议含义与决策理由见 `docs/nuclei-协议支持边界与模板清理.md`。
- 2026-05-30：已实际执行历史清理。删除 1,813 条不支持协议模板，删除 1,813 个 `EXP_plugin/` YAML 文件，批量任务 `id=61` 的悬空模板引用已清空。清理后剩余 nuclei 模板 11,109 条，复核确认库中不再存在不支持协议模板。

---

## BL-PLUGIN-008 插件列表页标题可点击跳转编辑

- 模块：插件管理
- 优先级：P2
- 状态：已完成
- 当前阶段：维护
- 价值：表格列太多时"编辑"按钮被挤出屏幕，用户点击标题即可跳转编辑页，不会以为没有编辑功能。
- 关联模块文档：docs/modules/01-插件管理模块.md
- 关联计划：plans/2026-06-05-插件列表标题可点击跳转编辑.md
- 关联代码：frontend/src/pages/PluginListPage.tsx:180
- 验收状态：已验收

### 范围

- 插件列表页标题改为 `<a>` 链接，跳转到 `/react-shell/exp-debug?plugin_id=<id>`

### 不做

- 不删除原有的"编辑"按钮（作为显式入口保留）

### 验收条件

- [ ] 点击插件标题跳转到插件调试页（编辑页）
- [ ] Vite build 通过

### 状态记录

- 2026-06-05：Mode D-lite 实现，1 行改动，Vite build 通过。后续调整：标题改为新标签页打开，文字颜色保持黑色（不用品牌色）。

---

## BL-PLUGIN-009 Python PoC 插件 target 参数从字符串改为字典

- 模块：插件管理
- 优先级：P0
- 状态：已完成
- 当前阶段：阶段十六
- 价值：Python PoC 插件运行时可以接收字典格式的 target 参数（而非仅字符串 URL），为后续传入代理配置、超时时间、系统内置函数引用等扩展能力打下基础。同时 Python 和 YAML 插件运行时接口统一。
- 关联模块文档：docs/modules/01-插件管理模块.md
- 关联计划：plans/2026-06-16-python-poc-target-dict-refactor.md
- 关联代码：`app_cybersparker/views/expload/task_manage/poc_runtime_resolver.py`, `app_cybersparker/views/expload/task_manage/batch_task_executor.py`, `app_cybersparker/views/expload/task_manage/auto_exp_task.py`, `app_cybersparker/views/expload/exp_debug.py`, `app_cybersparker/views/expload/task_manage/batch_exp_task.py`, `app_cybersparker/services/dirscan_worker.py`, `app_cybersparker/views/expload/result__manage/expResult.py`, `EXP_plugin/*.py`（16 个）
- 验收状态：已验收

### 范围

- `poc_runtime_resolver.py` — `call_runtime_method` 加类型校验，拒绝字符串 target，强制 `{"target": url}` 格式
- 7 个调用点 — 把传给插件的字符串 URL 包装为 `{"target": url}` 字典
- 16 个 Python 插件 — `_verify`/`_cmd_exc`/`_code_exc`/`_attact` 入口函数第一行提取 `url = target["target"]`，return 写回 `'target': url` 字符串，`cmd` 参数不动；内部 helper 函数保持字符串参数

### 不做

- 不修改 YAML template 文件（YAML 插件本身已接受 dict）
- 不在本次增加 `proxy`/`timeout`/函数引用等扩展字段（接口就位，字段后续加）
- 不删除 `EXP_plugin/` 下的残留文件
- 不清理 `dirscan_worker.py` 和 `expResult.py` 中可能存在的死导入
- 不修改 `[QVE-2022-2022]test2222_m703tgu0.py`（`_verify()` 零参数，功能不可用）

### 验收条件

- [ ] `call_runtime_method` 传入字符串 target 时抛出 TypeError（`test_poc_runtime_rejects_string_target` 测试通过）
- [ ] `python manage.py check` 0 issues
- [ ] `python manage.py test app_cybersparker.tests --parallel` 全部通过
- [ ] tests.py 中 7 处 `patch("...call_runtime_method")` mock 返回值的 `"target"` 字段均为字符串（非 dict）
- [ ] 下游消费者（`batch_task_executor.save_TaskResult`、`auto_exp_task._normalize_exp_result`、`dirscan_worker.consumer`、`batch_exp_task.TaskResult_verify`、`expResult` 复验）从插件返回值中读取的 `target` 字段为字符串（非 dict）
- [ ] 调试页选 Python 插件，输入 URL 执行 verify，结果正确返回
- [ ] 调试页选 YAML 插件，执行 verify，`__debug_trace` 仍生效
- [ ] 调试页选 Python 插件，执行 cmd_exc，cmd 参数正常传递
- [ ] 批量任务 1 Python + 1 YAML + 1 target，两者结果正确落库

### 依赖

- `EXP` 表（插件记录）
- `poc_runtime_resolver.py` 统一路由
- `batch_task_executor.py` / `auto_exp_task.py` / `dirscan_worker.py` 执行链路
- `exp_debug.py` 调试页（Python/YAML 两路分支）
- `batch_exp_task.py`（`TaskResult_verify` 手动复验）
- `expResult.py`（结果页手动复验）
- `EXP_plugin/*.py`（16 个，运行时动态加载）

### 状态记录

- 2026-06-16：计划方案 v3 完成，三轮独立 agent 审查通过。
- 2026-06-16：Mode D 实现完成。改动：poc_runtime_resolver.py（+8行类型校验）、8 个调用点（batch_task_executor/auto_exp_task/exp_debug/batch_exp_task/dirscan_worker/expResult）、16 个插件文件（EXP_plugin/*.py）、tests.py（+40行，3 个新测试）。Django check 0 issues，3 个新测试全部通过。CHANGELOG 已更新。

---

## BL-PLUGIN-010 插件调试页插件列表懒加载

- 模块：插件管理
- 优先级：P1
- 状态：已完成
- 当前阶段：维护
- 价值：插件调试页的插件下拉框不再一次性加载全部插件（数据库可能有几千条，接口响应慢、网络传输大），改为输入关键词后服务端搜索、最多返回 50 条，页面打开速度和搜索响应都大幅提升。
- 关联模块文档：docs/modules/01-插件管理模块.md
- 关联计划：plans/2026-06-23-BL-PLUGIN-010-插件调试懒加载.md
- 关联代码：`app_cybersparker/views/expload/exp_debug.py:245`、`frontend/src/pages/ExpDebugPage.tsx:55`
- 验收状态：未验收

### 范围

- 后端 `api_plugin_list`：支持 `?q=` 查询参数，按 title/CVE 模糊匹配，最多返回 50 条
- 前端 `ExpDebugPage`：插件 Select 改为 `onSearch` 触发服务端搜索，防抖 300ms，不再页面打开时全量加载

### 不做

- 不改变 API 响应格式（仍返回 `{status, data: [{id, title, CVE, label}]}`）
- 不支持分页翻页（下拉搜索不需要）
- 不修改其他使用插件列表的页面

### 验收条件

- [ ] 打开插件调试页，插件下拉框初始为空（不自动加载）
- [ ] 在插件选择框输入关键词（如"nginx"），300ms 后发 API 请求 `GET /api/v1/exp-debug/plugins?q=nginx`
- [ ] API 按 title/CVE 模糊匹配，最多返回 50 条，按 id 倒序
- [ ] 下拉搜索结果正确展示，选择后可正常加载插件信息
- [ ] `python manage.py check` 0 issues
- [ ] Vite build 通过

### 依赖

- `EXP` 表
- antd `Select` 组件 `onSearch` + `filterOption`

### 状态记录

- 2026-06-23：Mode D-lite，问题来自生产环境反馈——插件调试页 `/api/v1/exp-debug/plugins` 接口响应很久。
- 2026-06-23：实现完成。后端 `debug_getPluginList` 加 `?q=` 搜索参数（title/CVE icontains + 50 条上限）；前端 Select 改为 `onSearch` + 300ms 防抖 + `filterOption=false`；`?plugin_id=xxx` 跳转场景补丁（选中插件自动注入 options）。Django check 0 issues，Vite build 通过。
