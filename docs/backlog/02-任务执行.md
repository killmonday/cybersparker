# 02-任务执行

## BL-TASK-001 单任务执行

- 模块：任务执行
- 优先级：P0
- 状态：已完成
- 当前阶段：阶段一
- 价值：安全测试人员可以对单个插件执行批量目标验证。
- 关联模块文档：docs/modules/02-任务执行模块.md
- 关联计划：
- 关联代码：`app_cybersparker/views/expload/task_manage/exp_task.py`, `app_cybersparker/models.py:69`
- 验收状态：已验收

### 范围
- 任务 CRUD（创建/编辑/删除/列表/详情）
- 生产者-消费者线程执行模型
- 可配置线程数和请求间隔
- 任务启停控制（THREAD_DIC）
- 文件输入和历史结果输入

### 不做
- 任务优先级队列
- 任务调度（定时执行）

### 验收条件
- [ ] `POST /exploadTask/add` 创建单任务，`EXPTask` 记录写入 DB
- [ ] `POST /exploadTask/operate` 启动任务，`status` 变为 `running`，线程启动
- [ ] 任务执行完毕后 `status` 变为 `finish`，`EXPTask_result` 有对应结果记录
- [ ] `POST /exploadTask/operate` 停止任务，线程终止，`status` 变为 `stop`
- [ ] `GET /exploadTask/<uid>/result` 返回正确筛选的结果列表

### 依赖
- `EXP_plugin/` 插件文件
- `THREAD_DIC`（`cybersparker/settings.py:22`）

### 状态记录
- 2026-05-13：基于代码审查确认功能已实现，待验收。
- 2026-05-13：阶段一验收审计通过。代码契约验证：Django 系统检查 0 issues，模型/执行引擎可导入，URL 解析通过。验收条件精化：线程/协程模式验收明确为 status=finish + EXPTask_result 有对应记录。

### 状态记录
- 2026-05-13：基于代码审查确认功能已实现，待验收。
- 2026-05-13：阶段一验收审计通过。代码契约验证：Django 系统检查 0 issues，模型/执行引擎可导入，URL 解析通过。验收条件精化：线程/协程模式验收明确为 status=finish + EXPTask_result 有对应记录。

---

## BL-BATCH-001 批量任务执行

- 模块：任务执行
- 优先级：P0
- 状态：已完成
- 当前阶段：阶段一
- 价值：安全测试人员可以对多插件、多输入源执行批量漏洞利用。
- 关联模块文档：docs/modules/02-任务执行模块.md
- 关联计划：
  - plans/merry-plotting-snowflake.md
  - plans/批量EXP任务新增网络空间测绘输入源-2026-04-22.md
  - plans/2026-04-21-批量任务添加多种输入来源.md
- 关联代码：`app_cybersparker/views/expload/task_manage/batch_exp_task.py`, `app_cybersparker/views/expload/task_manage/batch_task_executor.py`, `app_cybersparker/models.py:116`, `app_cybersparker/services/cyberspace_engine_service.py`
- 验收状态：已验收

### 范围
- 批量任务 CRUD
- 输入源：文件上传 / 历史漏洞资产 / 历史上传文件 / 空间测绘引擎
- 运行模式：线程 / 协程子进程（gevent）
- 空间测绘引擎集成（fofa/zoomeye/quake/hunter/shodan）
- 任务启停控制（BATH_TASK_DIC）
- 历史文件管理

### 不做
- 分布式执行
- 任务重试机制

### 验收条件
- [ ] `POST /batch_exploadTask/add` 创建批量任务，`batch_EXPTask` 写入 DB
- [ ] 选择 `input_type=4`（空间测绘引擎）后，任务能调用引擎 API 获取目标
- [ ] 批量任务线程模式（run_mode=1）执行完毕，`batch_EXPTask.status` 变为 `finish`(1)，`EXPTask_result` 有 `task_id` 对应的 `task_type=2` 记录
- [ ] 批量任务协程模式（run_mode=2）执行完毕，`batch_EXPTask.status` 变为 `finish`(1)，`EXPTask_result` 有对应结果记录
- [ ] `POST /batch_exploadTask/operate` 停止任务，线程/子进程终止

### 依赖
- 空间测绘引擎配置（`CyberspaceEngineSetting`）
- 代理配置（`ProxySetting`，可选）
- 请求运行时 Monkey Patch
- `BATH_TASK_DIC`（`cybersparker/settings.py:24`）

### 状态记录
- 2026-05-13：基于代码审查确认功能已实现，待验收。
- 2026-05-13：阶段一验收审计通过。代码契约验证：Django 系统检查 0 issues，模型/执行引擎可导入，URL 解析通过。验收条件精化：线程/协程模式验收明确为 status=finish + EXPTask_result 有对应记录。
- 2026-05-29：补修批量任务结果页分页参数。此前结果页 `task_result()` 只排除了 `page/per_page`，没排除前端实际传的 `rows_per_page`，导致用户切换每页条数时把 `rows_per_page=20` 误拼进 ORM 过滤条件，抛 `FieldError: Cannot resolve keyword 'rows_per_page'`。现改为将 `rows_per_page` 也视为分页参数，不再进入 `search_dict`。定向回归测试 1 条通过，`python manage.py check` 0 issues。

---

## BL-BATCH-002 批量任务 Python POC 结果入库容错

- 模块：任务执行
- 优先级：P1
- 状态：已完成
- 当前阶段：阶段一（修复）
- 价值：POC 文件有语法错误时任务失败可被调试，而非静默跳过。
- 关联模块文档：docs/modules/02-任务执行模块.md
- 关联计划：plans/2026-05-13-fix-batch-task-python-result.md
- 关联代码：`app_cybersparker/views/expload/task_manage/batch_task_executor.py:116-117`
- 验收状态：已验收

### 范围
- `_build_exp_cache` 中 POC 加载失败时打印 traceback 到 stderr，而非静默跳过
- 修复测试 POC `[QVE-2026-1111]test11_a21ff8e5.py` 的 tab/space 混用缩进

### 不做
- 不做 POC 语法预检或 IDE 级错误提示
- 不改变现有异常处理控制流（仍然 continue）

### 验收条件
- [ ] POC 文件可被 `importlib` 正常加载，无 `TabError`
- [ ] `_verify()` 返回 `{"target": ..., "result": "ok"}` 的 dict 通过 `save_TaskResult` 的入库检查
- [ ] Django 系统检查 0 issues

### 依赖
- `batch_task_executor.py` 的 `_build_exp_cache` 方法

### 状态记录
- 2026-05-13：Mode D-lite 修复。根因：POC 文件 tab/space 混用导致 TabError，被 `except Exception: continue` 静默吞掉。修复：统一缩进 + 异常分支加 `traceback.print_exc()`。

---

## BL-BATCH-003 历史空间测绘结果复用

- 模块：任务执行
- 优先级：P1
- 状态：已完成
- 当前阶段：阶段一（扩展）
- 价值：安全测试人员可以复用以往通过空间测绘引擎获取的目标数据，避免对同一查询重复调用测绘引擎 API。
- 关联模块文档：docs/modules/02-任务执行模块.md
- 关联计划：
- 关联代码：`app_cybersparker/models.py:126`, `app_cybersparker/views/expload/task_manage/batch_exp_task.py`
- 验收状态：已验收

### 范围

- 新增 `input_type=5`（历史空间测绘结果）
- 查询所有 `input_type=4` 任务的 `target` 文件作为可选列表，按 target 去重
- 列表展示：引擎类型、查询语句、目标数量、来源任务、获取时间
- 支持多选，合并时去重写入新 target 文件
- 创建/编辑批量任务时支持 input_type=5

### 不做

- 不做历史引擎结果 + 实时查询叠加
- 不做对历史结果文件的内容预览
- 不做文件大小显示

### 验收条件

- [ ] `inputType_choices` 增加 `(5, "history cyberspace results")`
- [ ] `GET /batch_exploadTask/history_engine_results` 返回去重后的历史引擎结果列表，每条含 engine_type、engine_query、target_count、task_name、creat_time
- [ ] `POST /batch_exploadTask/add` 传入 `input_type=5` 及 `history_engine_files[]` 数组，成功创建 `batch_EXPTask` 记录，target 文件为多文件合并去重结果
- [ ] Django 系统检查 0 issues，数据库迁移正常应用

### 依赖

- `batch_EXPTask` 表已有 `input_type=4` 记录
- `EXP_input/engine_assets/` 目录存在

### 状态记录
- 2026-05-13：创建 backlog item。
- 2026-05-13：实现完成。修改 models.py（input_type=5）、新增 migration 0006、batch_exp_task.py 新增 list/collect/handle 函数、urls.py 新增路由、前端模板+JS 新增 input_type=5 表单区。Django 系统检查 0 新 issue，URL 路由解析通过，模型 choices 验证通过。

---

## BL-BATCH-004 任务启动阻塞与进度条不更新修复

- 模块：任务执行
- 优先级：P0
- 状态：已完成
- 当前阶段：阶段一（修复）
- 价值：用户启动空间测绘引擎任务时立即响应，执行过程中进度条实时更新。
- 关联模块文档：docs/modules/02-任务执行模块.md
- 关联计划：
- 关联代码：`app_cybersparker/views/expload/task_manage/batch_exp_task.py:581-750`, `app_cybersparker/views/expload/task_manage/batch_task_executor.py:58-102,295-345`
- 验收状态：已验收

### 范围

**Bug 1：任务启动阻塞** — `prepare_engine_target_before_start()` 在 `operate()` HTTP 请求处理中同步调用，空间测绘引擎 API 分页获取数据时阻塞前端响应。修复：将数据获取移入后台线程 `startTask()` 内部执行。

**Bug 2：进度条不更新** — 三个根因：
- `Task_handler.__init__` 未初始化 `self.total_line_count`，`get_progress()` 先于 `producer()` 执行时抛 AttributeError 被静默吞掉
- `producer()` 中当 `self.progress` 为 `"100%"` 或 `None` 时不初始化 `consumer_number`，导致始终为 0
- `operate()` 中 `row_dict` 在 DB update 后未刷新，传给 `startTask()` 的是旧 progress 值

### 不做

- 不改变任务执行引擎架构
- 不添加异步任务队列（Celery 等）

### 验收条件

- [ ] `POST /batch_exploadTask/operate` 启动 input_type=4 任务时，HTTP 响应在 1s 内返回（不等待引擎 API 数据获取）
- [ ] 任务执行过程中，`batch_EXPTask.process` 字段从 DB 可观察到从 0% 逐步增长到 100%
- [ ] 任务完成后 `status=finish(1)`，`process="100%"`
- [ ] 非引擎任务（input_type≠4）启动和执行不受影响
- [ ] Django 系统检查 0 issues

### 依赖

- `prepare_engine_target_before_start()`（`batch_exp_task.py:306`）
- `Task_handler`（`batch_task_executor.py:58`）

### 状态记录
- 2026-05-13：创建 backlog item。
- 2026-05-13：实现完成。Bug 1：`prepare_engine_target_before_start()` 从 `operate()` HTTP 请求路径移入 `startTask()` 后台线程。Bug 2：`Task_handler.__init__` 初始化 `total_line_count=1`；`producer()` always init `consumer_number`；`operate()` 所有路径统一 reset `process="0%"` 并 refresh `row_dict`；`detail()` 增加 `process` 字段；前端新增 3s 轮询自动更新进度条。Django 检查 0 issues。
- 2026-05-14：意外发现并修复批量协程运行 Nuclei YAML 的耗时异常：默认 `timeout=10` 仍为单请求超时；多 path/raw 模板中单个 HTTP 请求超时/连接异常后不再继续同一目标后续请求，避免同一目标串行等待多个 10s。协程子进程改为轻量 gevent runner 先做 socket/SSL patch 再导入 requests/执行器，避免 HTTPS 退化为阻塞式并发。验证：`python manage.py test app_cybersparker` 13 tests OK，`python manage.py check` 0 issues。
- 2026-05-14：意外发现并修复”协程运行中暂停 -> 编辑为线程模式 -> 重启后进度停在 0%”问题。根因：前端 `.btn-startTask` 重复绑定旧停止操作导致启动/停止请求竞态；后端停止分支写入未定义 `status=0` 放大状态混乱。修复：确认按钮绑定前 `.off(“click”)`；停止任务时清理 `BATH_TASK_DIC` 并写合法 `status=3`。验证：`python manage.py test app_cybersparker.tests.BatchTaskGeventRunnerTests` 4 tests OK，`python manage.py test app_cybersparker` 15 tests OK，`python manage.py check` 0 issues。
- 2026-05-18：后续鲁棒性补强：批量任务 dispatch 失败时回滚运行态并标记 `failed`；结果保存线程对空队列超时改为继续等待，仅在退出且队列排空时自然结束，避免晚到结果无人处理。验证：`python manage.py test --keepdb --noinput app_cybersparker.tests.BatchTaskGeventRunnerTests app_cybersparker.tests.BatchScanCeleryDispatchTests.test_batch_start_rolls_back_runtime_state_when_dispatch_fails app_cybersparker.tests.AutoScanAsyncRequestTests.test_save_exp_result_continues_after_single_payload_failure -v 2` 7/7 通过，`python manage.py check` 0 issues。

---

## BL-BATCH-005 空间测绘引擎数据复用控制

- 模块：任务执行
- 优先级：P1
- 状态：已完成
- 当前阶段：阶段一（扩展）
- 价值：安全测试人员编辑批量任务时，如果查询语句未变化且上次成功获取了数据，可以复用已有数据而无需重新调用测绘引擎 API，节省 API 配额和时间。
- 关联模块文档：docs/modules/02-任务执行模块.md
- 关联计划：plans/2026-05-14-batch-expload-engine-reuse-and-resume.md
- 关联代码：`app_cybersparker/models.py:116`, `app_cybersparker/views/expload/task_manage/batch_exp_task.py`
- 验收状态：已验收

### 范围

- 新增 `reuse_engine_data` BooleanField 到 `batch_EXPTask` 模型（default=False）
- 编辑页面（input_type=4）新增下拉菜单：”是否重新获取数据”，选项”是，重新获取”（默认）/ “否，复用已有数据”
- 有效性判定（服务端）：当前查询语句 == 上次查询语句 且 上次 target 文件存在且有效 → 选项有效；否则选项无效，强制重新获取
- 后端编辑提交时根据有效性判定和用户选择决定是否重新调用引擎 API

### 不做

- 不在新建任务时显示此选项（新建必然需要获取数据）
- 不做前端 JS 实时判定有效性（仅在编辑时通过后端返回 `can_reuse_engine_data` 判断）

### 验收条件

- [ ] `batch_EXPTask` 新增 `reuse_engine_data` 字段，migration 正常应用
- [ ] 编辑 input_type=4 任务时，`GET /batch_exploadTask/detail` 返回 `reuse_engine_data` 和 `can_reuse_engine_data`
- [ ] 当条件满足（查询相同 + 上次成功）时，选择”否，复用已有数据”提交后 target 不变
- [ ] 当条件不满足时，无论用户选什么，后端强制重新获取数据
- [ ] 已保存 `reuse_engine_data=false` 的 input_type=4 任务，在后续启动/重跑时会重新调用引擎 API，不复用旧 target 文件；`resume` 续跑不触发重拉
- [ ] Django 系统检查 0 issues

### 依赖

- `batch_EXPTask` 模型
- `cyberspace_engine_service.py` 的 `fetch_and_dump_targets`

### 状态记录

- 2026-05-14：创建 backlog item。
- 2026-05-14：实现完成。模型新增 `reuse_engine_data` BooleanField + migration 0007。后端：`_can_reuse_engine_data()` 辅助函数；`detail()` 返回 `can_reuse_engine_data`；`resolve_target_source()` input_type=4 分支处理复用逻辑（条件：query 相同 + target 文件有效 + 用户选择复用）。前端：编辑表单 engine source 区域新增下拉菜单；JS 根据 `can_reuse_engine_data` 控制可用性。Django 检查 0 issues，15 tests OK。
- 2026-05-29：修复回归。此前 `reuse_engine_data=false` 只在编辑保存时生效，真正启动/重跑任务时 `prepare_engine_target_before_start()` 只看旧 target 文件是否存在，导致第二次启动仍直接复用旧 TXT。现改为在 `operate()` → Celery worker → `startTask()` 链路显式透传 `force_refresh_engine`，启动/重跑会按已保存配置决定是否重新拉取；`resume` 续跑保持原进度，不触发重新拉取。定向测试 3 条通过，`python manage.py check` 0 issues。
- 2026-05-29：继续补修查询变更场景。此前编辑 `input_type=4` 任务时，`resolve_target_source()` 用同一个实例同时承接旧值和表单新值；表单校验后再读取“旧查询语句”时，实际拿到的已经是新查询，所以即使用户把 `app="nginx"` 改成 `app="apache"`，后端仍会误判为“查询没变”，把旧 TXT 继续保留下来。现改为在 `form.is_valid()` 前先快照旧 `engine_query` 和旧 `target`，复用判断只比较快照值；只要检索语句变了，就强制落成 `reuse_engine_data=false` 且清空 `target`，等待下次启动/重跑重新抓取。新增 1 条请求级回归测试通过，`python manage.py check` 0 issues。
- 2026-05-29：补齐引擎切换边界。此前复用判断只比较查询语句和旧 target，若用户把 `engine_type` 从 `fofa` 切到 `hunter`，但查询字符串刚好一样，系统仍会误复用 FOFA 上一轮抓到的 TXT。现改为复用条件收紧为“同引擎 + 同查询 + 旧 target 仍存在”三者同时满足；只要切换引擎，也强制落成 `reuse_engine_data=false` 且清空 `target`。新增 1 条请求级回归测试覆盖“同查询但切换引擎”场景，定向测试 7/7 通过，`python manage.py check` 0 issues。

---

## BL-BATCH-006 批量任务续跑功能

- 模块：任务执行
- 优先级：P1
- 状态：已完成
- 当前阶段：阶段一（扩展）
- 价值：安全测试人员可以在任务中断后从未完成的目标继续执行，无需重新开始，节省时间和计算资源。
- 关联模块文档：docs/modules/02-任务执行模块.md
- 关联计划：plans/2026-05-14-batch-expload-engine-reuse-and-resume.md
- 关联代码：`app_cybersparker/views/expload/task_manage/batch_exp_task.py`, `app_cybersparker/views/expload/task_manage/batch_task_executor.py`
- 验收状态：已验收

### 范围

- 列表页为未完成任务（status=stop, 0% < process < 100%）显示”续跑”按钮
- 点击”续跑”弹出确认提示：”确认要从上次中断位置继续执行任务吗？”
- 后端 `operate()` 新增 resume 分支：不重置 process=”0%”，保持当前进度传给 Task_handler
- Task_handler 已有 resume 能力（producer() 根据 progress 计算 read_index 跳过已完成目标），直接复用
- 续跑启动后更新 status=2（running）

### 不做

- 不改变现有启动/停止逻辑
- 不支持已完成任务（process=100%）的续跑
- 不支持从未开始过（process=0%）的任务续跑

### 验收条件

- [ ] 列表页 status=stop 且 0% < process < 100% 的任务显示”续跑”按钮
- [ ] 点击”续跑”弹出确认对话框，提示文案正确
- [ ] 确认续跑后，Task_handler 从上次中断位置继续执行，已完成的目标不被重复处理
- [ ] 续跑完成后 status=finish(1)，process=”100%”
- [ ] Django 系统检查 0 issues

### 依赖

- `Task_handler.producer()` 的进度恢复逻辑（`batch_task_executor.py:296-338`）
- `operate()` 视图函数

### 状态记录

- 2026-05-14：创建 backlog item。
- 2026-05-14：实现完成。后端 `operate()` 新增 `action="resume"` 分支（不重置 process="0%"）。前端列表页新增"续跑"按钮（status=stop 且 0%<process<100% 时显示）；JS 新增 `ResumeTask()` / `ConfirmResumeTask()` / `initResumeButtons()`。Django 检查 0 issues，15 tests OK。
- 2026-05-20：批量删除收口。列表页“提交”按钮改为显式 `type="button"`，`bath_startOrStop_task()` 点击时强制 `preventDefault()` / `stopPropagation()`，先弹确认框再执行批删；`deleteModal` 关闭后清空 `pendingBatchDeleteIds`。新增静态回归测试覆盖按钮类型、点击拦截和隐藏后状态清理，`python manage.py check` 与 `python manage.py test app_cybersparker.tests.BatchTaskDeleteModalStateTests` 通过。

---

## BL-UX-001 自动扫描结果独立页面+美化

- 模块：任务执行
- 优先级：P2
- 状态：已完成
- 当前阶段：阶段一
- 价值：用户在一个独立、全宽的浏览器标签页中查看和检索自动扫描任务的识别结果，不再受后台侧边栏的窄空间限制。
- 关联模块文档：docs/modules/02-任务执行模块.md
- 关联计划：plans/2026-05-15-standalone-result-page.md
- 关联代码：`app_cybersparker/views/expload/task_manage/auto_scan_result.py`, `auto_scan_identify_result_standalone.html`
- 验收状态：已验收

### 范围

- 新独立模板页面（不 extend `project/index.html`），在新浏览器窗口/标签页打开
- 浅色专业风配色方案（白/浅灰背景，蓝/灰主色调）
- 响应式布局：统计面板与结果区自适应排列
- 搜索栏全宽置顶，统计面板可切换显隐
- 保留原页面所有功能（搜索语法、统计分组、字段展示设置、导出 CSV）

### 不做

- 不修改后端数据查询逻辑
- 不修改识别结果本身的展示内容结构
- 不改变现有页面（向后兼容，原页面保留）

### 验收条件

- [ ] 独立页面在新标签页中打开，无后台侧边栏，内容全宽展示
- [ ] 配色为浅色专业风（白/浅灰底，蓝/灰主色）
- [ ] 页面在 1920px / 1366px / 1024px 宽度下均无明显错位
- [ ] 搜索功能正常：输入搜索条件后能过滤结果并更新统计面板
- [ ] 导出 CSV 功能正常
- [ ] 原后台内的结果页保持可用（向后兼容）

### 依赖

- `auto_scan_result.py` 视图
- `auto_scan_indentify_result.html` 模板

### 状态记录
- 2026-05-15：创建 backlog item。
- 2026-05-15：实现完成。新建 `auto_scan_identify_result_standalone.html` 独立模板（浅色专业风+CSS Grid响应式）；`Task_result` 新增 `standalone` kwarg；新增 `/Identify_task/<uid>/result/standalone` 路由；任务列表新增独立窗口链接。Django 检查 0 issues，15 tests OK。

---

## BL-BATCH-007 批量任务投递迁移到 Celery

- 模块：任务执行
- 优先级：P0
- 状态：已完成
- 当前阶段：阶段二（调度改造主线）
- 价值：批量任务启动从 Django 请求线程解耦，保留现有线程/gevent 执行器以降低第一阶段迁移风险。
- 关联模块文档：docs/modules/02-任务执行模块.md
- 关联计划：plans/2026-05-16-redis-celery-global-scheduler-plan.md
- 关联代码：`app_cybersparker/views/expload/task_manage/batch_exp_task.py`, `app_cybersparker/views/expload/task_manage/batch_task_executor.py`
- 验收状态：已验收

### 范围

- `POST /batch_exploadTask/operate` 启动路径改为写 DB 状态并投递 `batch_scan` Celery task。
- API 写入 `queued + dispatch_token`，Celery 消息携带 token；worker 侧校验 token 和非终态后 claim owner。
- Celery task 内部暂时调用现有 `batch_task_executor.Task_handler`。
- 保留线程模式和 gevent 子进程模式。
- 本项自带最小 stop bridge：worker/执行器轮询 DB `stop_requested` 与 Redis stop flag，并映射到本地 `exit_flag`。
- `BATH_TASK_DIC` 只作为本机执行器句柄缓存，不能反向覆盖 DB 终态。
- gevent 模式本阶段保留 `terminate()` fallback，cooperative stop 作为后续细化。
- 定义 worker 异常退出时批量任务的 `failed` 或 `stopped` 恢复规则，终态写入必须走 `dispatch_token/owner` CAS。

### 不做

- 不引入全局资源令牌。
- 不改结果写入为 Redis Stream。
- 不删除 gevent 子进程隔离策略。

### 验收条件

- [ ] 调用批量任务启动接口后，Django 请求线程只投递 Celery task，不创建长生命周期扫描线程或子进程。
- [ ] 同一 `task_id` 重复投递时，只有当前 `dispatch_token/owner` 对应的 worker 能执行，重复消息 no-op。
- [ ] Celery worker 在线程模式下执行后，`batch_EXPTask.status/process` 与旧执行路径一致。
- [ ] Celery worker 在 gevent 模式下仍通过子进程入口执行，monkey patch 不污染 worker 主进程。
- [ ] 停止 flag 写入 Redis/DB 后，线程模式通过 stop bridge 在循环检查点设置 `exit_flag` 并停止。
- [ ] gevent 模式停止至少能通过 `terminate()` fallback 收敛到 `stopped`，且不把旧 token 终态写回 DB。
- [ ] 旧 token、重复消息、迟到 stop 不会覆盖已有终态。
- [ ] 模拟 worker 异常退出后，任务状态按规则恢复为 `stopped` 或 `failed`，不会长期停留在 `running`。
- [ ] `python manage.py check` 0 issues。

### 依赖

- `BL-SCHED-002`。

### 状态记录

- 2026-05-16：基于 Redis + Celery 全局任务调度计划拆分，待用户确认优先级与范围。
- 2026-05-16：阶段二验收通过。9/9 验收条件有代码+测试证据支撑（dispatch token CAS、线程/gevent stop bridge、异常恢复规则）。
- 2026-05-20：后续修复 Celery worker + gevent 子进程冲突。`run_mode=2` 在 daemon worker 内不再二次 `spawn` 子进程，改为直接复用 `Task_handler` 在线程内执行 gevent 模式，避免 `AssertionError: daemonic processes are not allowed to have children`。定向验证：`python manage.py test app_cybersparker.tests.BatchTaskDaemonGuardTests --keepdb --noinput -v 2` 2/2 通过，`python manage.py check` 0 issues。
- 2026-05-20：继续修复 gevent HTTPS TLS 递归。将 gevent monkey patch 明确改为 `ssl=False`，避免在 Python 3.11 + requests/urllib3 链路下重写 `ssl.SSLContext` 后触发 `minimum_version` 递归。定向验证：`/opt/venv/bin/python manage.py test app_cybersparker.tests.RequestRuntimePatchTests app_cybersparker.tests.BatchTaskDaemonGuardTests --keepdb --noinput -v 2` 10/10 通过；`/opt/venv/bin/python manage.py check` 0 issues。
- 2026-05-20：发现上一轮 daemon worker 直跑 gevent 会污染同一 Celery worker 进程，导致后续自动扫描线程池报 `gevent.exceptions.LoopExit`。先补安全兜底：daemon worker 下 `run_mode=2` 不再直接在当前 worker 内启 gevent patch。定向验证：`/opt/venv/bin/python manage.py test app_cybersparker.tests.BatchTaskDaemonGuardTests --keepdb --noinput -v 2` 2/2 通过，`/opt/venv/bin/python manage.py check` 0 issues。
- 2026-05-20：继续收口为正式方案。新增独立队列 `batch_scan_gevent` 和专用 `worker_gevent`，`run_mode=2` 改为投递到协程专用 worker；线程模式继续走 `batch_scan` 主 worker，避免线程/协程/自动扫描共用同一 worker 进程。定向验证：`/opt/venv/bin/python manage.py test app_cybersparker.tests.CeleryRuntimeInfrastructureTests.test_celery_app_declares_expected_queues app_cybersparker.tests.BatchQueueRoutingTests app_cybersparker.tests.BatchTaskDaemonGuardTests --keepdb --noinput -v 2` 5/5 通过，`sh -n start_celery.sh` 通过。

---

## BL-BATCH-008 批量任务全局预算与结果事件治理

- 模块：任务执行
- 优先级：P1
- 状态：已完成
- 当前阶段：阶段二
- 价值：批量任务在线程/gevent 两种模式下共享全局线程/协程/DB writer 预算，并通过结果事件缓冲降低数据库写入压力。
- 关联模块文档：docs/modules/02-任务执行模块.md
- 关联计划：plans/2026-05-16-redis-celery-global-scheduler-plan.md
- 关联代码：`app_cybersparker/views/expload/task_manage/batch_task_executor.py`
- 验收状态：已验收

### 范围

- 线程模式启动前申请线程预算，退出后释放。
- gevent 模式启动前申请协程/greenlet 预算，子进程退出后释放。
- 漏洞结果写入结果事件缓冲，由 DB writer 入库。
- 停止任务循环检查 Redis/DB flag。

### 不做

- 不重写插件同步执行接口。
- 不改变批量任务输入源解析逻辑。
- 不改 gevent monkey patch 的子进程隔离边界。

### 验收条件

- [ ] 线程模式实际 worker 数不超过申请到的全局线程预算。
- [ ] gevent 模式实际 greenlet 数不超过申请到的全局协程预算。
- [ ] gevent 子进程退出后，协程预算 lease 被释放或 TTL 回收。
- [ ] 批量漏洞结果进入 Redis Stream，并由 DB writer 写入 `EXPTask_result`。
- [ ] 停止 flag 写入 Redis/DB 后，线程模式和 gevent 模式均可停止。

### 依赖

- `BL-BATCH-007`。
- `BL-SCHED-005`。
- `BL-SCHED-006`。

### 状态记录

- 2026-05-16：基于 Redis + Celery 全局任务调度计划拆分，待用户确认优先级与范围。
- 2026-05-16：线程/gevent 消费者数纳入全局资源预算约束；新增 6 个测试全通（11/11 OK，Pyright 0/0/0，Django check 0 issues）。
- 2026-05-16：阶段二验收通过。5/5 验收条件通过（线程/协程预算、gevent 释放、Stream 写入、停止）。

---

## BL-TASK-002

- 模块：任务执行
- 优先级：P1
- 状态：已完成
- 当前阶段：阶段二
- 价值：在删除或迁移单任务模块前明确 `Attact/cmd_input` 是否仍有业务价值，避免功能回退。
- 关联模块文档：docs/modules/02-任务执行模块.md
- 关联计划：plans/2026-05-16-redis-celery-global-scheduler-plan.md
- 关联代码：`app_cybersparker/views/expload/task_manage/exp_task.py`
- 验收状态：已验收

### 范围

- 复用 `BL-SCHED-001` 的 `EXPTask.taskType` 与非空 `cmd_input` 统计。
- 检查单任务前端入口、API、结果页和执行器对 `Verify` / `Attact` 的依赖。
- 给出删除、保留或迁移到批量任务的明确建议。
- 若建议删除，列出批量任务需要补齐的能力和只读归档要求。
- 若建议保留，列出单任务迁移到 Celery 的前置条件。

### 不做

- 不删除单任务代码。
- 不隐藏前端入口。
- 不实现 `Attact/cmd_input` 的批量任务等价能力。

### 验收条件

- [ ] 有可复查的数据统计：`EXPTask.taskType` 分组数量、非空 `cmd_input` 数量、最近使用时间。
- [ ] 列出前端入口、API、结果页和执行器中依赖 `Attact/cmd_input` 的代码位置。
- [ ] 明确说明批量任务当前是否覆盖 `Verify` 和 `Attact` 两类语义。
- [ ] 给出删除/保留/迁移三选一建议，并说明理由。
- [ ] 后续开发事项记录删除前置条件或保留迁移条件。

### 依赖

- `BL-SCHED-001`。

### 状态记录

- 2026-05-16：基于 Redis + Celery 全局任务调度计划拆分，待用户确认优先级与范围。
- 2026-05-16：审计完成。`taskType=1` (Verify) 批量任务已覆盖；`taskType=2` (Attact + cmd_input) 为单任务独有能力。建议后续在批量任务补齐 Attact 后归档单任务。
- 2026-05-16：阶段二验收通过。5/5 验收条件满足（数据统计、代码位置、语义覆盖、三选一建议、后续事项记录）。小缺口：统计缺"最近使用时间"字段，已记录到后续开发事项。

---

## BL-TASK-003 单任务 Attact 能力迁移到批量任务

- 模块：任务执行
- 优先级：P2
- 状态：已完成
- 当前阶段：阶段三
- 价值：让批量任务支持 Attact 模式（Shell 命令调用 POC 的 `_attact` 方法），统一任务执行入口，后续可归档单任务模块。
- 关联模块文档：docs/modules/02-任务执行模块.md
- 关联计划：
- 关联代码：`app_cybersparker/views/expload/task_manage/exp_task.py`, `app_cybersparker/views/expload/task_manage/batch_task_executor.py`, `app_cybersparker/models.py:69`
- 验收状态：已验收

### 范围
- `batch_EXPTask` 新增 `task_type`（1=Verify, 2=Attact）和 `cmd_input` 字段
- `batch_task_executor.py` 消费者按 `task_type` 选择 `verify` 或 `attact` + `cmd_input`
- 完成后将单任务 UI 入口标记为 deprecated，代码保留只读归档

### 不做
- 不删除单任务代码（只标记 deprecated）
- 不改变现有 Verify 模式的执行逻辑

### 验收条件
- [ ] `batch_EXPTask.task_type=2` 且设置 `cmd_input` 后，消费者调用 POC 的 `_attact` 方法
- [ ] 单任务列表页显示 deprecated 标记
- [ ] 现有 Verify 模式不受影响
- [ ] `python manage.py check` 0 issues

### 依赖
- `batch_EXPTask` 模型
- `batch_task_executor.py`

### 状态记录
- 2026-05-23：从 `docs/后续开发事项.md` + BL-TASK-002 审计结论迁移到正式 backlog。

---

## BL-NUCLEI-001 Nuclei helper 函数补齐（15 个）

- 模块：任务执行
- 优先级：P0
- 状态：已完成
- 当前阶段：阶段三增强
- 价值：当前引擎 B 仅 20 个 helper 函数，大量社区模板依赖缺失函数生成 payload 或做编码变换导致执行失败。补齐后显著提升模板兼容率。
- 关联模块文档：docs/modules/02-任务执行模块.md
- 关联计划：
- 关联代码：`app_cybersparker/views/expload/task_manage/nuclei_runtime_engine.py`（SAFE_FUNCTIONS 字典 + 函数实现）
- 验收状态：已验收

### 范围

补齐以下 15 个高频 helper 函数，与引擎 A 签名和行为对齐：

| # | 函数 | 用途 | 实现方式 |
|---|------|------|---------|
| 1 | `url_encode` | URL 编码 | `urllib.parse.quote` |
| 2 | `url_decode` | URL 解码 | `urllib.parse.unquote` |
| 3 | `sha256` | SHA256 哈希 | `hashlib.sha256` |
| 4 | `rand_int` | 随机整数 | `random.randint` |
| 5 | `rand_text_alpha` | 随机字母串 | `random.choices` |
| 6 | `rand_text_alphanumeric` | 随机字母数字串 | `random.choices` |
| 7 | `rand_text_numeric` | 随机数字串 | `random.choices` |
| 8 | `replace` | 字符串替换 | `str.replace` |
| 9 | `replace_regex` | 正则替换 | `re.sub` |
| 10 | `gzip` | Gzip 压缩 | `gzip.compress` |
| 11 | `gzip_decode` | Gzip 解压 | `gzip.decompress` |
| 12 | `date_time` | 当前时间格式化 | `datetime.now().strftime` |
| 13 | `unix_time` | Unix 时间戳 | `int(time.time())` |
| 14 | `join` | 数组拼接字符串 | `str.join` |
| 15 | `trim` | 去首尾空白 | `str.strip` |

### 不做

- 中低频 24 个函数（后续按需补）
- 引擎 A 中未实现的函数（`aes_gcm`、`generate_java_gadget`、`print_debug`）
- 修改引擎 A 的 pocsuite3 代码

### 验收条件

- [ ] 15 个函数全部在引擎 B `SAFE_FUNCTIONS` 字典中注册
- [ ] 每个函数至少 1 个正确性测试（输入→期望输出），覆盖边界值（如 `rand_int(1, 10)` 结果在范围内）
- [ ] `python manage.py check` 0 issues
- [ ] `rtk pytest tests/ -x -q` 全量通过，无新增失败

### 依赖

- 引擎 B 的 `_safe_eval_expression` 现有机制
- stdlib 模块（urllib.parse / hashlib / random / gzip / re / datetime / time）

### 状态记录

- 2026-05-25：Mode B0 澄清完成，创建 backlog item。
- 2026-05-25：实现完成。引擎 B 新增 `_url_encode`/`_sha256`/`_gzip`/`_replace`/`_trim`/`_join`/`_rand_int`/`_date_time`/`_unix_time` 等 15 个函数实现并注册到 `SAFE_FUNCTIONS`（从 20 个增至 35 个）。新增 `urllib.parse.quote/unquote`、`gzip`、`datetime` 等 import。15 个函数 DSL 求值集成测试全部通过。

---

## BL-NUCLEI-002 JSON Extractor + XPath Matcher/Extractor + Cookie 复用

- 模块：任务执行
- 优先级：P1
- 状态：已完成
- 当前阶段：阶段三增强
- 价值：JSON extractor 让引擎 B 能处理现代 API 漏洞模板的 token/session 提取链；XPath 补全 XML/SOAP 接口的匹配和提取；cookie 复用让需要登录态的多请求模板正常工作。
- 关联模块文档：docs/modules/02-任务执行模块.md
- 关联计划：
- 关联代码：`app_cybersparker/views/expload/task_manage/nuclei_runtime_engine.py`
- 验收状态：已验收

### 范围

**JSON Extractor**：
- jq 风格简化路径解析：`.key.subkey`、`[0]`、`.key[0].sub`
- 不支持：负索引 `[-1]`、嵌套数组 `[0][1]`、key 中含 `.` 的转义、filter/pipe/条件表达式
- 纯 Python 实现（不用 `jq` C 扩展）
- 支持 `internal: true` 变量注入给下游请求

**XPath Matcher**：
- 对 HTML/XML 响应体执行 XPath 表达式，选中节点即匹配成功
- 复用已有 `lxml` 库

**XPath Extractor**：
- 从 HTML/XML 响应体按 XPath 提取节点文本或属性值
- 支持 `attribute` 属性（如 `@href`）
- 支持 `internal` 变量注入

**Cookie/Session 复用**：
- 同一个 YAML 模板的所有 HTTP request 块共用同一个 `requests.Session()`
- 在 `run_nuclei_template` 创建 session，传入各 `_execute_http_request` 调用

### 不做

- 完整 jq 语法（filter、pipe、条件）
- XPath 2.0/3.0 特性
- 跨模板 cookie 持久化
- 手动 cookie 管理页面

### 验收条件

- [ ] JSON extractor：从 `{"data": {"token": "abc"}}` 提取 `.data.token` 得到 `"abc"`
- [ ] JSON extractor：从 `{"items": [{"name": "a"}, {"name": "b"}]}` 提取 `.items[0].name` 得到 `"a"`
- [ ] JSON extractor 提取值通过 `internal` 注入到动态变量
- [ ] XPath matcher：`//title[contains(text(),'Login')]` 匹配 HTML `<title>Login Page</title>` 返回 True
- [ ] XPath extractor：`//a/@href` 提取 `<a href="/admin">` 得到 `"/admin"`
- [ ] 同模板内第 1 个请求的 Set-Cookie 被第 2 个请求携带
- [ ] 不同模板执行互不影响（各自独立 session）
- [ ] 非 JSON 输入上 JSON extractor 不抛异常；非 XML 输入上 XPath 不抛异常
- [ ] `python manage.py check` 0 issues

### 依赖

- `lxml`（已在 requirements.txt）
- 引擎 B 现有 `requests.Session()` 使用模式
- 引擎 B 现有 extractor/matcher 调度框架

### 状态记录

- 2026-05-25：Mode B0 澄清完成，创建 backlog item。
- 2026-05-25：实现完成。JSON extractor：新增 `_json_path_get`（`.key`/`[0]`/`.key[0].sub`）+ `_extract_json`。XPath matcher/extractor：新增 `_match_xpath` + `_extract_xpath`（lxml，attribute 提取，internal 注入），注册到 `_apply_matchers`/`_apply_extractors`。Cookie 复用：`_execute_http_request` 新增 `session` 参数，`run_nuclei_template` 创建共享 `requests.Session()`。8 项集成测试通过。

---

## BL-BATCH-009 批量漏洞任务"从检索语句导入"输入源

- 模块：任务执行
- 优先级：P1
- 状态：已完成
- 当前阶段：阶段四
- 价值：用户可以直接用资产检索语法圈定批量漏洞验证的目标资产，不用先导出 URL 再上传文件。
- 关联模块文档：docs/modules/02-PoC管理模块.md
- 关联计划：plans/2026-05-27-检索语句导入任务输入源.md
- 关联代码：`app_cybersparker/models.py` (batch_EXPTask), `app_cybersparker/views/expload/task_manage/batch_exp_task.py`, `app_cybersparker/views/expload/task_manage/batch_task_executor.py`
- 验收状态：已验收

### 范围

- `batch_EXPTask` 模型：`input_type` 新增 `(6, "从检索语句导入")`；新增 `search_query` / `parsed_query` / `frozen_max_id` / `last_id` 字段（migration 由 BL-AUTO-016 统一生成）
- 创建/编辑表单：`input_type` 下拉新增选项，选中后展示 search_query textarea
- 创建时校验：复用共享解析器校验语法 + 匹配数 > 0
- 编辑检索语句后：若正在运行则先停止，再重新校验、重新冻结 `frozen_max_id`/`parsed_query`、`last_id` 归零、status 重置为 3(停止)
- 执行期消费：`input_type=6` 时主线程持有 `iter_search_query_targets` generator，产出的 URL 通过 `queue.Queue` 分发给 worker 线程
- 注：批量任务有 `run_mode=1`（线程）和 `run_mode=2`（gevent 子进程）两种模式，`input_type=6` 在两种模式下的 generator 消费行为一致——generator 由主线程持有，`filter().update()` 不受 gevent monkey-patch 影响（`ssl=False` 不涉及 Django ORM）

### 不做

- 不修改现有 input_type 1-5 的消费逻辑
- 不做数量上限

### 验收条件

- [ ] `POST /batch_exp_task/add` 且 `input_type=6`：检索语句语法错误 → 拒绝创建并提示
- [ ] 匹配资产数为 0 → 拒绝创建并提示
- [ ] 匹配 >0 → 创建成功，字段写入 DB
- [ ] 编辑检索语句后保存：`last_id` 归零，status 重置为 3(停止)
- [ ] 编辑检索语句且任务正在运行时：先停止再重置
- [ ] 执行时 `input_type=6` 走 generator + keyset 分页，`last_id` 每批更新
- [ ] `run_mode=2`（gevent 子进程）模式下 `input_type=6` 消费正常
- [ ] `python manage.py check` 0 issues

### 依赖

- BL-AUTO-016（共享 parser 收口 + migration）

### 状态记录

- 2026-05-27：Mode B0 需求澄清完成，创建 backlog item。
- 2026-05-27：独立审查（NEEDS_REVISION）→ 修订：状态重置明确为 3(停止)、新增 last_id 归零 & running 先停 & gevent 模式验收条件
- 2026-05-27：实现完成。batch_EXPTask 视图 input_type=6 校验、batch_task_executor executor `_producer_from_search_query` keyset 分页消费、tasks.py values() 新增字段。django check 0 issues。
- 2026-05-28：补修运行态缺口。`input_type=6` 的重跑/重启现在会显式重置 `last_id=0`，resume 保留原游标；batch 执行器的检索语句进度总数和已完成数改为按冻结范围统计，避免进度失真。

---

## BL-BATCH-012 批量任务暂停状态与续跑

- 模块：任务执行
- 优先级：P1
- 状态：已完成
- 当前阶段：阶段六后续增强
- 价值：用户可以像自动扫描一样对批量任务发起优雅暂停，暂停后保留当前进度并可继续从中断位置续跑，不再只能硬停或误以为暂停无效。
- 关联模块文档：docs/modules/02-任务执行模块.md
- 关联计划：plans/2026-05-28-batch-pause-resume.md
- 关联代码：`app_cybersparker/models.py`, `app_cybersparker/views/expload/task_manage/batch_exp_task.py`, `app_cybersparker/views/expload/task_manage/batch_task_executor.py`, `app_cybersparker/tasks.py`, `batch_Expload_Task.js`, `bath_exp_task_list.html`
- 验收状态：已验收

### 范围

- `batch_EXPTask` 新增 `pause_requested` 字段，`status_choices` 增加 `pause`（4）
- 运行中的批量任务支持“暂停”：producer 停止读取新目标，已进入队列的目标继续处理完，再落为 `pause`
- 暂停后的批量任务支持“续跑”：沿用现有进度百分比恢复逻辑，从未完成目标继续执行
- 列表页操作按钮按状态重组（running→暂停，stop→启动+续跑，pause→续跑+重跑，finish→重跑）
- 前端状态标签新增“已暂停 / 暂停中”文案，交互参考自动扫描页面

### 不做

- 不删除现有 stop 能力（保留给接口和异常兜底）
- 不改单任务、自动扫描、目录扫描的状态模型
- 不额外新增阶段字段或新的执行模式

### 验收条件

- [ ] 运行中的批量任务点击暂停后，不再读取新目标；队列中已有目标处理完后状态落为 `pause(4)`
- [ ] 暂停后的任务列表显示“续跑”按钮，点击后从上次中断位置继续执行
- [ ] 暂停后的任务点击重跑时，进度重置为 `0%`，从头开始执行
- [ ] `python manage.py check` 0 issues
- [ ] 定向回归测试覆盖 pause 信号、pause→resume、stop 修复场景

### 依赖

- `BL-BATCH-007`（Celery 批量任务链路）
- `BL-BATCH-008`（批量任务执行器与预算治理）

### 状态记录

- 2026-05-28：用户反馈批量任务“暂停后仍继续跑”，先修 stop 信号在当前目标剩余插件中不及时生效的问题；随后扩展正式 pause 状态与续跑交互。
- 2026-05-28：实现完成。模型新增 `pause_requested` + `status=4(pause)`；批量任务入口支持 `status=pause`；执行器新增 `check_pause_signal()`，暂停后停止读取新目标并在队列排空后落为已暂停；列表页新增暂停按钮与暂停中/已暂停文案；补充 15 条批量任务回归测试通过。
- 2026-05-29：补修批量任务对不支持的 Nuclei `code` 协议处理。此前 YAML 包装层会返回 `RuntimeMethodResult({"matched": False, "result": "unsupported nuclei protocol: code"})`，自动扫描会先跳过，但批量任务 `consumer_exp()` 仍直接入队，最终把错误文字当成成功验证结果入库。现改为批量任务先判断 `if not result: continue`，`matched=False` 的 YAML 返回直接跳过，不再参与结果入库。定向回归测试 2 条通过，`python manage.py check` 0 issues。

---

## BL-BATCH-010 batch_EXPTask 加 EXP 筛选字段 + 迁移 + 表单

- 模块：任务执行
- 优先级：P0
- 状态：已完成
- 当前阶段：阶段六
- 价值：批量漏洞任务在"手动选择 EXP"之外新增"按 severity 和 tags 筛选 EXP"模式，用户灵活控制跑哪些漏洞验证模板。
- 关联模块文档：docs/modules/02-任务执行模块.md
- 关联计划：plans/2026-05-27-EXP-severity-tags分类与任务过滤.md
- 关联代码：
  - `app_cybersparker/models.py`（batch_EXPTask）
  - `app_cybersparker/views/expload/task_manage/batch_exp_task.py`（batch_ExpTask_ModelForm + 视图）
- 验收状态：已验收

### 范围
- `batch_EXPTask` 新增 4 个字段：
  - `exp_select_mode`：SmallIntegerField，choices (1=手动选择, 2=按条件筛选)，default=1
  - `severity_filter`：JSONField，nullable。结构 `{"mode": "include/exclude", "values": ["critical", "empty", ...]}`
  - `tag_filter`：JSONField，nullable。结构 `{"mode": "include/exclude", "values": [1, 2, "empty"]}`
  - `filter_logic`：CharField，choices (AND/OR)，default="AND"
- 迁移文件（0044）
- `batch_ExpTask_ModelForm`：`fields` 列表新增上述 4 个字段
- 前端：`exp_select_mode=1` 显示插件多选；`exp_select_mode=2` 隐藏多选，显示筛选条件面板（severity 勾选 + tags 多选 + include/exclude + AND/OR）

### 不做
- 不修改 `batch_EXPTask.EXP` 字段（CharField）的存储方式
- 不修改 `exp_select_mode=1` 时的 UI 和逻辑

### 验收条件
- [ ] `python manage.py makemigrations` 生成 0044（batch_EXPTask 新增字段）
- [ ] 创建批量任务，`exp_select_mode=2`，`severity_filter={"mode":"include","values":["critical","high"]}`，保存后 DB 值正确
- [ ] 表单白名单包含 4 个新字段
- [ ] 前端 `exp_select_mode=1` 时显示原有插件多选
- [ ] 前端 `exp_select_mode=2` 时隐藏插件多选，显示筛选面板
- [ ] `python manage.py check` 0 issues

### 依赖
- BL-PLUGIN-005（EXP.severity + Tag 模型已存在，筛选依赖这两个字段）
- BL-PLUGIN-006（EXP 后台页面已就绪，可查看现有模板的 severity/tags 辅助调试）

### 状态记录
- 2026-05-27：Mode B 生成，创建 backlog item。
- 2026-05-27：实现完成。模型 4 字段 + 0044 迁移 + ModelForm；前端筛选面板（exp_select_mode 切换 + severity 多选 + tag 多选下拉 + include/exclude + AND/OR）；detail API 补全筛选字段和 search_query。

---

## BL-BATCH-011 批量任务执行引擎按筛选条件过滤 EXP

- 模块：任务执行
- 优先级：P0
- 状态：已完成
- 当前阶段：阶段六
- 价值：执行时按用户配置的 severity/tags 筛选实际运行的 EXP 列表，不设筛选时默认排除纯信息探测类模板（severity=info）。当前口径统一为：python3 和 nuclei_yaml 两种 EXP 都参与筛选。
- 关联模块文档：docs/modules/02-任务执行模块.md
- 关联计划：plans/2026-05-27-EXP-severity-tags分类与任务过滤.md
- 关联代码：
  - `app_cybersparker/views/expload/task_manage/batch_task_executor.py`（`_build_exp_cache`、`operate`/`startTask` 入口）
  - `app_cybersparker/views/expload/task_manage/batch_exp_task.py`（`resolve_exp_ids`）
- 验收状态：已验收

### 范围
- 在 `operate()` / `startTask()` 入口处：若 `exp_select_mode=2`，调用 `resolve_exp_filter(task)` 从 severity/tags 筛选出 EXP ID 列表，赋值给 `self.expID_list`
- `resolve_exp_filter(task)` 实现：
  - 两个 filter 都为空时 → 默认 `qs.exclude(severity="info")`
  - 有筛选条件时 → 按 include/exclude + AND/OR 构建 Q 对象
  - `_build_severity_q` 处理 `"empty"`（severity="" 的模板）
  - `_build_tag_q` 处理 `"empty"`（无 tag 的模板）
- `batch_exp_task.py` 的 `resolve_exp_ids()` 新增 `exp_select_mode=2` 分支

### 不做
- 不修改 `exp_select_mode=1`（手动选择）时的 EXP 列表构建逻辑
- 不修改 `_build_exp_cache()` 的缓存结构

### 验收条件
- [ ] `exp_select_mode=2`，`severity_filter={"mode":"include","values":["critical","high"]}` → 执行时只跑 critical+high 的 EXP
- [ ] `exp_select_mode=2`，`tag_filter={"mode":"exclude","values":[tag_detect_id]}` → 执行时排除含 detect 标签的 EXP
- [ ] `exp_select_mode=2`，`severity_filter` + `tag_filter` 都设置，`filter_logic="AND"` → 两个条件同时满足
- [ ] `exp_select_mode=2`，筛选条件为空 → 默认排除 severity=info 的 EXP
- [ ] `exp_select_mode=2`，`severity_filter` 含 `"empty"` → severity="" 的模板被纳入
- [ ] `exp_select_mode=1` → 原有手动选择行为不变
- [ ] `python manage.py check` 0 issues

### 依赖
- BL-BATCH-010（batch_EXPTask 筛选字段 + 表单已就绪）
- BL-PLUGIN-005（EXP.severity + Tag 模型已存在）

### 状态记录
- 2026-05-27：Mode B 生成，创建 backlog item。
- 2026-05-27：实现完成。batch_task_executor 新增 `resolve_exp_filter` + `_build_severity_q` + `_build_tag_q`；exp_select_mode=2 时从筛选条件构建 expID_list；空筛默认排除 info；severity 不允许空值。

---

## BL-TASK-010 任务自定义参数 task_args

- 模块：任务执行
- 优先级：P1
- 状态：已完成
- 当前阶段：阶段十六
- 价值：用户在创建任务时可输入 JSON 格式的自定义参数，执行时自动注入 `target["task_args"]`，Python PoC 插件内直接取用，实现任务级别的动态参数传递（如回连地址、payload 前缀等）。
- 关联模块文档：docs/modules/02-任务执行模块.md
- 关联计划：plans/2026-06-16-task-args-custom-param.md
- 关联代码：`app_cybersparker/models.py` (batch_EXPTask/auto_scan_tasks/DirScanTask)、`app_cybersparker/tasks.py`、`app_cybersparker/views/expload/task_manage/auto_scan_task.py`、`app_cybersparker/views/expload/task_manage/batch_exp_task.py`、`app_cybersparker/views/expload/task_manage/batch_task_executor.py`、`app_cybersparker/views/expload/task_manage/auto_exp_task.py`、`app_cybersparker/services/dirscan_worker.py`、`app_cybersparker/views/expload/exp_debug.py`
- 验收状态：已验收

### 范围

- 三个任务模型各加 `task_args` TextField（JSON 字符串，可空）
- tasks.py `.values()` 查询列表补充 `task_args` 字段
- 启动层（startTask/phase1）解析 JSON → 放入 data dict → 传递到执行器（不额外查 DB）
- 执行器 consumer 构建 target dict 时注入 `task_args`（仅 Python 插件，YAML 不注入）
- 三种任务的前端表单加 TextArea 输入框（JSON 格式校验）
- 调试页（ExpDebugPage）加 task_args TextArea 输入框，执行时传入后端
- 调试页后端解析 task_args 并注入 target dict（仅 Python 插件）

### 不做

- 不在 Nuclei YAML 插件执行时注入 task_args
- 不限制 task_args 的 JSON 结构
- 不在该字段加数据库索引
- 不删除 `EXP_plugin/` 下的残留文件

### 验收条件

- [ ] 三种任务的创建/编辑表单可输入 task_args，DB 有对应值
- [ ] `python manage.py check` 0 issues
- [ ] `python manage.py test app_cybersparker.tests --parallel` 通过
- [ ] 批量任务 Python 插件执行时 `target["task_args"]` 为已配置的 dict
- [ ] 批量任务 YAML 插件执行时 target 中无 task_args 字段
- [ ] 不填 task_args 时 `target["task_args"] == {}`

### 依赖

- `batch_EXPTask` / `auto_scan_tasks` / `DirScanTask` 模型
- `poc_runtime_resolver.py` — target dict 构造
- 三种任务的前端 React 表单页

### 状态记录

- 2026-06-16：计划方案生成。待用户批准后进入 Mode D 实现。
