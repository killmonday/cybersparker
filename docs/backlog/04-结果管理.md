# 04-结果管理

## BL-RESULT-001 统一结果管理

- 模块：结果管理
- 优先级：P1
- 状态：已完成
- 当前阶段：阶段一
- 价值：安全测试人员可以统一查看、复验、导出所有任务的执行结果。
- 关联模块文档：docs/modules/04-结果管理模块.md
- 关联计划：
- 关联代码：`app_cybersparker/views/expload/result__manage/expResult.py`, `app_cybersparker/views/expload/result__manage/all_Indentify_result.py`, `app_cybersparker/views/expload/result__manage/all_auto_exp_result.py`
- 验收状态：已验收

### 范围
- 统一漏洞结果列表（分 task_type 区分单/批）
- 结果详情查看
- 结果复验（重新调用插件验证）
- 结果导出（CSV/JSON 下载）
- 结果删除
- 全量识别结果列表/导出
- 全量联动漏洞结果列表/导出/删除

### 不做
- 结果搜索/过滤（当前仅有基本分页）
- 结果统计分析/图表
- 批量复验

### 验收条件
- [ ] `GET /result_List` 返回所有类型结果，含正确分页
- [ ] `POST /targetRunVerify` 对指定目标重新执行验证，产出新结果或更新旧结果
- [ ] `POST /result_download` 导出结果为可下载文件
- [ ] `POST /result_delete` 删除指定结果记录
- [ ] `GET /all_Indentify_result` 返回全量识别结果
- [ ] `GET /auto_exp_result` 返回全量联动漏洞结果

### 依赖
- `EXPTask_result` 模型（`app_cybersparker/models.py:103`）
- `auto_scan_indentify_result` 模型（`app_cybersparker/models.py:207`）
- `auto_scan_exp_result` 模型（`app_cybersparker/models.py:198`）

### 状态记录
- 2026-05-13：基于代码审查确认功能已实现，待验收。
- 2026-05-13：阶段一验收审计通过。全部 6 条验收条件可测，代码契约验证通过。

---

## BL-RESULT-002 漏洞结果表改为追加留存

- 模块：结果管理
- 优先级：P1
- 状态：已完成
- 当前阶段：阶段三
- 价值：同一资产、同一漏洞的每次成功验证都保留历史记录，"当时存在过"的证据不会因为后续漏洞消失而丢失。
- 关联模块文档：docs/modules/04-结果管理模块.md §4.3, docs/modules/资产检索与目录扫描设计.md §7.3-7.4
- 关联计划：plans/2026-05-23-漏洞结果留存设计收口.md
- 关联代码：
  - `app_cybersparker/services/result_event_service.py:_write_auto_exp_event`
  - `app_cybersparker/views/expload/task_manage/auto_scan_result.py:build_related_vulns`
  - `app_cybersparker/views/expload/result__manage/all_auto_exp_result.py:list`
- 验收状态：已验收

### 范围
- `_write_auto_exp_event` 从 `get_or_create + update` 改为永远 `create` 新记录
- `build_related_vulns` 按 EXP_id 去重，只保留每个插件的最新一条（`-id`）
- 确认 `all_auto_exp_result.list` 已有 `order_by("-id")`（当前代码已包含），无需修改
- 测试更新：`test_auto_exp_events_do_not_duplicate_rows` 改为验证追加语义

### 不做
- 不修改 `_write_identify_event` 和 `_write_batch_event` 的写入语义
- 不给 `auto_scan_exp_result` 加唯一约束
- 不修改全局结果管理页的去重逻辑（全量列表不做去重）

### 验收条件
- [ ] 同一 task_id + target + EXP_id 的两次事件产生两条 DB 记录
- [ ] 资产检索页"受影响漏洞"卡片只显示每个插件的最新一条结果
- [ ] 全局联动漏洞结果列表按 `-id` 排序
- [ ] `vuln_result_text` 懒加载接口正确返回指定记录的结果内容
- [ ] `python manage.py test` 相关测试通过
- [ ] `python manage.py check` 0 issues

### 依赖
- `auto_scan_exp_result` 模型（无唯一约束，可有多条记录）
- `result_event_service.py`

### 状态记录
- 2026-05-23：新建条目，设计已在前一轮会话收口，实现待做。
- 2026-05-23：实现完成。`_write_auto_exp_event` 已改为 `create()`；`build_related_vulns` 已有 `order_by('-id')`；`all_auto_exp_result.list` 已有 `order_by('-id')`。

---

## BL-RESULT-003 AssetTaskRelation 模型与数据迁移

- 模块：结果管理
- 优先级：P0
- 状态：已完成
- 当前阶段：阶段五
- 价值：资产可以被多个任务关联，不再因为 task_id 被覆盖而查不到。
- 关联模块文档：docs/modules/04-结果管理模块.md
- 关联计划：plans/2026-05-27-资产表task_id改为多对多关系表.md
- 关联代码：`app_cybersparker/models.py`（auto_scan_indentify_result）
- 验收状态：已验收

### 范围
- 新建 `AssetTaskRelation` 模型（task_id + identify_result FK + created_at），含唯一约束 `(task_id, identify_result_id)`（唯一约束自带 B-tree 索引，2026-06-21 删除冗余的显式 `idx_task_asset`）
- 数据迁移：`INSERT INTO asset_task_relation (task_id, identify_result_id) SELECT task_id, id FROM auto_scan_indentify_result WHERE task_id IS NOT NULL`
- 删除 `auto_scan_indentify_result.task_id` 字段及其索引

### 不做
- 不修改 `auto_scan_exp_result` 的 task_id
- 不处理历史重复数据（task_id 为 NULL 的记录）

### 验收条件
- [ ] `python manage.py makemigrations` 生成迁移文件（含 CreateModel + RemoveField + 数据迁移 RunPython）
- [ ] `python manage.py migrate` 成功执行
- [ ] `AssetTaskRelation` 表存在，唯一约束和复合索引已创建
- [ ] `auto_scan_indentify_result` 表不再有 `task_id` 列
- [ ] 迁移后 `AssetTaskRelation` 行数 = 迁移前 `auto_scan_indentify_result` 中 task_id IS NOT NULL 的记录数

### 依赖
- `auto_scan_indentify_result` 模型（`app_cybersparker/models.py:283`）

### 状态记录
- 2026-05-27：新建条目，计划已通过独立审查。
- 2026-05-27：实现完成。新建 `AssetTaskRelation` 模型（含唯一约束和复合索引），迁移文件 0042 含数据迁移（复制现有 task_id 到关系表），删除 `auto_scan_indentify_result.task_id` 字段。系统检查 0 issues。

---

## BL-RESULT-004 结果写入路径改为关系表

- 模块：结果管理
- 优先级：P0
- 状态：已完成
- 当前阶段：阶段五
- 价值：高并发扫描写入时不会因为逐条 get_or_create 产生锁竞争，写入性能恒定。
- 关联模块文档：docs/modules/04-结果管理模块.md
- 关联计划：plans/2026-05-27-资产表task_id改为多对多关系表.md
- 关联代码：`app_cybersparker/services/result_event_service.py:_write_identify_event`
- 验收状态：已验收

### 范围
- `_write_identify_event` 中每次写入 identify 记录后，追加 `AssetTaskRelation(task_id=..., identify_result=row)` 到缓冲列表
- 新增 `_relation_buffer` 和 `_flush_relations()` 函数，缓冲满 500 条或批次结束时执行 `bulk_create(ignore_conflicts=True, batch_size=500)`
- 清理 `defaults` 字典中残留的 `task_id` 键

### 不做
- 不修改 `_write_auto_exp_event` 和 `_write_batch_event` 的写入逻辑
- 不去重：同一个任务扫同一资产多次，`ignore_conflicts=True` 自动跳过

### 验收条件
- [ ] `_write_identify_event` 成功写入后，`AssetTaskRelation` 表有对应记录
- [ ] 同一 `(task_id, identify_result_id)` 重复写入时不报错不重复
- [ ] `defaults` 字典中不再包含 `task_id` 键
- [ ] `python manage.py check` 0 issues

### 依赖
- BL-RESULT-003（AssetTaskRelation 模型已存在，auto_scan_indentify_result 已无 task_id 字段）

### 状态记录
- 2026-05-27：新建条目。
- 2026-05-27：实现完成。`_write_identify_event` 移除了 defaults 中的 task_id，新增 `_relation_buffer` + `_flush_relations()`，缓冲满 500 条或批次结束时 `bulk_create(ignore_conflicts=True)` 批量写入。系统检查 0 issues。

---

## BL-RESULT-005 查询路径改为关系表关联 + 索引 + 验证

- 模块：结果管理（跨模块影响：目录扫描、资产检索）
- 优先级：P0
- 状态：已完成
- 当前阶段：阶段五
- 价值：所有按 task_id 查资产的页面和导出功能正常工作，资产检索支持 task_id 搜索，漏洞结果关联正确。
- 关联模块文档：docs/modules/04-结果管理模块.md
- 关联计划：plans/2026-05-27-资产表task_id改为多对多关系表.md
- 关联代码：
  - `app_cybersparker/views/expload/task_manage/auto_scan_result.py`（standalone、chart_data、task_result_html、asset_export）
  - `app_cybersparker/tasks.py`（导出任务）
  - `app_cybersparker/views/expload/task_manage/dirscan_task_manage.py`（源任务展开）
  - `app_cybersparker/services/result_event_service.py`（build_related_exp_lookup/Exists）
  - `app_cybersparker/models.py`（auto_scan_exp_result 索引）
- 验收状态：已验收

### 范围

**a) 索引**
- `auto_scan_exp_result` 加 `(task_id, target)` 复合索引

**b) 6 处按 task_id 查资产的查询改为关系表关联**
- `auto_scan_result.py:536` standalone 页面 → EXISTS 子查询
- `auto_scan_result.py:683` chart_data → 反向 JOIN
- `auto_scan_result.py:740` task_result_html → 反向 JOIN
- `auto_scan_result.py:1030-1035` asset_export → EXISTS 子查询
- `tasks.py:397` 导出任务 → EXISTS 子查询
- `dirscan_task_manage.py:60` 目录扫描源任务展开 → 反向 JOIN

**c) 资产检索 task_id 搜索**
- `to_query_structure` 新增 `task_id` 字段处理：`Q(task_relations__task_id=value)`

**d) 漏洞结果关联三表 JOIN（中风险）**
- `build_related_exp_lookup` 和 `build_related_exp_exists` 改为三步关联（identify → relation → exp_result）

**e) 验证**
- `EXPLAIN` 检查三表 JOIN 走索引（无全表扫描）

### 不做
- 不修改 `auto_scan_exp_result` 的 task_id 字段
- 不修改批量任务和单任务的结果查询

### 验收条件
- [ ] standalone 页面按 task_id 能查到资产（两个不同任务扫同一资产后各自可见）
- [ ] 全局资产检索 `task_id:100` 能查到对应资产
- [ ] 目录扫描按源任务展开资产正常
- [ ] 漏洞结果关联（vuln/cve facet）在三表 JOIN 下结果正确
- [ ] 资产导出功能正常
- [ ] `EXPLAIN` 输出确认三表 JOIN 走 `idx_task_asset` 和 `idx_exp_task_target` 索引
- [ ] `python manage.py check` 0 issues

### 依赖
- BL-RESULT-003（AssetTaskRelation 模型 + 数据迁移完成）
- BL-RESULT-004（写入路径已切换到关系表）

### 状态记录
- 2026-05-27：新建条目。
- 2026-05-27：实现完成。6 处 ORM filter 改为 `task_relations__task_id=...`；3 处 SQL JOIN 改为三步关联（identify → relation → exp_result）；`all_Indentify_result.py` CSV 导出移除 task_id 列；测试用例同步更新。`auto_scan_exp_result` 的 (task_id, target) 索引已存在无需创建。系统检查 0 issues。
- 2026-05-28：补修导出链路遗漏。`run_export_task` 不再读取已删除的 `item.task_id`，改为通过 `AssetTaskRelation` 取当前资产归属任务，再按 `(task_id, target)` 关联漏洞结果；补了导出回归测试，避免多任务共享同一资产时导出串任务。
