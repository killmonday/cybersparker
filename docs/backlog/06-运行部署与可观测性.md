# 06-运行部署与可观测性

## BL-AUTH-001 认证中间件

- 模块：运行部署与可观测性
- 优先级：P1
- 状态：已完成
- 当前阶段：阶段一
- 价值：平台具有基本的访问控制，非认证用户无法访问业务页面。
- 关联模块文档：docs/modules/06-运行部署与可观测性模块.md
- 关联计划：
- 关联代码：`app_cybersparker/middleware/auth.py`, `app_cybersparker/middleware/basic.py`, `cybersparker/settings.py:64-65`
- 验收状态：已验收

### 范围
- AuthMiddleware：检查 session['info']，未登录返回 404
- BasicAuthMiddleware：检查 Basic Authorization 头，未通过返回 401
- 登录页面 `/login` 免认证

### 不做
- 角色/权限系统
- OAuth/SSO 集成
- 登录限流/账户锁定

### 验收条件
- [ ] 未登录访问 `/Dashboards` 返回 404
- [ ] 未登录访问 `/expload/list` 返回 404
- [ ] 登录后正常访问所有业务页面
- [ ] Basic Auth 头正确时可通过认证

### 依赖
- Django session 中间件
- Django auth 中间件

### 状态记录
- 2026-05-13：基于代码审查确认功能已实现，待验收。
- 2026-05-13：阶段一验收审计通过。全部验收条件可测，代码契约验证通过。

---

## BL-DEPLOY-001 Docker 部署与自动构建

- 模块：运行部署与可观测性
- 优先级：P2
- 状态：已完成
- 当前阶段：阶段一
- 价值：支持 Docker 容器化部署。
- 关联模块文档：docs/modules/06-运行部署与可观测性模块.md
- 关联计划：
- 关联代码：`Dockerfile`, `docker-compose.yml`, `autobuild.py`
- 验收状态：已验收

### 范围
- Dockerfile 构建镜像
- docker-compose 编排
- 自动构建脚本

### 不做
- 生产级 Docker 编排（多实例/负载均衡）
- CI/CD 流水线

### 验收条件
- [ ] `docker build` 成功构建镜像
- [ ] `docker-compose up` 启动服务可正常访问
- [ ] 数据持久化（`db/`、`EXP_plugin/` 等目录挂载）

### 依赖
- Docker Engine
- docker-compose

### 状态记录
- 2026-05-13：基于代码审查确认功能已实现，待验收。
- 2026-05-13：阶段一验收审计通过。全部验收条件可测，代码契约验证通过。

---

## BL-PG-001 数据库迁移至 PostgreSQL

- 模块：运行部署与可观测性
- 优先级：P1
- 状态：已完成
- 当前阶段：阶段一
- 价值：解决 SQLite 单 writer 并发瓶颈，支持多线程真正并发写入数据库。
- 关联模块文档：docs/modules/06-运行部署与可观测性模块.md
- 关联计划：plans/2026-05-15-pg-migration.md
- 关联代码：`cybersparker/settings.py`、`app_cybersparker/apps.py`
- 验收状态：已验收

### 范围
- `cybersparker/settings.py`：DATABASES 切换至 PostgreSQL (`localhost:5433/cybersparker`)，密码硬编码 + 环境变量覆盖
- `app_cybersparker/apps.py`：移除 SQLite WAL/同步 PRAGMA 配置
- 各 task 文件：删除无效 `import sqlite3` 死代码
- `requirements.txt`：新增 `psycopg2-binary`
- `docker-compose.yml`：移除 SQLite 文件挂载 `db/db_cybersparker.db`

### 不做
- 不迁移现有 SQLite 数据

### 验收条件
- [x] `python manage.py check` 0 issues
- [x] `python manage.py migrate` 全部迁移成功
- [x] `python manage.py test` 15/15 测试通过
- [x] PostgreSQL 21 张业务表创建成功

### 依赖
- PostgreSQL 服务器 localhost:5433
- `psycopg2-binary`

### 状态记录
- 2026-05-15：迁移完成，四层验证通过。

---

## BL-SCHED-001 全局调度基线观测与状态模型

- 模块：运行部署与可观测性
- 优先级：P0
- 状态：已完成
- 当前阶段：阶段二（调度改造主线）
- 价值：在不改变现有执行模型的前提下获得线程、连接、队列和耗时基线，为 Redis + Celery 改造提供可回滚依据。
- 关联模块文档：docs/modules/06-运行部署与可观测性模块.md
- 关联计划：plans/2026-05-16-bl-sched-001-runtime-baseline.md
- 关联代码：`cybersparker/settings.py`, `cybersparker/urls.py`, `app_cybersparker/views/Dashboards.py`, `app_cybersparker/services/scheduler_runtime_service.py`
- 验收状态：已验收

### 范围

- 新增统一资源配置项，默认只观测不强制限流。
- 建立运行时诊断入口，输出线程数、数据库连接池占用、任务队列长度和任务耗时。
- 梳理 `THREAD_DIC` / `BATH_TASK_DIC` / `KILL_AUTO_TASK_DIC` 的替代状态模型。
- 明确状态真源：DB 是业务终态真源，Redis 是运行态/协调真源，旧内存字典仅作为本机执行器句柄缓存。
- 规划 `auto_scan_tasks`、`batch_EXPTask` 的状态模型迁移字段：`queued`、`failed`、`dispatch_token`、`owner`、`stop_requested`、`heartbeat_at`、`last_error`。
- 规划 UI 兼容映射：新增“等待/失败”展示，不破坏现有 `finish/running/stop` 语义。
- 采样统计 `EXPTask.taskType=Attact` 与非空 `cmd_input` 使用量，仅产出原始统计，不做去留决策。

### 不做

- 不引入 Celery worker。
- 不改变任何任务启动、停止或执行路径。
- 不删除或迁移单任务模块。

### 验收条件

- [x] 启动任一批量任务或自动识别任务时，诊断入口返回 `thread_count`、`db_pool_checked_out`、`queue_lengths`、`elapsed_seconds` 字段。
- [x] 未运行任务时诊断入口返回空队列和当前进程线程数，不抛异常。
- [x] 统一资源配置项存在，默认值不改变现有任务行为。
- [x] 文档记录三类内存字典的替代状态模型和迁移边界，且明确旧内存字典不得反向覆盖 DB 终态。
- [x] 状态模型迁移方案列出 `queued`、`failed`、`dispatch_token`、`owner`、`stop_requested`、`heartbeat_at`、`last_error` 字段及 UI“等待/失败”映射。
- [x] 输出 `EXPTask.taskType` 分组数量与非空 `cmd_input` 数量，供 `BL-TASK-002` 复用。
- [x] `python manage.py check` 0 issues。

### 依赖

- 无。

### 状态记录

- 2026-05-16：基于 Redis + Celery 全局任务调度计划拆分，待用户确认优先级与范围。
- 2026-05-16：新增 `/runtime/diagnostics` 只读入口与 `app_cybersparker/services/scheduler_runtime_service.py`，输出线程数、连接池占用、队列长度、耗时、资源配置、句柄计数、单任务使用量与状态模型规划。
- 2026-05-16：确认 `dj_db_conn_pool` 的连接池状态应通过 `dj_db_conn_pool.core.pool_container` 读取，不能依赖 Django wrapper 暴露全局 pool 属性。
- 2026-05-16：自检通过 `python manage.py test app_cybersparker.tests.SchedulerRuntimeDiagnosticsTests` 与 `python manage.py check`。
- 2026-05-16：运行态采样结果：`EXPTask.taskType` 当前为 `Verify=6`、`Attact=0`，非空 `cmd_input=0`，可直接供 `BL-TASK-002` 复用。
- 2026-05-16：阶段二验收通过。7/7 验收条件有代码+测试证据支撑。

---

## BL-SCHED-002 Redis + Celery 基础设施

- 模块：运行部署与可观测性
- 优先级：P0
- 状态：已完成
- 当前阶段：阶段二（调度改造主线）
- 价值：引入可独立启动的任务执行基础设施，为 Web 请求线程与长生命周期扫描任务解耦做准备。
- 关联模块文档：docs/modules/06-运行部署与可观测性模块.md
- 关联计划：plans/2026-05-16-bl-sched-002-celery-infra.md
- 关联代码：`cybersparker/settings.py`, `cybersparker/celery.py`, `app_cybersparker/tasks.py`, `app_cybersparker/services/celery_runtime_service.py`, `app_cybersparker/services/task_state_cas_service.py`, `docker-compose.yml`
- 验收状态：已验收

### 范围

- 增加 Celery app 和 Redis broker 配置。
- 定义 `auto_scan`、`batch_scan`、`result_writer`、`maintenance` 队列。
- 补充 worker 启动命令和本地开发配置。
- 增加 Celery eager / fake Redis 的基础测试。
- 前置 DB 连接预算闸门：`Web池 + Celery池 + gevent子进程池 + 预留 <= 100`，默认按每进程 `5+2` 同量级收紧。
- 增加统一 CAS helper：只有当前 `dispatch_token/owner` 匹配且 DB 仍非终态时，才允许写入 `success/stopped/failed`；旧 token、重复消息、迟到 stop 一律 no-op。
- 增加 dispatch 硬闸门：预算超限时 worker 启动失败，并拒绝开启 Celery dispatch。

### 不做

- 不切换自动识别或批量任务的业务启动路径。
- 不启用 Redis result backend 作为业务状态真源。
- 不实现全局资源令牌。

### 验收条件

- [x] `celery -A cybersparker worker -Q auto_scan,batch_scan,result_writer,maintenance` 可启动并识别 4 个队列。
- [x] `celery -A cybersparker inspect active_queues` 可看到 `auto_scan`、`batch_scan`、`result_writer`、`maintenance`。
- [x] `CELERY_TASK_ALWAYS_EAGER=True` 时示例 task 在测试中同步执行并返回预期结果。
- [x] 连接预算公式超限时，worker 启动硬失败且 Celery dispatch 被拒绝开启，不允许仅告警。
- [x] 统一 CAS helper 覆盖旧 token、重复消息、迟到 stop，不会覆盖已有终态。
- [x] 未启动 Celery worker 时，Django Web 启动和现有页面导入不受影响。
- [x] `python manage.py check` 0 issues。

### 依赖

- `BL-SCHED-001`。

### 状态记录

- 2026-05-16：基于 Redis + Celery 全局任务调度计划拆分，待用户确认优先级与范围。
- 2026-05-16：阶段二验收通过。7/7 验收条件有代码+测试证据支撑。

---

## BL-SCHED-005 Redis 全局资源预算令牌

- 模块：运行部署与可观测性
- 优先级：P0
- 状态：已完成
- 当前阶段：阶段二（调度改造主线）
- 价值：用 Redis 原子令牌为线程、协程、HTTP in-flight 和 DB writer 建立全局预算，防止多任务并发时资源超卖。
- 关联模块文档：docs/modules/06-运行部署与可观测性模块.md
- 关联计划：plans/2026-05-16-redis-celery-global-scheduler-plan.md
- 关联代码：`app_cybersparker/services/resource_lease_service.py`
- 验收状态：已验收

### 范围

- 使用 Redis Lua 实现原子申请、释放和续租。
- 资源键覆盖 `http_inflight`、`threads`、`coroutines`、`db_writers`、`running_auto_scan`、`running_batch_scan`。
- 实现 lease TTL、owner heartbeat 和崩溃回收。
- 增强 owner/resource lease 与 heartbeat；stop bridge 的最小实现属于 `BL-AUTO-002` / `BL-BATCH-007`，本项不再作为停止能力的首个落点。
- 资源不足时记录可观测的 `waiting_resource` 运行时标记，并延迟重试，不把业务任务误标为完成。

### 不做

- 不改自动识别 HTTP 请求层为 async。
- 不改结果写入链路。
- 不新增 backlog 开发状态值。

### 验收条件

- [x] 并发 100 次申请 `limit=10` 的同一资源时，成功 lease 数不超过 10。
- [x] 释放 lease 后，后续申请可以成功复用额度。
- [x] lease 超过 TTL 且无 heartbeat 后可被回收。
- [x] heartbeat 能延长 lease，未过期 lease 不被误回收。
- [x] 资源不足时任务记录 `waiting_resource` 运行时标记，并进入延迟重试路径。
- [x] 单元测试覆盖超卖、释放、TTL 回收、heartbeat 续租和资源不足路径。

### 依赖

- `BL-SCHED-002`。
- `BL-AUTO-002`。
- `BL-BATCH-007`。

### 状态记录

- 2026-05-16：基于 Redis + Celery 全局任务调度计划拆分，待用户确认优先级与范围。
- 2026-05-16：阶段二验收通过。6/6 验收条件有 6 个专项测试支撑。

---

## BL-SCHED-006 结果事件缓冲与 DB Writer

- 模块：运行部署与可观测性
- 优先级：P0
- 状态：已完成
- 当前阶段：阶段二（调度改造主线）
- 价值：在数据库连接池耗尽或写入异常时，扫描结果先进入可重试缓冲，避免高压场景下丢失结果。
- 关联模块文档：docs/modules/06-运行部署与可观测性模块.md
- 关联计划：plans/2026-05-16-bl-sched-006-result-event-buffer.md
- 关联代码：`app_cybersparker/services/result_event_service.py`, `app_cybersparker/tasks.py`, `app_cybersparker/views/expload/task_manage/auto_exp_task.py`, `app_cybersparker/views/expload/task_manage/batch_task_executor.py`
- 验收状态：已验收

### 范围

- 定义识别结果、自动联动漏洞结果、批量漏洞结果的事件结构和幂等键。
- 为三类结果建立唯一键/幂等列并使用 upsert：识别结果 `task_id + target + product`；自动联动漏洞结果 `task_id + target + exp_id`（无 `exp_id` 时使用 `task_id + target + plugin_name`）；批量漏洞结果 `task_id + target + plugin_name`。
- 使用 Redis Streams + consumer group 作为结果事件缓冲。
- 增加 DB writer worker，批量 upsert 结果。
- DB 连接超时、`OperationalError` 或写入失败时不 ACK，等待重试。
- Redis/Stream 不可用时写入最小 append-only 本地 spool，Redis 恢复后回放。
- 增加 pending 消息巡检与 reclaim。

### 不做

- 不在本项实现 spool 轮转、归档和巡检增强；这些属于 `BL-SCHED-007`。
- 不改变业务结果表的查询页面展示。

### 验收条件

- [x] 识别结果事件使用 `task_id + target + product` 唯一键，重复消费后结果表不重复。
- [x] 自动联动漏洞结果使用 `task_id + target + exp_id` 唯一键；无 `exp_id` 时使用 `task_id + target + plugin_name` 兜底，重复消费后结果表不重复。
- [x] 批量漏洞结果使用 `task_id + target + plugin_name` 唯一键，重复消费后结果表不重复。
- [x] 模拟 DB pool 耗尽时，Stream 中未 ACK 消息数量增加，结果事件不丢失。
- [x] 模拟 Redis/Stream 不可用时，结果事件写入最小 append-only spool；Redis 恢复后可回放入 Stream。
- [x] DB 恢复后，pending 消息可被 reclaim 并成功写入 PostgreSQL。
- [x] DB writer 并发数受 `db_writers` 资源预算限制。
- [x] 集成测试通过 Stream 条数、spool 条数、pending 数和结果表行数证明“不丢不重”。

### 依赖

- `BL-SCHED-005`。

### 状态记录

- 2026-05-16：基于 Redis + Celery 全局任务调度计划拆分，待用户确认优先级与范围。
- 2026-05-16：阶段二验收通过。8/8 验收条件有代码+测试证据支撑。
- 2026-05-18：修复 result writer backlog 排空不足：`run_result_writer_task` 改为单次任务内循环 drain，直到本轮 `processed=0` 才退出，避免自动扫描任务已产出 Redis 结果事件但数据库长时间为空。新增 backlog drain 回归测试，结果链路 10/10 通过。
- 2026-05-18：修复结果写库对 NUL 字符不兼容：识别/漏洞/批量结果入库前统一清理 `\x00`，避免单条脏结果触发 PostgreSQL 文本写库异常，导致 writer 失败和自动扫描卡在 99%。验证：结果写库回归 5/5 通过，Django check 0 issues。
- 2026-05-18：为 PostgreSQL 业务库全部 public 表安装 `BEFORE TRUNCATE` 保护触发器，统一禁止整表 TRUNCATE，保留 DELETE 可用，防止外部连接再次误清空业务数据。验证：23/23 表已受保护，手工 TRUNCATE 被拦截，DELETE 仍可用。
- 2026-05-18：补强 Celery PostgreSQL 连接失步防御：worker 子进程启动时重置旧连接并销毁继承连接池；auto/batch 的 DB fallback 查询与 `heartbeat_at` 写回在连接损坏时降级处理，降低 prefork + 线程场景下坏连接炸线程的概率。
- 2026-05-17：修复 result writer 后续运行时问题：`run_result_writer_task` 入口补 `close_old_connections()`，`process_result_stream()` 将 `DatabaseError` 纳入可重试数据库故障分支；写库失败事件保留 pending，后续 writer 恢复后可继续处理。补充 9 条结果链路回归测试全部通过。

---

## BL-SCHED-007 结果缓冲本地 Spool 治理增强

- 模块：运行部署与可观测性
- 优先级：P1
- 状态：已完成
- 当前阶段：阶段二（调度改造主线）
- 价值：在 `BL-SCHED-006` 的最小 append-only spool 基础上，补齐轮转、归档、巡检和回放治理，降低长期运行风险。
- 关联模块文档：docs/modules/06-运行部署与可观测性模块.md
- 关联计划：plans/2026-05-16-bl-sched-007-spool-governance.md
- 关联代码：`app_cybersparker/services/result_event_service.py`
- 验收状态：已验收

### 范围

- 在 `BL-SCHED-006` 已具备最小 append-only spool 的基础上，补齐文件轮转、归档、容量告警和巡检。
- 增强 spool 回放策略：支持断点、重复回放检测和失败重试统计。
- 明确 spool 文件清理策略，防止长期运行无限增长。

### 不做

- 不把本地 spool 作为常规结果队列。
- 不支持跨机器共享 spool 文件。
- 不重新定义 `BL-SCHED-006` 已落地的最小 spool 写入格式。

### 验收条件

- [x] spool 文件达到轮转阈值后生成新文件，旧文件进入归档或待回放队列。
- [x] 回放过程支持断点恢复，重复回放不会产生重复结果。
- [x] 巡检任务能统计待回放条数、失败次数和最大文件大小。
- [x] spool 文件路径可配置，默认目录不存在时安全创建，不包含用户可控路径遍历。

### 依赖

- `BL-SCHED-006`。

### 状态记录

- 2026-05-16：基于 Redis + Celery 全局任务调度计划拆分，待用户确认优先级与范围。
- 2026-05-16：阶段二验收通过。4/4 验收条件有代码+测试证据支撑。

---

## BL-AUDIT-FIX-001 审计问题修复

- 模块：运行部署与可观测性
- 优先级：P1
- 状态：已完成
- 当前阶段：阶段二（调度改造主线）
- 价值：修复审计发现的测试不稳定、历史测绘输入源保护、日志输出和文档口径问题，让回归结果与运行状态更可信。
- 关联模块文档：docs/modules/06-运行部署与可观测性模块.md
- 关联计划：plans/2026-06-07-审计问题修复.md
- 关联代码：`app_cybersparker/test_runner.py`, `app_cybersparker/tests.py`, `app_cybersparker/views/expload/task_manage/auto_scan_task.py`, `app_cybersparker/views/expload/task_manage/batch_exp_task.py`, `app_cybersparker/views/expload/task_manage/auto_scan_result.py`, `frontend/src/components/HistoryEngineResultPicker.tsx`, `frontend/src/pages/AutoScanTaskListPage.tsx`, `frontend/src/pages/BatchTaskListPage.tsx`
- 验收状态：已验收

### 范围

- 修复全量后端测试中的数据库连接状态串线和任务句柄全局字典残留。
- 给历史测绘结果输入源补后端 API 契约测试和前端 TypeScript 编译级测试。
- 收紧历史测绘文件读取范围，只允许读取测绘结果目录下的文件。
- 优化历史测绘结果列表的非空行统计方式。
- 将结果检索接口的直接 `print` 计时输出改成默认关闭的 debug 日志。
- 同步测试数量、旧模板回退能力、风险项等文档口径。

### 不做

- 不新增前端测试依赖。
- 不立即优化全局资产检索 facet 聚合查询，只记录项目风险项。
- 不改变历史测绘结果 API 的返回字段结构。

### 验收条件

- [x] `python manage.py test app_cybersparker.tests --verbosity 1` 全量测试通过。
- [x] `/api/v1/identify-tasks/history-engine-results` 返回 `status=true` 且 `data.results` 包含前端需要字段。
- [x] 历史测绘输入源拒绝读取测绘结果目录外的项目内文件。
- [x] 前端 TypeScript 编译能保护历史测绘结果选择器和提交字段。
- [x] 正常请求不再向 stdout 输出 `[TIMING ...]` 计时日志。
- [x] 文档中的后端测试数量、旧模板回退能力和项目风险项口径一致。

### 依赖

- plans/2026-06-07-功能性能稳定性审计待处理清单.md

### 状态记录

- 2026-06-07：用户确认 AUDIT-001~AUDIT-008 全部修复，RISK-001 做轻量性能优化，RISK-002 记录到项目风险项文档。
- 2026-06-07：已完成代码和文档修改。验证通过：`python manage.py check` 0 issues；`python manage.py test app_cybersparker.tests --verbosity 1` 223/223 通过；`npx --prefix frontend tsc --noEmit -p frontend/tsconfig.json` 通过。

---

## BL-DEPLOY-002 种子数据导出头部污染修复

- 模块：运行部署与可观测性
- 优先级：P0
- 状态：已完成
- 当前阶段：部署维护
- 价值：新环境首次启动时，种子数据可以自动导入，不会因为 `seed_data.sql` 头部混入 shell 命令而在 `date` 处报 SQL 语法错误。
- 关联模块文档：docs/modules/06-运行部署与可观测性模块.md
- 关联计划：plans/2026-06-23-seed-data-sql头部污染修复.md
- 关联代码：`deploy/seed/export_seed_data.sh`, `deploy/seed/seed_data.sql`, `deploy/docker-entrypoint.sh`, `app_cybersparker/tests.py`
- 验收状态：待验收

### 范围

- 修复 `deploy/seed/export_seed_data.sh` 的文件头生成逻辑，保证导出的 `seed_data.sql` 头部只包含 SQL 注释。
- 修正仓库中现有 `deploy/seed/seed_data.sql` 被污染的头部内容。
- 补一条回归测试，防止后续再次把 shell 命令文本写进 SQL 文件。
- 同步更新种子数据说明和部署模块文档。

### 不做

- 不重构 `docker-entrypoint.sh` 的导入流程。
- 不改种子数据包含的 9 张表范围。
- 不重新设计生产初始化策略。

### 验收条件

- [x] 执行导出脚本生成的 `seed_data.sql` 头部不包含 `date '+%Y-%m-%d %H:%M:%S' >> "${OUTPUT}"` 等 shell 命令文本。
- [x] `deploy/seed/seed_data.sql` 当前仓库版本头部已清理，不会再在 `date` 处触发 SQL 语法错误。
- [x] 回归测试能稳定覆盖这次污染场景。
- [x] 说明文档明确导出脚本和种子文件的职责边界。

### 依赖

- `deploy/docker-entrypoint.sh` 继续负责首次部署自动导入。

### 状态记录

- 2026-06-23：生产环境 fresh deploy 报 `syntax error at or near "date"`。排查确认不是 entrypoint 判断错，而是 `deploy/seed/seed_data.sql` 文件头混入了 shell 命令文本，准备按 TDD 修复。
- 2026-06-23：已完成修复。`export_seed_data.sh` 改为先算 `GENERATED_AT` 再输出 SQL 注释头；仓库内 `seed_data.sql` 现有污染头部已清理；新增 `SeedExportScriptTests` 2 条回归测试，验证“导出结果头部只允许 SQL 注释”和“仓库内 seed_data.sql 头部无 shell 命令文本”。

---

## 后续 Backlog 候选

以下为识别到的待规划事项，尚未拆分正式 backlog item：

| 候选 ID | 描述 | 优先级建议 |
|---|---|---|
| BL-CANDIDATE-01 | 前端自动化测试补强（后端已有全量测试，前端仍缺少独立测试脚本） | P1 |
| BL-CANDIDATE-02 | 安全配置加固（SECRET_KEY 外部化、DEBUG 关闭、ALLOWED_HOSTS 收敛） | P0 |
| BL-CANDIDATE-03 | CSRF 保护集成（前端 CSRF token） | P1 |
| BL-CANDIDATE-04 | 结果搜索/过滤功能 | P2 |
| BL-CANDIDATE-05 | 插件导入/导出 | P2 |
| BL-CANDIDATE-06 | 任务定时调度 | P2 |
| BL-CANDIDATE-08 | 健康检查接口 | P1 |
| BL-CANDIDATE-09 | 请求日志/链路追踪 | P2 |
| BL-CANDIDATE-10 | 清理遗留文件（`batch_exp_task copy.py`, `1.py`） | P1 |
