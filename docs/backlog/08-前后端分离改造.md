# 08-前后端分离改造

## BL-FE-001 鉴权、CSRF 与 API 契约冻结

- 模块：前后端分离
- 优先级：P0
- 状态：已完成
- 当前阶段：阶段七
- 价值：先把登录态、鉴权、CSRF、分页和错误码口径定死，避免 React 页面接一个坏一个。
- 关联模块文档：docs/modules/08-前后端分离模块.md
- 关联计划：plans/2026-05-30-前后端分离改造方案A.md
- 关联代码：`app_cybersparker/views/login.py`, `app_cybersparker/middleware/auth.py`, `cybersparker/settings.py`, `cybersparker/urls.py`
- 验收状态：已验收

### 范围
- 梳理现有登录态口径（`session['info']`）
- 明确 `/api/v1` 的登录校验、未登录返回、分页、筛选、错误码规范
- 明确哪些 POST 接口要求 CSRF，哪些历史接口暂保留豁免
- 输出首批 React 页面可直接使用的 API 契约文档

### 不做
- 不重写权限系统
- 不改 Celery/Redis/任务执行逻辑
- 不把所有旧接口一次性改成 RESTful

### 验收条件
- [x] 形成一份可执行的 `/api/v1` 契约清单，覆盖登录态、分页、错误码、筛选、CSRF 规则
- [x] 至少确认 1 个受保护 GET 接口和 1 个受保护 POST 接口的未登录/已登录行为口径
- [x] 明确 React 页面不能直接依赖的旧接口清单
- [x] `python manage.py check` 0 issues

### 依赖
- 现有登录页与中间件实现

### 状态记录
- 2026-05-30：Mode H 共识规划通过，确定这是前后端分离的第一个阻塞项。
- 2026-05-30：已完成登录鉴权、CSRF 和首批接口现状三路梳理，首版 `/api/v1` 契约形成：React 第一阶段先只接 GET，写操作不直连历史 `@csrf_exempt` 接口；每个 React 页面需先有 Django 壳页路由。
- 2026-05-30：已完成最小后端收口：新增 `/api/v1/auth/session`，`/api/v1/**` 未登录统一 `401 JSON`，补注册 `/logout`，并让已存在 session 的请求不再被 BasicAuth 重复挑战。定向测试与 `django check` 通过。

---

## BL-FE-002 React 基线接入、Django 壳页挂载与静态资源出口收口

- 模块：前后端分离
- 优先级：P0
- 状态：已完成
- 当前阶段：阶段七
- 价值：先把前端工程、构建产物、母版挂载点和回退路径搭起来，后续每一页都能在同一基线上迁移。
- 关联模块文档：docs/modules/08-前后端分离模块.md
- 关联计划：plans/2026-05-30-BL-FE-002-React基线接入与壳页挂载.md
- 关联代码：`frontend/`, `app_cybersparker/views/react_shell.py`, `app_cybersparker/templates/project/react_shell.html`, `cybersparker/urls.py`
- 验收状态：已验收

### 范围
- 新建 React + Vite + TypeScript 前端工程
- 在 Django 母版内容区预留 React 挂载点
- 定义构建产物的唯一静态资源出口
- 保留旧页面回退路径
- 验证刷新、直达入口、未登录跳转行为

### 不做
- 不接管整站导航
- 不删除旧模板静态资源
- 不实现高风险业务页

### 验收条件
- [x] 前端工程可独立开发与构建
- [x] Django 页面能稳定加载 React 内容区挂载点
- [x] 刷新和直达入口页时不会因为登录态或路由问题直接白屏
- [x] 新旧页面可并存，出现问题可按页回退
- [x] `python manage.py check` 0 issues

### 依赖
- BL-FE-001

### 状态记录
- 2026-05-30：Mode H 共识规划通过，要求母版导航先由 Django 保留控制。
- 2026-05-30：已完成最小 React 基线接入：新增 `frontend/` React + Vite + TypeScript 工程，构建产物输出到 `app_cybersparker/static/react-shell/`；新增 Django 壳页视图 `app_cybersparker/views/react_shell.py`、模板 `app_cybersparker/templates/project/react_shell.html` 和路由 `/react-shell`；空壳页已接入 `GET /api/v1/auth/session`。
- 2026-05-30：验证通过：`npm --prefix frontend run build`、`python manage.py test app_cybersparker.tests.AuthContractTests app_cybersparker.tests.ReactShellIntegrationTests --keepdb`、`python manage.py check`、`pyright app_cybersparker/tests.py cybersparker/urls.py`。

---

## BL-FE-003 首批低风险列表页迁移

- 模块：前后端分离
- 优先级：P1
- 状态：已完成
- 当前阶段：阶段七
- 价值：用低风险页面跑通 React 列表、筛选、分页和详情跳转的完整迁移套路。
- 关联模块文档：docs/modules/08-前后端分离模块.md
- 关联计划：plans/2026-05-30-BL-FE-003-插件列表React样板.md
- 关联代码：`frontend/`, `app_cybersparker/views/react_shell.py`, `app_cybersparker/views/expload/plugin_manage.py`, `dict_manage.py`, `proxy_setting.py`, `cyberspace_engine_setting.py`, `fingerprint.py`, `cybersparker/urls.py`
- 验收状态：已验收

### 范围
- 首批迁移以下页面的列表区：插件列表、字典列表、指纹列表、代理设置列表、测绘引擎设置列表
- 先完成列表、筛选、分页、详情跳转和空态展示
- 首批可以保留“去旧页编辑”的过渡入口

### 不做
- 不在这一项里迁任务页
- 不在这一项里迁资产检索页和 standalone 结果页
- 不强求首批就把所有新增/编辑/删除动作搬到 React

### 验收条件
- [x] 上述页面都能从 React 列表稳定展示数据
- [x] 搜索、筛选、分页行为与旧页一致
- [x] 每一页都保留旧页回退入口
- [x] 页面布局与当前后台主框架保持一致，不大改菜单和主内容结构
- [x] `python manage.py check` 0 issues

### 依赖
- BL-FE-001
- BL-FE-002

### 状态记录
- 2026-05-30：共识结论明确：首批不碰任务页和复杂结果页，只先打低风险列表页。
- 2026-05-30：已完成插件列表 React 样板：新增 `GET /api/v1/plugins`、`/react-shell/plugins`，React 页面已支持列表、筛选、分页和回旧页入口；旧 `expload/list` 页面行为未改。后续其余低风险列表页按同样模式推进。
- 2026-05-30：已完成字典列表 React 样板：新增 `GET /api/v1/dicts`、`/react-shell/dicts`，React 页面已支持列表、搜索、分页和回旧页入口；旧 `dict/list` 页面行为未改。
- 2026-05-30：已完成代理列表 React 样板：新增 `GET /api/v1/proxies`、`/react-shell/proxies`，React 页面已支持列表、搜索、分页和回旧页入口；旧 `proxy_setting` 页面行为未改。
- 2026-05-30：已完成测绘引擎列表 React 样板：新增 `GET /api/v1/cyberspace-engines`、`/react-shell/engines`，React 页面已支持列表、搜索、分页和回旧页入口；旧 `cyberspace_engine_setting` 页面行为未改。
- 2026-05-30：已完成指纹列表 React 样板：新增 `GET /api/v1/fingerprints`、`/react-shell/fingerprints`，React 页面已支持列表、搜索、分页和回旧页入口；旧 `fingerprint_List` 页面行为未改。

---

## BL-FE-004 结果页接口抽离但不换 UI

- 模块：前后端分离
- 优先级：P1
- 状态：已完成
- 当前阶段：阶段七
- 价值：先把结果页和资产检索背后的查询契约抽出来，后面换 React UI 时不需要一边拆接口一边改交互。
- 关联模块文档：docs/modules/08-前后端分离模块.md
- 关联计划：plans/2026-05-30-BL-FE-004-结果页接口抽离.md
- 关联代码：`app_cybersparker/views/expload/task_manage/auto_scan_result.py`, `app_cybersparker/views/expload/result__manage/expResult.py`, `app_cybersparker/views/expload/result__manage/all_auto_exp_result.py`
- 验收状态：已验收

### 范围
- 抽离结果列表、任务内结果、资产检索、facet、分页相关接口契约
- 让旧模板继续使用原页面，不强行切 React UI
- 收口结果页字段、筛选参数、分页参数、错误返回

### 不做
- 不在这一项里重写 standalone 结果页 UI
- 不在这一项里重写资产检索页 UI
- 不改变现有搜索语义

### 验收条件
- [x] 结果页和资产检索相关 JSON 契约有明确文档或接口层封装
- [x] 旧模板继续可用，现有结果页不因接口抽离而退化
- [x] 至少覆盖结果列表、facet、分页 3 类接口口径
- [x] `python manage.py check` 0 issues

### 依赖
- BL-FE-001

### 状态记录
- 2026-05-30：Architect/Critic 明确要求：结果页必须先抽接口，不得直接进入 React UI 替换。
- 2026-05-30：已完成第一版接口边界梳理，先按 5 类入口收口：任务内结果 JSON、全局资产检索 JSON、facet JSON、漏洞结果 JSON、导出/源码查看接口；结果页搜索参数暂继续沿用 `search_data`，facet 暂继续沿用 `field + search_data + offset`。
- 2026-05-30：已完成 facet 最小收口：`facet()` 与 `global_facet()` 统一复用字段白名单、参数校验和错误返回逻辑；新增 task 内与全局 invalid field 的 400 错误测试，旧搜索语义和旧模板 UI 未改。
- 2026-05-30：已新增结果页 JSON 包装入口：`GET /api/v1/identify-tasks/<uid>/results` 与 `GET /api/v1/assets/search`，先复用旧 JSON 分支，不改现有 standalone 页、全局检索页和搜索语义。
- 2026-05-30：已新增 facet JSON 包装入口：`GET /api/v1/identify-tasks/<uid>/facets` 与 `GET /api/v1/assets/facets`，统一 task 内与全局 facet 的 API 出口与错误口径。
- 2026-05-30：已完成结果列表字段契约正式化：`Task_result(..., format=json)` 与 `global_asset_search(..., format=json)` 统一复用同一序列化 helper，在保留旧顶层 `results` / `estimated_total` / `favicon_*` 字段的同时，新增 `contract_version`、`contract`、`query`、`pagination`、`favicon_facet` 这组显式契约元信息；facet/detail 依赖入口统一由 Django 路由反解生成，避免后续 URL 调整后契约静默漂移。补充 task/global 两条新契约测试与全局旧 HTML 页冒烟测试，本项达到完成线。

---

## BL-FE-005 中风险表单页迁移

- 模块：前后端分离
- 优先级：P1
- 状态：已完成
- 当前阶段：阶段七
- 价值：在低风险列表页稳定后，再迁真正有新增/编辑/删除闭环的页面，逐步减少对旧模板表单的依赖。
- 关联模块文档：docs/modules/08-前后端分离模块.md
- 关联计划：plans/2026-05-30-BL-FE-005-代理设置React表单闭环.md
- 关联代码：`frontend/`, `app_cybersparker/views/react_shell.py`, `app_cybersparker/views/expload/proxy_setting.py`, `cyberspace_engine_setting.py`, `fingerprint.py`, `cybersparker/urls.py`
- 验收状态：已验收

### 范围
- 当前阶段先迁 3 个中风险表单页：代理设置、测绘引擎、指纹
- 覆盖详情回填、新增、编辑、校验、错误提示
- 字典表单、插件编辑（不含调试执行）继续留在后续批次

### 不做
- 不迁插件调试页
- 不迁任务启动、暂停、续跑等长生命周期控制页
- 不重做整站权限系统

### 验收条件
- [x] 至少 3 个中风险表单页完成 React 可写闭环
- [x] 详情回填、新增、编辑、校验和错误提示行为与旧页一致
- [x] 保留旧页回退路径
- [x] `python manage.py check` 0 issues

### 依赖
- BL-FE-002
- BL-FE-003

### 状态记录
- 2026-05-30：共识结论明确：表单页应晚于低风险列表页，插件调试能力继续延后。
- 2026-05-30：已完成代理设置 React 可写闭环：新增 `GET /api/v1/proxies/<id>`、`POST /api/v1/proxies/create`、`POST /api/v1/proxies/<id>/update`，React 代理页已支持新增、编辑、错误提示和提交后刷新列表，旧 `proxy_setting` 页面行为未改。
- 2026-05-30：已完成测绘引擎设置 React 可写闭环：新增 `GET /api/v1/cyberspace-engines/<id>`、`POST /api/v1/cyberspace-engines/create`、`POST /api/v1/cyberspace-engines/<id>/update`，React 引擎页已支持新增、编辑、错误提示和提交后刷新列表，旧 `cyberspace_engine_setting` 页面行为未改。
- 2026-05-30：已完成指纹 React 可写闭环：新增 `GET /api/v1/fingerprints/<id>`、`POST /api/v1/fingerprints/create`、`POST /api/v1/fingerprints/<id>/update`，React 指纹页已支持新增、编辑、错误提示和提交后刷新列表，旧 `fingerprint_List` 页面行为未改。

---

## BL-FE-006 高风险页面二期评估与拆分

- 模块：前后端分离
- 优先级：P1
- 状态：已完成
- 当前阶段：阶段七
- 价值：先把最难的任务页、调试页、资产检索页拆清楚，避免过早进入大块实现后返工。
- 关联模块文档：docs/modules/08-前后端分离模块.md
- 关联计划：plans/2026-05-30-BL-FE-006-高风险页面二期评估与拆分.md
- 关联代码：`app_cybersparker/templates/project/expload/task_manage/`, `exp_debug.html`, `fingerprint_debug.html`, `auto_scan_result.py`
- 验收状态：已验收

### 范围
- 评估自动扫描任务页、批量任务页、目录扫描页、插件调试页、指纹调试页、资产检索页、standalone 结果页的迁移难点
- 建立接口依赖图、页面状态图、回退策略和二期拆分子项
- 为这些高风险页面再做一次实现前拆分

### 不做
- 不在这一项里直接完成高风险页面迁移
- 不改任务调度和扫描执行语义
- 不改结果搜索语义

### 验收条件
- [x] 形成一份高风险页面迁移依赖清单
- [x] 为每类页面给出子项拆分和建议顺序
- [x] 明确哪些页面进入下一期，哪些继续保留旧模板
- [x] `python manage.py check` 0 issues

### 依赖
- BL-FE-001
- BL-FE-004

### 状态记录
- 2026-05-30：Critic 要求高风险页面不得直接并入首批迁移，必须先做二期评估与拆分。
- 2026-05-30：开始执行，先围绕任务页、调试页、standalone 结果页、全局资产检索页做依赖盘点与状态图拆分，不直接进入页面实现。
- 2026-05-30：已完成高风险页面二期评估：7 类页面的路由/视图/模板/旧接口依赖已盘点完成，并按“结果展示线 → 任务控制线 → 调试工作台线”给出拆分顺序。建议下一期先做 standalone 结果页、全局资产检索页和自动扫描任务页，批量任务页、目录扫描页、插件调试页、指纹调试页继续保留旧模板分批进入。

---

## BL-FE-101 standalone 结果页 React 化（仅任务内结果主链路）

- 模块：前后端分离
- 优先级：P0
- 状态：已完成
- 当前阶段：阶段八
- 价值：先把单任务结果页主内容区切到 React，验证现有 `/api/v1/identify-tasks/<uid>/results` 与 `/api/v1/identify-tasks/<uid>/facets` 契约能支撑真实高风险页面。
- 关联模块文档：docs/modules/08-前后端分离模块.md
- 关联计划：plans/2026-05-30-BL-FE-101-standalone结果页React化.md
- 关联代码：`app_cybersparker/views/expload/task_manage/auto_scan_result.py`, `app_cybersparker/templates/project/expload/task_manage/auto_scan_identify_result_standalone.html`, `frontend/`
- 验收状态：已验收

### 范围
- 将 standalone 结果页主内容区切到 React
- 直接消费 `/api/v1/identify-tasks/<uid>/results` 与 `/api/v1/identify-tasks/<uid>/facets`
- 覆盖任务内搜索、分页、facet、结果列表和详情展开主链路

### 不做
- 不改搜索语义
- 不改旧 `/Identify_task/<uid>/result` 后台页
- 不改任务控制
- 不新增写操作

### 验收条件
- [x] 同一 `task_id`、同一查询条件下，新旧 standalone 页结果总数一致
- [x] 同一查询条件下，新旧页首屏结果列表顺序一致
- [x] facet 前几项统计与旧页一致
- [x] 翻页、每页条数切换、展开详情后页面状态不丢（当前主链路覆盖上一页/下一页/每页条数；附属能力留在 FE-103）
- [x] `python manage.py check` 0 issues

### 依赖
- BL-FE-004
- BL-FE-006

### 状态记录
- 2026-05-30：阶段八规划创建，确定该项为下一期第一优先级。
- 2026-05-30：已开始执行方案 A：新增 React 壳页入口 `/react-shell/identify-tasks/<uid>/results`，不直接替换旧 `/Identify_task/<uid>/result/standalone` 模板入口。当前已接通最小主链路：任务内搜索、facet（可点击）、分页（上一页/下一页/每页条数）、结果列表和详情展开第一版，并通过前端构建、TypeScript 检查、React 壳页测试、结果页契约测试和 `python manage.py check`。
- 2026-05-30：自动扫描任务列表里的“结果（独立窗口）”入口已切到新的 React 结果页壳路径；HTML 原文查看、漏洞结果查看、端口概览懒加载这 3 类附属能力继续留在 FE-103，不算作 BL-FE-101 完成条件。
- 2026-05-30：已补首轮更贴近验收的对照证据：`/api/v1/identify-tasks/<uid>/results` 与旧 standalone JSON 分支在同 task/query 下结果总数、首屏列表、facet 统计保持一致；`rows_per_page` 走现有白名单口径（5/10/13/100），与旧页一致。
- 2026-05-30：已完成第一版收口：自动扫描任务列表已有真实入口切到 React 结果页壳路径；当前版本保持在 BL-FE-101 范围内，不替换旧 standalone 模板入口，不纳入 HTML 原文查看、漏洞结果查看、端口概览懒加载，这 3 项继续留在 FE-103。

---

## BL-FE-102 全局资产检索页 React 化（仅跨任务检索主链路）

- 模块：前后端分离
- 优先级：P0
- 状态：已完成
- 当前阶段：阶段八
- 价值：在 FE-101 的共享组件基础上，把 `/asset/search` 的主内容区切到 React，最大化复用结果展示组件。
- 关联模块文档：docs/modules/08-前后端分离模块.md
- 关联计划：plans/2026-05-30-阶段八-结果展示线React化规划.md
- 关联代码：`app_cybersparker/views/expload/task_manage/auto_scan_result.py`, `app_cybersparker/templates/project/expload/task_manage/auto_scan_identify_result_standalone.html`, `frontend/`, `app_cybersparker/views/react_shell.py`, `cybersparker/urls.py`
- 验收状态：已验收

### 范围
- 将 `/asset/search` 主内容区切到 React
- 直接消费 `/api/v1/assets/search` 与 `/api/v1/assets/facets`
- 覆盖跨任务搜索、分页、facet、结果列表主链路

### 不做
- 不改导出后端
- 不改搜索 DSL
- 不扩到任务页

### 验收条件
- [ ] 同一查询条件下，新旧全局检索页结果总数一致
- [ ] 同一查询条件下，新旧页首屏列表顺序一致
- [ ] facet 统计口径与旧页一致
- [ ] 左侧导航入口与旧后台结构兼容
- [ ] `python manage.py check` 0 issues

### 依赖
- BL-FE-101

### 状态记录
- 2026-05-30：阶段八规划创建，确定该项为 standalone 结果页后的第二优先级。
- 2026-05-30：已完成最小实现：后端新增 `react_global_asset_search` 视图和 `/react-shell/assets/search` 路由；前端新增 `GlobalAssetSearchPage` 组件，复用 BL-FE-101 结果卡片/facet/分页/详情展开骨架；侧边栏入口已切到新 React 路径；旧 `/asset/search` 保留为回退路径。验证通过（build / tsc / 32 tests / check）。
- 2026-05-31：完全重写 `GlobalAssetSearchPage`，逐一对齐原版 Django 模板全部交互与视觉。补全：16 个可折叠 facet、favicon 快捷筛选条、Grid 结果行（IP 操作/tooltip/chip 视觉）、4 列展开详情（国旗+端口概览懒加载+产品 chip 点击+同 IP 展开+漏洞卡片+CVE chip+响应头 pre）、展开/收起全部、导出 CSV 弹窗（19 字段+CSRF+漏洞联动）、完整分页（页码+跳页+±10 页+1000 条选项）、Top bar 匹配计数。样式复用原版暖色调。验证通过（tsc zero error / vite build / check）。

---

## BL-FE-103 结果详情附属能力收口（HTML / 漏洞结果 / 端口概览）

- 模块：前后端分离
- 优先级：P1
- 状态：已完成
- 当前阶段：阶段八
- 价值：把结果展示线从“能看主列表”补到“能正常看详情”，避免出现主链路可用、细节能力残缺的半成品。
- 关联模块文档：docs/modules/08-前后端分离模块.md
- 关联计划：plans/2026-05-30-阶段八-结果展示线React化规划.md
- 关联代码：`app_cybersparker/views/expload/task_manage/auto_scan_result.py`, `app_cybersparker/templates/project/expload/task_manage/auto_scan_identify_result_standalone.html`, `frontend/`
- 验收状态：已验收

### 范围
- HTML 原文查看
- 漏洞结果查看
- 端口概览”查看更多”懒加载
- 结果详情附属交互收口

### 不做
- 不改主列表和主筛选
- 不改搜索语义
- 不改任务控制
- 不纳入导出链路

### 验收条件
- [x] HTML 原文查看在新旧页行为一致
- [x] 漏洞结果查看在新旧页行为一致
- [x] 端口概览懒加载行为与旧页一致
- [x] 详情区展开后不会打乱主列表状态
- [x] `python manage.py check` 0 issues

### 依赖
- BL-FE-101
- BL-FE-102

### 状态记录
- 2026-05-30：阶段八规划创建，明确本项只收详情附属能力，不夹带主列表与导出改造。
- 2026-05-30：已完成 3 类附属能力收口：HTML 原文查看（新标签页打开 `/identify_result/<id>/html`）、漏洞结果查看（弹窗 fetch `/identify_result/<vulnId>/vuln-result`）、端口概览懒加载（展开详情后 fetch `/asset/port_overview_more` 分页加载）。两个结果页（TaskResultsPage / GlobalAssetSearchPage）均已补齐。验证通过（build / tsc / 32 tests / check）。

---

## BL-FE-104 自动扫描结果页 React 化

- 模块：前后端分离
- 优先级：P1
- 状态：已完成
- 当前阶段：阶段十三
- 价值：自动扫描结果是高频结果管理页，当前侧边栏仍指向旧页面。
- 关联模块文档：docs/modules/08-前后端分离模块.md
- 关联代码：`app_cybersparker/views/expload/result__manage/all_auto_exp_result.py`, `app_cybersparker/templates/project/expload/result_manage/all_auto_exp_result.html`
- 验收状态：已验收

### 范围
- 列表（ID/创建时间/目标/产品/插件/结果）+ 搜索筛选 + 分页
- 批量删除 + 单行删除
- CSV 下载
- 结果栏展开查看

### 不做
- 不改 CSV 导出后端逻辑
- 不改已有下载文件格式

### 验收条件
- [ ] 新旧页同条件下列表数据一致
- [ ] 搜索/筛选/分页行为与旧页一致
- [ ] 删除/批量删除行为与旧页一致
- [ ] CSV 下载可用

### 依赖
- 无

### 状态记录
- 2026-05-31：阶段十三规划创建。
- 2026-05-31：已完成 React 化。后端新增 `auto_exp_result_list_api` JSON API（`all_auto_exp_result.py`），多维筛选+分页。前端 `AutoExpResultListPage` 覆盖列表/筛选/批量删除/CSV下载。路由：`/api/v1/auto-exp-results`、`/react-shell/auto-exp-results`。

---

## BL-FE-105 字典组管理页 React 化

- 模块：前后端分离
- 优先级：P1
- 状态：已完成
- 当前阶段：阶段十三
- 价值：字典组是字典管理的配套页面，复用已有 dict_list 模式即可完成。
- 关联模块文档：docs/modules/08-前后端分离模块.md
- 关联代码：`app_cybersparker/views/expload/dict_manage.py`, `app_cybersparker/templates/project/expload/dict_group.html`
- 验收状态：已验收

### 范围
- 列表（ID/组名/描述/创建时间）+ 搜索 + 分页
- 新增/编辑表单（2 字段：name, description）
- 删除

### 不做
- 不改字典组与字典的关联逻辑

### 验收条件
- [ ] 新旧页列表数据一致
- [ ] 新增/编辑/删除行为与旧页一致
- [ ] 搜索和分页正常

### 依赖
- 无

### 状态记录
- 2026-05-31：阶段十三规划创建。
- 2026-05-31：已完成 React 化。后端新增 `dict_group_list_api` + CRUD JSON API（`dict_manage.py`）。前端 `DictGroupListPage` 覆盖列表/搜索/新增/编辑/删除表单弹窗。路由：`/api/v1/dict-groups`、`/react-shell/dict-groups`。

---

## BL-FE-106 ceye 配置页 React 化

- 模块：前后端分离
- 优先级：P1
- 状态：已完成
- 当前阶段：阶段十三
- 价值：ceye DNSLog 配置是单表单页，迁移工作量极小。
- 关联模块文档：docs/modules/08-前后端分离模块.md
- 关联代码：`app_cybersparker/views/expload/ceye_config.py`, `app_cybersparker/templates/project/expload/ceye_config.html`
- 验收状态：已验收

### 范围
- 单表单（api_token, identifier）+ 保存
- 当前配置只读展示

### 不做
- 不改 ceye 后端通信逻辑

### 验收条件
- [ ] 表单回填当前配置值
- [ ] 保存后配置生效
- [ ] 旧页仍可访问

### 依赖
- 无

### 状态记录
- 2026-05-31：阶段十三规划创建。
- 2026-05-31：已完成 React 化。后端新增 `ceye_config_api` JSON API（`ceye_config.py`），GET 返回当前配置，POST 保存。前端 `CeyeConfigPage` 单表单页。路由：`/api/v1/ceye-config`、`/react-shell/ceye-config`。

---

## BL-FE-107 导出任务页 React 化

- 模块：前后端分离
- 优先级：P1
- 状态：已完成
- 当前阶段：阶段十三
- 价值：导出任务是只读状态列表，结构简单。
- 关联模块文档：docs/modules/08-前后端分离模块.md
- 关联代码：`app_cybersparker/views/expload/export_task.py`, `app_cybersparker/templates/project/expload/export_task_list.html`
- 验收状态：已验收

### 范围
- 只读状态列表（ID/任务类型/检索条件/导出字段/状态/行数/创建时间）
- 已完成任务的下载按钮
- 刷新按钮

### 不做
- 不改导出执行后端

### 验收条件
- [ ] 列表数据与旧页一致
- [ ] 下载按钮可用
- [ ] 状态标签颜色与旧页一致

### 依赖
- 无

### 状态记录
- 2026-05-31：阶段十三规划创建。
- 2026-05-31：已完成 React 化。后端新增 `export_task_list_api` JSON API（`export_task.py`），只读列表+分页。前端 `ExportTaskListPage` 覆盖列表/刷新/下载按钮。路由：`/api/v1/export-tasks`、`/react-shell/export-tasks`。

---

## BL-FE-108 漏洞利用结果页 React 化

- 模块：前后端分离
- 优先级：P1
- 状态：已完成
- 当前阶段：阶段十三
- 价值：漏洞利用结果是最复杂的结果管理页，含验证工作流。
- 关联模块文档：docs/modules/08-前后端分离模块.md
- 关联代码：`app_cybersparker/views/expload/result__manage/expResult.py`, `app_cybersparker/templates/project/expload/result_manage/all_exp_result.html`
- 验收状态：已验收

### 范围
- 列表（ID/创建时间/目标/插件/结果）+ 搜索筛选 + 分页
- 批量删除 + 单行删除 + CSV 下载
- 结果展开查看（JSON viewer 弹窗）
- 验证弹窗（5 种操作类型：verify/command/code/file/attack + 命令输入）

### 不做
- 不改验证执行引擎
- 不改 CSV 导出后端

### 验收条件
- [ ] 新旧页同条件下列表数据一致
- [ ] 搜索/筛选/分页行为与旧页一致
- [ ] 验证弹窗 5 种操作类型可用
- [ ] 删除/CSV下载可用

### 依赖
- 无

### 状态记录
- 2026-05-31：阶段十三规划创建。
- 2026-05-31：已完成 React 化。后端新增 `exp_result_list_api` JSON API（`expResult.py`），多维筛选+分页。前端 `ExpResultListPage` 覆盖列表/筛选/批量删除/CSV下载/结果展开详情/漏洞验证弹窗（插件选择/操作类型/命令执行）。复用旧 `/getPluginInfo`、`/targetRunVerify`、`/result_List/get_plugin` 端点。路由：`/api/v1/exp-results`、`/react-shell/exp-results`。

---

## BL-FE-109 仪表盘 React 化

- 模块：前后端分离
- 优先级：P1
- 状态：已完成
- 当前阶段：阶段十三
- 价值：仪表盘是首页入口，从 Django 模板迁移到 React 后可统一技术栈。
- 关联模块文档：docs/modules/08-前后端分离模块.md
- 关联代码：`app_cybersparker/views/Dashboards.py`, `app_cybersparker/templates/project/expload/Dashboards.html`
- 验收状态：已验收

### 范围
- 4 张统计卡片（插件数/指纹数/识别结果数/EXP结果数）
- 2 个 ECharts 图表（TOP15 插件结果柱状图 + 类型分布饼图）

### 不做
- 不改数据统计口径
- 不新增图表类型

### 验收条件
- [ ] 4 张卡片数值与旧页一致
- [ ] 2 个图表数据与旧页一致
- [ ] 页面加载无白屏

### 依赖
- 无

### 状态记录
- 2026-05-31：阶段十三规划创建。
- 2026-05-31：已完成 React 化。后端新增 `dashboard_api` JSON API（`Dashboards.py`），返回统计卡片、插件 TOP15、类型分布。前端 `DashboardPage` 4 张统计卡片 + 2 个 CSS 柱状图（插件结果 TOP15 + 类型分布）。路由：`/api/v1/dashboard`、`/react-shell/dashboard`。

---

## BL-FE-110 插件调试页 React 化

- 模块：前后端分离
- 优先级：P1
- 状态：已完成
- 当前阶段：阶段十四
- 价值：插件调试页是 CodeMirror 编辑器 + 实时执行流复合环境，迁移到 React 可统一技术栈并支持 Python/YAML 语法高亮。
- 关联代码：`app_cybersparker/views/expload/exp_debug.py`, `frontend/`, `react_shell.py`, `urls.py`
- 验收状态：已验收

### 范围
- 插件选择下拉（模糊搜索）
- 表单字段（CVE/标题/类型/语言/日期/支持方法）
- POC 代码编辑器（CodeMirror 6，Python/YAML 语法高亮）
- 调试执行（目标 + 操作类型 + 命令参数 + 执行按钮）
- 结果展示（CodeMirror 只读）
- 保存闭环（新增/编辑）

### 不做
- 不改后端执行引擎
- 不改文件读写逻辑

### 验收条件
- [x] 插件列表加载正常
- [x] 代码编辑器支持 Python/YAML 语法高亮
- [x] 调试执行+结果展示正常
- [x] 保存（新增/编辑）闭环正常
- [x] build / check / tests 全过

### 状态记录
- 2026-05-31：创建并完成。新增 JSON API 包装（`api_plugin_list`/`api_exp_info`/`api_exp_execute`/`api_exp_save`），前端 `ExpDebugPage` 接入 `@uiw/react-codemirror`。路由：`/api/v1/exp-debug/*`、`/react-shell/exp-debug`。

---

## BL-FE-111 指纹调试页 React 化

- 模块：前后端分离
- 优先级：P1
- 状态：已完成
- 当前阶段：阶段十四
- 价值：指纹调试页是多维度规则匹配 + favicon/证书/URI 采集工具，迁移到 React 可统一技术栈。
- 关联代码：`app_cybersparker/views/expload/fingerPrint_debug.py`, `frontend/`, `react_shell.py`, `urls.py`
- 验收状态：已验收

### 范围
- URL/代理/规则输入 + 全库匹配开关
- 已有指纹 datalist 模糊回填
- 响应头/响应正文展示（CodeMirror 6 HTML 高亮）
- 匹配结果 + 命中指纹列表
- 资产特征展示（favicon 图标、HTTPS 证书、URI 路径）

### 不做
- 不改后端匹配引擎
- 不改 favicon/证书采集逻辑

### 验收条件
- [x] 指纹列表加载正常
- [x] 规则编辑 + datalist 回填正常
- [x] 匹配执行 + 多面板结果展示正常
- [x] 资产特征（favicon/证书/URI）展示正常
- [x] build / check / tests 全过

### 状态记录
- 2026-05-31：创建并完成。新增 JSON API 包装（`api_fingerprint_list`/`api_fingerprint_mate`），前端 `FingerprintDebugPage` 接入 CodeMirror 6。路由：`/api/v1/fingerprint-debug/*`、`/react-shell/fingerprint-debug`。
