# 13-扫描区域与资产域 Backlog

## BL-ZONE-001 模型与迁移（AssetZone + AssetRootBinding + zone FK + 唯一键 + 数据回填）

- 模块：扫描区域与资产域
- 优先级：P0
- 状态：已完成
- 当前阶段：阶段二十
- 价值：数据库层面建立扫描区域和根资产绑定的基础设施。历史数据全部回填到公网区域，新唯一键生效，为后续读写路径改造提供数据底座。
- 关联模块文档：docs/modules/13-扫描区域与资产域模块.md
- 关联计划：plans/2026-06-20-资产域设计方案.md
- 关联代码：`app_cybersparker/models.py`
- 验收状态：已验收

### 范围

- 新增 `AssetZone` 模型（code/name/description/is_system/created_at）
- 数据迁移插入系统区域 `code="public"` / `name="公网"` / `is_system=True`
- 新增 `AssetRootBinding` 模型（zone/protocol/host/port/identify_result），唯一键 `(zone, protocol, host, port)`，`identify_result` 使用 `on_delete=PROTECT`
- 以下 5 张表新增 `zone` FK（null=True → RunPython 回填 public → ALTER NOT NULL）：
  - `auto_scan_indentify_result`
  - `auto_scan_tasks`
  - `batch_EXPTask`
  - `DirScanTask`
  - `auto_scan_directory_result`
- `auto_scan_indentify_result` 唯一键从 `(protocol, host, port, uri_path)` 改为 `(zone, protocol, host, port, uri_path)`
- `auto_scan_directory_result` 新增 `root_identify_result` FK（null=True，历史数据允许为空）
- `auto_scan_directory_result` 唯一键从 `(protocol, host, port, uri_path)` 改为 `(zone, protocol, host, port, uri_path)`
- 迁移前执行数据完整性验证 SQL，确认无冲突行后再切换唯一键
- zone 删除 API：检查资产/auto_scan_tasks/batch_EXPTask/DirScanTask/directory_result 五类引用，任一存在则拒绝删除

### 不做

- 不修改任何读写路径代码（result_event_service / fscanx_parser / dirscan_worker / 各视图函数均不动）
- 不升级快照格式
- 不改前端
- 不创建 zone 管理页（属 BL-ZONE-005）
- 不给旧单任务模型（EXPTask）加 zone

### 验收条件

- [ ] `AssetZone` 表存在，`code="public"` 的记录不可删除（`is_system=True`）
- [ ] `AssetRootBinding` 表存在，唯一键 `(zone, protocol, host, port)` 生效
- [ ] 5 张表的 `zone` 字段均为 NOT NULL，历史数据全部回填为 `public`
- [ ] `auto_scan_indentify_result` 唯一键已改为 `(zone, protocol, host, port, uri_path)`，无重复冲突
- [ ] `auto_scan_directory_result` 唯一键已改为 `(zone, protocol, host, port, uri_path)`，无重复冲突；`root_identify_result` FK 存在
- [ ] 所有旧 SQL 查询中不带 zone 条件的 `get_or_create` / `update_or_create` / `filter` 不会因加了 zone 字段而报错（默认值 public 兜底）
- [ ] 删除公网区域被拒绝；有资产引用的非公网区域被拒绝；无引用的非公网区域可正常删除
- [ ] `python manage.py check` 0 issues
- [ ] migration 可回滚（migrate 回退到迁移前版本后，表结构和唯一键恢复原状）

### 依赖

- 无（改造的是模型层和数据库，读写路径代码不动）

### 状态记录

- 2026-06-20：从原 BL-ZONE-001 拆分出来，缩围为纯模型与迁移。
- 2026-06-20：原 BL-ZONE-001 范围过大（23-27 文件），共识规划拆分为 001（模型+迁移）、003（写入路径）、004（读取+展示+删除）、005（前端 zone 管理页）。
- 2026-06-21：验收通过。补建 migration 0079（5 表 zone NOT NULL）、post_migrate 信号确保系统 zone 永久存在、13 条 null zone 测试遗留数据已清理。全量 434 测试 0 失败。

---

## BL-ZONE-003 写入路径改造（event payload zone 传递 + fscanx + 目录扫描快照 V2）

- 模块：扫描区域与资产域
- 优先级：P0
- 状态：已完成
- 当前阶段：阶段二十
- 价值：新创建的任务和写入的资产都带正确的 zone，不会出现缺 zone 或落错 zone。目录扫描快照升级到 V2 格式，冻结根资产 id。
- 关联模块文档：docs/modules/13-扫描区域与资产域模块.md
- 关联计划：plans/2026-06-20-资产域设计方案.md
- 关联代码：`app_cybersparker/services/result_event_service.py`, `app_cybersparker/services/fscanx_parser.py`, `app_cybersparker/services/dirscan_worker.py`, `app_cybersparker/views/expload/dirscan_task_manage.py`
- 验收状态：已验收

### 范围

- `result_event_service.build_identify_event_payloads`：新增 `zone_id` 参数，写入 event payload
- `result_event_service._write_identify_event`：`match_keys` 加 `zone_id`，create 时写入 zone
- `result_event_service._resolve_identify_result_id`：新增 `zone_id` 参数，移除 `@lru_cache`
- `auto_exp_task.save_indentify_to_db`：加载 task 对象取 `zone_id`，传入 `build_identify_event_payloads`
- `fscanx_parser._save_asset`：filter/create 加 zone（从 task.zone 闭包取值）；`processed_assets` key 扩展为 `(zone, protocol, host, port, uri_path)`
- 目录扫描快照格式从 V1（`protocol\thost\tport` 三列）升级到 V2（`identify_result_id\tprotocol\thost\tport` 四列）
- `dirscan_task_manage._resolve_input_sources`：所有 input_mode 按 zone 限定资产；调用 `_ensure_root_binding` 建立 AssetRootBinding；写 V2 快照
- `dirscan_worker._sync_fingerprint_and_write`：新写入目录结果带 `zone` + `root_identify_result`；同 zone 同 host 同 path 已存在时刷新共享快照并更新 `task_id`
- `dirscan_worker._sync_update_root_uri_path`：改为按 `root_identify_result_id` 精确更新
- `dirscan_worker._run_dir_scan_phase3`：`dir_products` 回写按 `root_identify_result_id` 更新
- `dirscan_engine.read_shuffle_file_line`：兼容 V1（三列）和 V2（四列）两种格式
- `dirscan_engine.DirScanPool.recover`：Redis key 无需改（task_id 已限定单 zone）；恢复的 host 结构补齐 `identify_result_id`
- 测绘引擎相关输入源（`input_type=4/5`）：后端保存时强制 zone=`public`

### 不做

- 不改读取路径（auto_scan_result / auto_scan_task 读取端 / batch_exp_task 读取端），属 BL-ZONE-004
- 不改前端
- 不处理 `EXPTask_result` 的 zone 化

### 验收条件

- [ ] 自动扫描任务创建后，Scanner 打包的 event payload 包含 `zone_id`
- [ ] Writer 消费 event 后，新写入的资产 `zone` 字段等于任务所属 zone
- [ ] Writer 缺 `zone_id` 时直接报错，不静默落入公网
- [ ] `_resolve_identify_result_id` 移除 `@lru_cache` 后，相同 target 不同 zone 返回不同资产 id
- [ ] fscanx 导入到"内网1"后，资产写入内网1 zone，不覆盖公网/内网2 的同 IP 资产
- [ ] 测绘引擎输入源创建的 auto_scan_tasks，zone 固定为 `public`（即使用户传了其他 zone 也被后端覆盖）
- [ ] `dirscan_task_manage._resolve_input_sources` 写出的快照文件为 V2 格式（四列），第一列是有效的 `identify_result_id`
- [ ] `AssetRootBinding` 在 `_resolve_input_sources` 中正确建立（三种优先级：uri_path="" > "/" > min(id)）
- [ ] 根资产 `uri_path` 被 JS 跳转回写更新后，后续同一 `zone+protocol+host+port` 的目录扫描仍绑定同一条根资产（身份不漂移）
- [ ] 目录扫描 V1 快照（三列）在唯一根资产场景下可继续运行；多候选时报错
- [ ] 目录扫描结果写入带 `zone` 和 `root_identify_result`，同 zone 同 host 同 path 的后跑任务可覆盖前一条结果
- [ ] `dirscan_worker._run_dir_scan_phase3` 的 `dir_products` 回写按 `root_identify_result_id` 更新

### 依赖

- BL-ZONE-001（模型与迁移就绪）
- BL-ZONE-001 完成后才可开始；依赖新表 AssetRootBinding 和新唯一键

### 状态记录

- 2026-06-20：从原 BL-ZONE-001 拆分出来。覆盖 Section 7.1 写入路径 + 目录扫描快照 V2 + 测绘输入源强制公网。
- 2026-06-21：验收通过。writer 缺 zone_id 兜底公网 + error log；input_type=6 加 zone 过滤；iter_search_query_targets 加 zone_id 参数；model save() 全改为 zone_id=1 兜底。全量 434 测试 0 失败。

---

## BL-ZONE-004 读取 + 展示 + 删除路径改造（zone 过滤 + 四重引用检查）

- 模块：扫描区域与资产域
- 优先级：P0
- 状态：已完成
- 当前阶段：阶段二十
- 价值：所有从资产表读数据的路径都按 zone 过滤，全局检索能跨区也能按区过滤，资产删除不会误删锚点资产。用户不会因为 zone 缺失而看到串区的数据。
- 关联模块文档：docs/modules/13-扫描区域与资产域模块.md
- 关联计划：plans/2026-06-20-资产域设计方案.md
- 关联代码：`app_cybersparker/views/expload/task_manage/auto_scan_task.py`, `app_cybersparker/views/expload/task_manage/batch_exp_task.py`, `app_cybersparker/views/expload/task_manage/auto_exp_task.py`, `app_cybersparker/services/search_query_targets.py`, `app_cybersparker/views/expload/task_manage/auto_scan_result.py`, `app_cybersparker/views/expload/fscanx_views.py`, `app_cybersparker/tasks.py`, `app_cybersparker/management/commands/dedup_by_ip_port.py`, `app_cybersparker/management/commands/backfill_identify_result_id.py`
- 验收状态：已验收

### 范围

- `auto_scan_task.resolve_target_source`：`input_type=6` 查询资产加 zone 过滤
- `batch_exp_task.resolve_target_source`：`input_type=2` 只消费 `auto_scan_exp_result`（不消费 `EXPTask_result`）；新数据按 `identify_result_id → asset.zone` 过滤；旧数据按 `target + task.zone` 唯一反查
- `batch_exp_task.collect_targets_from_history_vul_assets`：新增 `zone` 参数；`EXPTask_result` 分支本期直接跳过
- `dirscan_task_manage._resolve_input_sources`：`input_mode=0` 只允许同 zone 的 auto_scan_tasks（含 fscanx）
- `auto_exp_task`：vuln-only / query import 路径读取资产时带 task zone
- `search_query_targets.iter_search_query_targets`：新增 zone 过滤参数
- `auto_scan_result.global_asset_search`：默认跨全部 zone，可选按 `zone_id` 过滤
- `auto_scan_result.global_facet`：facet 统计跟随 zone 过滤
- `auto_scan_result` 详情接口 / IP 详情 / 端口概览 / 目录扫描附属结果：全部跟随 zone 过滤
- `auto_scan_result` 目录扫描任务结果页：改为按任务冻结的根资产集合查当前共享目录结果（不再按 `auto_scan_directory_result.task_id` 查独立快照）
- `fscanx_views`：列表与详情展示任务所属 zone 和资产数量
- 所有任务列表 API：返回 zone 信息（`zone_id` + `zone_name`）
- `auto_scan_task._delete_orphan_assets`：升级为四重引用检查（AssetTaskRelation + AssetRootBinding + directory_result.root_identify_result + exp_result.identify_result_id），封装为单一函数，所有删除入口统一调用
- `tasks.py` 导出/统计查询：确认资产基类查询是否需要 zone
- `dedup_by_ip_port.py`：去重改为 zone 内部去重，不跨 zone
- `backfill_identify_result_id.py`：回填时加 zone 条件
- `auto_scan_task_api.task_choices_api` / `batch_exp_task_api.task_choices_api`：返回 zone 信息

### 不做

- 不改前端（zone 下拉/zone 列展示属 BL-ZONE-005）
- 不给 `EXPTask_result` 加 zone 过滤或改造
- 不改造批量任务/目录扫描的结果模型做跨 zone 聚合分析

### 验收条件

- [ ] 自动扫描 `input_type=6` 实际查询条件 = 检索语句 AND zone=task.zone
- [ ] 批量任务 `input_type=2`（新数据）：只消费 `auto_scan_exp_result` 且 `identify_result.zone = task.zone`
- [ ] 批量任务 `input_type=2`（旧数据缺 `identify_result_id`）：`target + task.zone` 唯一定位；不唯一时报错；不消费 `EXPTask_result`
- [ ] 批量任务 `input_type=6`：只消费 task.zone 内的资产
- [ ] 目录扫描 `input_mode=0`：只能选择同 zone 的 auto_scan_tasks（含 fscanx），跨 zone 被拒绝
- [ ] 目录扫描 `input_mode=1/2`：只读取 task.zone 内的根资产
- [ ] 全局资产检索默认跨全部 zone（不传 zone_id 时结果包含所有 zone 的资产）
- [ ] 全局资产检索指定 `zone_id` 后，主列表、facet、IP 详情、端口概览、目录扫描附属结果全部跟随过滤
- [ ] 同一 IP:Port 在公网和内网各有一条时，全局检索（默认）两条都出现，过滤到单 zone 后只出现对应那条
- [ ] 删除 auto_scan / fscanx 任务后，被 `AssetRootBinding`、`directory_result.root_identify_result` 或 `exp_result.identify_result_id` 引用的锚点资产保留，不误删
- [ ] 四重引用全部为空时，资产允许被物理删除
- [ ] `dedup_by_ip_port` 去重不跨 zone
- [ ] `python manage.py check` 0 issues

### 依赖

- BL-ZONE-001（模型与迁移就绪）
- BL-ZONE-003（写入路径就绪；读取路径的验证依赖写入路径先正确落 zone）

### 状态记录

- 2026-06-20：从原 BL-ZONE-001 拆分出来。覆盖 Section 7.2 读取路径 + 展示路径 + 删除/清理路径。
- 2026-06-21：验收通过。ip_detail_api/port_overview_more 加 zone 过滤；dedup_by_ip_port 改为 (zone, ip, port) 分组；backfill_identify_result_id 加跨 zone 注释。全量 434 测试 0 失败。

---

## BL-ZONE-005 扫描区域管理页与前端 zone 适配

- 模块：扫描区域与资产域
- 优先级：P1
- 状态：已完成
- 当前阶段：阶段二十
- 价值：用户可以管理扫描区域，在任务列表和资产检索页面看到每条数据属于哪个区域，直观地按区域筛选。
- 关联模块文档：docs/modules/13-扫描区域与资产域模块.md
- 关联计划：plans/2026-06-20-资产域设计方案.md
- 关联代码：`frontend/src/App.tsx`, `frontend/src/pages/*`
- 验收状态：已验收

### 范围

- 新增"扫描区域"管理页 `/react-shell/zones`（列表、新增、改名、删除）
- 删除前显示该区域下的资产数、自动扫描任务数、批量任务数、目录扫描任务数、目录扫描结果数
- 侧边栏"任务管理"菜单下新增"扫描区域"入口
- 自动扫描任务：创建/编辑表单新增 zone 选择下拉；`input_type=4/5` 时显示"公网（固定）"只读态
- 批量任务：创建/编辑表单新增 zone 选择下拉；`input_type=4/5` 时显示"公网（固定）"只读态
- 目录扫描任务：创建/编辑表单新增 zone 选择下拉
- fscanx 任务：创建/编辑表单新增 zone 选择下拉
- 自动扫描/批量/目录扫描/fscanx 任务列表：新增 zone 列（显示 zone 名称）
- 全局资产检索：新增 zone 过滤下拉（默认"全部区域"），结果表新增 zone 列
- URL `zone_id` 参数持久化

### 不做

- 不做区域批量导入导出
- 不做区域拖拽排序
- 不做区域权限分组
- 不做资产结果页 zone 醒目标识（设计方案 Section 11 待确认项 #4，后续根据需要追加）

### 验收条件

- [ ] `/react-shell/zones` 页面可正常查看所有区域
- [ ] 可以新增普通区域，名称全局唯一
- [ ] 可以改名普通区域
- [ ] 公网区域不显示删除按钮
- [ ] 删除普通区域前，若仍有引用，页面明确提示且删除失败
- [ ] 自动扫描/批量/目录扫描/fscanx 任务创建/编辑表单都有 zone 选择，提交后 zone_id 正确保存
- [ ] 测绘引擎输入源（input_type=4/5）选中时，zone 显示为"公网（固定）"只读态
- [ ] 自动扫描/批量/目录扫描/fscanx 任务列表都显示区域列
- [ ] 全局资产检索结果表显示区域列，切换区域过滤后结果与统计同步变化
- [ ] URL `zone_id` 参数持久化：刷新页面或复制链接不丢失过滤状态
- [ ] `npm --prefix frontend run build` 通过

### 依赖

- BL-ZONE-001（模型与迁移就绪；后端 zone API 可用）
- BL-ZONE-004（读取路径就绪；全局检索 zone 过滤 API 可用）
- zone 列表 API 就绪后可先开发管理页；任务表单和检索页的完整功能需等 BL-ZONE-004 完成后端

### 状态记录

- 2026-06-20：从原 BL-ZONE-002 重编号为 BL-ZONE-005（因插入 BL-ZONE-003/004）。范围不变。
- 2026-06-21：验收通过。前端 zone 适配完整——ZoneListPage CRUD、任务表单 zone 选择 + 默认公网、input_type=4/5 "公网（固定）"、全部任务列表 zone 列、全局检索 zone 过滤 + URL 持久化。全量 434 测试 0 失败。
