# 文档列出但代码未实现的事项

> 2026-06-01 审计产出。仅记录"文档声称存在但代码中找不到"的情况。

经全面审计，这类情况极少——主要问题是反方向：代码已实现但文档未跟上（已批量修复）。

## 已确认：文档有但代码无

### 1. `/dirscan/task/<id>/result` 路由

- **来源文档**：`docs/modules/07-目录扫描模块.md`（旧版）
- **文档描述**：`GET /dirscan/task/<id>/result` — 任务结果查看
- **实际代码**：`cybersparker/urls.py` 中无此路由。目录扫描任务路由只有 list/add/edit/delete/detail/operate，无独立的 result 页面路由
- **影响**：无。目录扫描结果目前通过自动扫描结果页（`/react-shell/auto-exp-results`）统一查看
- **处理**：已从模块文档中移除该路由条目

---

## 说明

本次审计（25 条发现）的绝大部分属于：

- **代码有、文档无**（under-documented）：如 3 个 React 页面/路由未列入文档、8 个模型未记录、Celery 队列数少写 2 个等
- **文档自相矛盾**：如阶段十二同时说"不做迁移"和"已完成迁移"、页面数量 16/18/20 四处不一致
- **文档过时**：如模块 08 停留在二期评估阶段、模块 07 模型标"待实现"但早已实现

真正的"文档列出但代码未实现"仅上述 1 条。
