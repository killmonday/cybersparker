# 项目控制台

> 最后更新：2026-06-23（seed_data.sql 头部污染修复：修正导出脚本 heredoc，避免 fresh deploy 自动导入在 `date` 处报 SQL 语法错误）

## 前后端分离项目 — 最终状态

**全部后台页面已完成 React 迁移**，覆盖全部后台内容区。侧边栏入口全部切到新路径。旧 Django HTML 页面（80+ 路由、27 模板）已删除，仅保留 `login.html`。所有数据接口统一在 `/api/v1/*` 下。Nginx 直接提供 React 壳页（Vite 构建产出的 `index.html`），Django 仅处理 API 和登录认证。

**调试工作台线（插件调试页、指纹调试页）已完成 React 迁移**：接入 CodeMirror 6（`@uiw/react-codemirror`），插件调试页覆盖插件选择/代码编辑（Python/YAML）/表单字段/调试执行/结果展示/保存闭环；指纹调试页覆盖 URL 输入/代理选择/规则编辑（含已有指纹 datalist）/全库匹配开关/响应头/正文/匹配结果/命中指纹列表/资产特征展示。

### 运行部署维护（2026-06-23）

| Backlog ID | 说明 | 优先级 | 状态 | 验收 |
|---|---|---|---|---|
| BL-DEPLOY-002 | 种子数据导出头部污染修复 | P0 | 已完成 | 待验收 |

## 已完成 Backlog

### 阶段七：前后端分离基线

| Backlog ID | 说明 | 状态 | 验收 |
|---|---|---|---|
| BL-FE-001 | 鉴权、CSRF 与 API 契约冻结 | 已完成 | 已验收 |
| BL-FE-002 | React 基线接入、Django 壳页挂载 | 已完成 | 已验收 |
| BL-FE-003 | 首批低风险列表页（插件/字典/代理/引擎/指纹） | 已完成 | 已验收 |
| BL-FE-004 | 结果页接口抽离（不换 UI） | 已完成 | 已验收 |
| BL-FE-005 | 中风险表单页（代理/引擎/指纹可写闭环） | 已完成 | 已验收 |
| BL-FE-006 | 高风险页面二期评估与拆分 | 已完成 | 已验收 |

### 阶段八：结果展示线

| Backlog ID | 说明 | 状态 | 验收 |
|---|---|---|---|
| BL-FE-101 | standalone 结果页 React 化 | 已完成 | 已验收 |
| BL-FE-102 | 全局资产检索页 React 化 | 已完成 | 已验收 |
| BL-FE-103 | 结果详情附属能力（HTML/漏洞/端口） | 已完成 | 已验收 |

### 阶段九：自动扫描任务页

| Backlog ID | 说明 | 状态 | 验收 |
|---|---|---|---|
| BL-FE-201 | 后端 API + React 列表 + 轮询 | 已完成 | 已验收 |
| BL-FE-202 | 新增表单 + 操作闭环 | 已完成 | 已验收 |

### 阶段十：批量任务页

| Backlog ID | 说明 | 状态 |
|---|---|---|
| BL-BATCH-201 | 后端 API + React 列表 + 轮询 + EXP 详情抽屉 | 已完成 | 已验收 |
| BL-BATCH-202 | 新增表单 + 操作闭环 | 已完成 | 已验收 |

### 阶段十一：目录扫描页

| Backlog ID | 说明 | 状态 |
|---|---|---|
| BL-DIRSCAN | API + React 列表 + 操作闭环 | 已完成 | 已验收 |

### 阶段十二：调试工作台线

| 决策 | 说明 |
|------|------|
| 已完成 React 迁移 | 插件调试页/指纹调试页已 React 化，接入 CodeMirror 6，覆盖调试执行/结果展示/保存闭环（详见 BL-FE-110~111） |

## React 页面路由总览

| 页面 | React 路由 | 侧边栏已切 |
|------|-----------|-----------|
| 插件列表 | `/react-shell/plugins` | 是 |
| 插件详情 | `/react-shell/plugins/:id` | 是 |
| 字典列表 | `/react-shell/dicts` | 是 |
| 代理设置 | `/react-shell/proxies` | 是 |
| 测绘引擎 | `/react-shell/engines` | 是 |
| 指纹管理 | `/react-shell/fingerprints` | 是 |
| 资产检索 | `/react-shell/assets/search` | 是 |
| 任务结果 | `/react-shell/identify-tasks/<uid>/results` | — |
| 同IP端口详情 | `/react-shell/ip-detail` | — |
| 自动扫描任务 | `/react-shell/identify-tasks` | 是 |
| 批量任务 | `/react-shell/batch-tasks` | 是 |
| 目录扫描 | `/react-shell/dirscan-tasks` | 是 |
| 自动扫描结果 | `/react-shell/auto-exp-results` | 是 |
| 字典组管理 | `/react-shell/dict-groups` | 是 |
| ceye 配置 | `/react-shell/ceye-config` | 是 |
| 导出任务 | `/react-shell/export-tasks` | 是 |
| 漏洞利用结果 | `/react-shell/exp-results` | 是 |
| 仪表盘 | `/react-shell/dashboard` | 是 |
| 插件调试 | `/react-shell/exp-debug` | 是 |
| 文件托管 | `/react-shell/hosted-files` | 是 |
| 指纹调试 | `/react-shell/fingerprint-debug` | 是 |
| 文件管理 | `/react-shell/target-files` | 是 |
| AI 模型配置 | `/react-shell/ai-model-configs` | 是 |
| AI 生成 PoC | `/react-shell/poc-gen-tasks` | 是 |
| PoC 执行页 | `/react-shell/poc-gen-tasks/:id` | — |
| fscanx 任务 | `/react-shell/fscanx-tasks` | 是 |
| fscanx 详情 | `/react-shell/fscanx-tasks/:taskId` | — |
| 用户管理 | `/react-shell/users` | 是 |
| 漏洞快捷利用 | `/react-shell/vuln-exploit` | — |
| 目录扫描结果 | `/react-shell/dirscan-results` | — |

### 阶段十九：用户管理

| Backlog ID | 说明 | 优先级 | 状态 | 验收 |
|---|---|---|---|---|
| BL-USER-001 | 用户模型与认证改造（UserProfile + migration + login 改写 + AuthContext） | P0 | 已完成 | 已验收 |
| BL-USER-002 | 用户管理 API（/api/v1/users CRUD + 角色边界校验 + session 失效） | P0 | 已完成 | 已验收 |
| BL-USER-003 | 后端权限执行（permissions.py + 约 115 个 API 视图加权限） | P0 | 已完成 | 已验收 |
| BL-USER-004 | 前端用户管理页 + 按钮权限适配（UserManagementPage + 各页面按钮条件渲染） | P0 | 已完成 | 已验收 |

**阶段目标**：新增用户管理体系——超级管理员可以管所有人（增删改密码改角色），普通管理员可以管所有功能模块但只能管普通用户，普通用户只能看不能改（且不能看插件/指纹内容）。

**关联文档**：`docs/modules/12-用户管理模块.md`、`docs/backlog/12-用户管理.md`、`plans/2026-06-19-用户管理设计.md`

**依赖顺序**：BL-USER-001 → BL-USER-002 → BL-USER-003 + BL-USER-004（003 和 004 可并行）

### 阶段十八：文件托管

| Backlog ID | 说明 | 优先级 | 状态 | 验收 |
|---|---|---|---|---|
| BL-HOST-001 | 文件托管页面（上传/下载/删除/重命名/公开鉴权） | P1 | 已完成 | 已验收 |

**阶段目标**：提供独立的通用文件托管服务，用户上传任意类型文件，设置公开或需登录下载，生成直接下载链接供公网访问。

**关联文档**：`docs/modules/11-文件托管模块.md`、`docs/backlog/11-文件托管.md`、`plans/2026-06-19-文件托管.md`

### 阶段十三 Backlog 总览（6 个剩余页面）

| Backlog ID | 页面 | 优先级 | 状态 | 复杂度 |
|---|---|---|---|---|
| BL-FE-104 | 自动扫描结果 | P1 | 已完成 | 已验收 | 低 |
| BL-FE-105 | 字典组管理 | P1 | 已完成 | 已验收 | 低 |
| BL-FE-106 | ceye 配置 | P1 | 已完成 | 已验收 | 低 |
| BL-FE-107 | 导出任务 | P1 | 已完成 | 已验收 | 低 |
| BL-FE-108 | 漏洞利用结果 | P1 | 已完成 | 已验收 | 高 |
| BL-FE-109 | 仪表盘 | P1 | 已完成 | 已验收 | 中 |

### 阶段十四：文件管理

| Backlog ID | 说明 | 优先级 | 状态 | 验收 |
|---|---|---|---|---|
| BL-FILE-001 | 文件管理页（上传/下载/删除） | P1 | 已完成 | 已验收 |
| BL-FILE-002 | 测绘引擎文件自动清理（Celery Beat） | P2 | 已完成 | 已验收 |

**阶段目标**：用户通过独立页面管理目标文件（上传/下载/删除），删除时自动处理任务引用；测绘引擎缓存数据 60 天自动清理。

**依赖**：`EXP_input/` 目录、AuthMiddleware、`maintenance` Celery 队列、Celery Beat（需新增调度配置）。

### 阶段十五：AI生成PoC

| Backlog ID | 说明 | 优先级 | 状态 | 验收 |
|---|---|---|---|---|
| BL-AIPOC-001 | AI模型配置 CRUD（API + React页面） | P0 | 已完成 | 已验收 |
| BL-AIPOC-002 | PoC生成任务 — 模型 + 创建页 + 列表页 + 资料提取 | P0 | 已完成 | 已验收 |
| BL-AIPOC-003 | PoC生成任务 — 执行页 + Celery异步生成 + 结果展示 | P0 | 已完成 | 已验收 |
| BL-AIPOC-004 | 保存PoC到EXP插件库 | P0 | 已完成 | 已验收 |
| BL-AIPOC-005 | 插件类型从创建时必选改为执行页可选切换 | P1 | 已完成 | 已验收 |
| BL-AIPOC-006 | URL爬取HTML清洗增强（含GitHub/知乎/微信适配） | P0 | 已完成 | 已验收 |
| BL-AIPOC-007 | URL爬取图片下载与识图转文字修复 | P0 | 已完成 | 已验收 |
| BL-AIPOC-008 | URL爬取反爬对抗（浏览器伪装 + 懒加载图片等） | P0 | 已完成 | 已验收 |
| BL-AIPOC-009 | 直接输入文本作为参考资料 | P1 | 已完成 | 已验收 |

**阶段目标**：用户配置 AI 模型 → 通过 URL 爬取或上传文件提供漏洞参考资料 → AI 自动生成 Python/Nuclei PoC → 一键保存到 EXP 插件库。

**依赖**：BL-AIPOC-001 → BL-AIPOC-002 → BL-AIPOC-003 → BL-AIPOC-004；外部依赖：Puppeteer + turndown（Node.js）、openai/dashscope/py7zr（Python）、01-插件管理（EXP 表入库）。

**关联文档**：`docs/modules/10-AI生成PoC模块.md`、`docs/backlog/10-AI生成PoC.md`、`plans/2026-06-07-AI生成PoC-需求澄清.md`

### 最终架构

- Django：仅负责 `/api/v1/*`（JSON API）、`/login`、`/logout`、`/runtime/diagnostics`。
- Nginx：直接返回 React 壳页（Vite 构建产出的 `index.html`），静态资源长缓存（30d）。
- 开发兜底：Django 内置 `_serve_react_shell` 视图，直连 8999 端口时也能访问 React 页面。
- URL 路由总数：142 条（134 条 API + 8 条非 API/认证/诊断/兜底）。
- 旧 Django 模板：仅保留 `login.html`，其余 27 个已删除。

### 执行顺序建议

1. `BL-FE-101` → `BL-FE-102`
2. `BL-FE-101` + `BL-FE-102` → `BL-FE-103`

### 当前阶段风险

| 风险 | 等级 | 说明 |
|---|---|---|
| task / global 范围串线 | 高 | standalone 与全局页共用结果组件时，最容易把 task_id 约束带丢或误带进全局页 |
| 新旧搜索结果口径不一致 | 高 | 新旧页若在总数、首屏列表、facet 统计上不一致，会直接破坏结果页可信度 |
| 详情附属能力收口不完整 | 中 | HTML 原文查看、漏洞结果查看、端口概览懒加载都挂在旧入口上，迁移时容易漏接 |

## 阶段一（核心能力基线）历史状态

阶段目标：建立漏洞利用平台当时的核心执行链路——插件管理、单任务/批量任务/自动识别三类任务执行、结果统一管理、代理与空间测绘引擎配置。说明：这里是阶段一历史记录，单任务当前已降为遗留兼容链路。

**阶段一验收状态：代码层面已验收（12/12 backlog item 代码契约验证通过）。运行时端到端测试待补充（需启动服务并人工走查 UI）。**

### 阶段十七：fscanx 输出文件导入

| Backlog ID | 说明 | 优先级 | 状态 | 验收 |
|---|---|---|---|---|
| BL-AUTO-019 | fscanx 输出文件导入（模型变更 + 10处同步代码 + 解析器 + 线程执行 + API + 前端） | P1 | 已完成 | 已验收 |

**阶段目标**：用户上传 fscanx 扫描器输出文件，系统自动解析端口/Web产品/弱口令/OS信息/RDP信息/FTP&SM共享清单等数据，资产数据写入 `auto_scan_indentify_result` 参与全局检索，服务详情存入新表 `fscanx_service_detail` 在独立页面展示。

**核心变更**：`auto_scan_indentify_result.unique_together` 扩展为 `(protocol, host, port, uri_path)`，新增 `source_type` 字段和 `fscanx_service_detail` 表。

**关联文档**：`plans/2026-06-18-fscanx-导入功能.md`

### 阶段二十：扫描区域与资产域（共识通过，2026-06-21 全链路修复完成）

| Backlog ID | 说明 | 优先级 | 状态 | 验收 |
|---|---|---|---|---|
| BL-ZONE-001 | 模型与迁移（AssetZone + AssetRootBinding + zone FK + 唯一键 + 数据回填） | P0 | 已完成 | 已验收 |
| BL-ZONE-003 | 写入路径改造（event payload zone 传递 + fscanx + 引擎强制公网） | P0 | 已完成 | 已验收 |
| BL-ZONE-004 | 读取 + 展示 + 删除路径改造（zone 过滤 + 四重引用检查） | P0 | 已完成 | 已验收 |
| BL-ZONE-005 | 扫描区域管理页与前端 zone 适配 | P1 | 已完成 | 已验收 |

**阶段目标**：让公网资产和多个内网资产域在同一套任务、检索、目录扫描、漏洞验证流程中并存，不再因相同 `ip:port` 覆盖或串线。

**执行顺序**：BL-ZONE-001 → BL-ZONE-003 → BL-ZONE-004 → BL-ZONE-005（严格顺序；001 是数据底座，003 是写入正确性根基，004 是读取正确性收口，005 是前端接入）。

**2026-06-21 修复摘要**：ModelForm 补 zone 字段，save() NULL→公网兜底，详情 API 补 zone_id，前端 zone 预加载+默认公网+切换即时搜索，Task_result/facet 加 zone 过滤+用户可切换，HTML 检索语义分流（tsvector），索引清理 2 个+补建 4 个，tsvector 子查询性能修复，设计文档索引清单更新至 23 个，数据库 5745 条 NULL 回填，新增 9 个测试，全量 434 测试 0 失败，11 个 commits。

**共识结论**：Mode H 已通过。方向定为”单资产主表 + AssetZone + AssetRootBinding 根资产绑定”。zone 过 Redis Stream 采用方案 A（event payload 携带 zone_id，不由 writer 回查 task 表）。测绘输入源固定公网，目录扫描通过 `root_identify_result_id` 绑定根资产，目录扫描结果按资产维度共享快照存储（允许同 zone 下多个任务互相刷新同一份结果），批量任务 `history vuln assets` 本期只支持自动扫描链路的 `auto_scan_exp_result`。

**关联文档**：`docs/modules/13-扫描区域与资产域模块.md`、`docs/backlog/13-扫描区域与资产域.md`、`plans/2026-06-20-资产域设计方案.md`

## 活动 Backlog

| Backlog ID | 模块 | 优先级 | 状态 | 验收 | 审计 |
|---|---|---|---|---|---|
| BL-PLUGIN-001 | 插件管理 | P0 | 已完成 | 已验收 | 6/6 条件通过 |
| BL-PLUGIN-002 | Nuclei YAML 运行时 | P0 | 已完成 | 已验收 | 3/3 条件通过（条件精化后） |
| BL-TASK-001 | 单任务执行 | P0 | 已完成 | 已验收 | 5/5 条件通过 |
| BL-BATCH-001 | 批量任务执行 | P0 | 已完成 | 已验收 | 5/5 条件通过（条件精化后） |
| BL-AUTO-001 | 自动识别任务 | P0 | 已完成 | 已验收 | 5/5 条件通过（条件精化后） |
| BL-FINGER-001 | 指纹规则管理 | P1 | 已完成 | 已验收 | 3/3 条件通过 |
| BL-RESULT-001 | 统一结果管理 | P1 | 已完成 | 已验收 | 6/6 条件通过 |
| BL-PROXY-001 | 代理配置管理 | P1 | 已完成 | 已验收 | 4/4 条件通过 |
| BL-ENGINE-001 | 空间测绘引擎配置 | P1 | 已完成 | 已验收 | 2/2 条件通过（条件精化后） |
| BL-RUNTIME-001 | 请求运行时 Monkey Patch | P1 | 已完成 | 已验收 | 3/3 条件通过 |
| BL-AUTH-001 | 认证中间件 | P1 | 已完成 | 已验收 | 4/4 条件通过 |
| BL-DEPLOY-001 | Docker 部署与自动构建 | P2 | 已完成 | 已验收 | 3/3 条件通过 |
| BL-BATCH-005 | 空间测绘引擎数据复用控制 | P1 | 已完成 | 已验收 | 5/5 条件通过 |
| BL-BATCH-006 | 批量任务续跑功能 | P1 | 已完成 | 已验收 | 5/5 条件通过 |
| BL-PG-001 | SQLite → PostgreSQL 数据库迁移 | P1 | 已完成 | 已验收 | 四层验证通过 |
| BL-ENHANCE-001 | 独立结果页 + 聚类统计 + 异步分页 | P1 | 已完成 | 已验收 | — |
| BL-ENHANCE-002 | HTML 内容精准检索（pg_trgm + GIN） | P1 | 已完成 | 已验收 | — |
| BL-ENHANCE-003 | 产品存储重构（ArrayField + GIN 索引） | P1 | 已完成 | 已验收 | — |
| BL-ENHANCE-004 | 查询解析器修复（引号内特殊字符） | P1 | 已完成 | 已验收 | — |
| BL-AUTO-015 | HTML 检索语义分流 | P1 | 已完成 | 已验收 | — |
| BL-AUTO-020 | 测绘引擎非HTTP协议资产导入 | P1 | 已完成 | 已验收 | — |
| BL-AUTO-021 | 裸域名 URL 解析误判修复 | P1 | 已完成 | 已验收 | — |

## 增强迭代变更摘要（2026-05-15）

| 变更 | 说明 |
|------|------|
| 独立结果页 | `/Identify_task/<id>/result/standalone` — 左侧聚类统计、异步分页、收起/展开全部、HTML 源码查看、每页条数控制 |
| 聚类统计动态刷新 | 切换检索条件后侧边栏统计自动跟随当前过滤结果更新 |
| HTML 内容检索 | `html:"keyword"` 搜索目标主机 HTML 响应内容，GIN 索引加速 |
| 产品存储 | `CharField(\n 分隔)` → `ArrayField(CharField)`，精确 `product:"nginx"` 查询，GIN 索引加速 |
| 查询解析器 | 修复引号内含 `()`、空格等特殊字符时解析失败的问题（如 `product:"Microsoft(ISA Server)"`） |

## 验证证据摘要

| 维度 | 结果 |
|---|---|
| Django 系统检查 | 0 issues |
| 数据库迁移 | 全部已应用 |
| URL 路由解析 | 全部通过 |
| 模型导入 | 全部模型可导入，字段与文档一致 |
| 视图函数导入 | 全模块 CRUD 视图可导入（插件/任务/指纹/代理/引擎/结果/目录扫描/导出/仪表盘） |
| 执行引擎 | 全部可导入（单/批/自动/指纹/Nuclei/POC/适配器/测绘服务/目录扫描） |
| Monkey Patch | 7/7 patch 模块可导入 |
| 中间件注册 | 9 个中间件正确注册，Auth/Basic 已包含 |
| 错误日志 | `error_log/` 存在，3 个历史日志文件 |
| 模板文件 | 旧 Django 模板已删除（27 个），仅保留 `login.html`（见最终架构） |
| 契约一致性 | URL 映射 100% 匹配，数据模型 100% 匹配 |
| CHANGELOG | 已同步至 2026-06-19 |

## 审计发现的处理

| 发现 | 处理 |
|---|---|
| 4 条模糊验收条件 | 已精化为具体可测表述 |
| CHANGELOG 滞后 | 已补齐 2026-04-22、2026-04-25、2026-05-13 变更 |
| `batch_exp_task copy.py` 冗余文件 | 记录为 BL-CANDIDATE-10（遗留文件清理，P1） |
| `indentify` 全局拼写错误 | 记录为阶段二候选修正，暂不影响功能 |
| 阶段验收条件缺少自动化验证 | 记录为阶段二 BL-CANDIDATE-01（测试覆盖补强） |
| 缺少 `/logout` 路由 | 不阻塞阶段一，记录为阶段二候选 |

## 阶段一剩余风险

| 风险 | 等级 | 说明 |
|---|---|---|
| 无运行时端到端测试 | 中 | 当前验证为代码契约级，未启动服务做实际 HTTP 请求验证 |
| 无前端 UI 人工验证 | 低 | CRUD 页面 JS 报错/表单提交成功需浏览器端人工确认 |
| PyYAML 未在 requirements.txt | 低 | `pyyaml` 是 Nuclei 运行时依赖但未声明在 requirements.txt 中 |

## 阶段二调度改造（全部验收通过）

| Backlog ID | 模块 | 优先级 | 状态 | 验收 | 说明 |
|---|---|---|---|---|---|
| BL-SCHED-001 | 运行部署与可观测性 | P0 | 已完成 | 已验收 | 全局调度基线观测与状态模型 |
| BL-SCHED-002 | 运行部署与可观测性 | P0 | 已完成 | 已验收 | Redis + Celery 基础设施 |
| BL-AUTO-002 | 指纹与自动识别 | P0 | 已完成 | 已验收 | 自动识别任务投递迁移到 Celery |
| BL-BATCH-007 | 任务执行 | P0 | 已完成 | 已验收 | 批量任务投递迁移到 Celery |
| BL-SCHED-005 | 运行部署与可观测性 | P0 | 已完成 | 已验收 | Redis 全局资源预算令牌 |
| BL-SCHED-006 | 运行部署与可观测性 | P0 | 已完成 | 已验收 | 结果事件缓冲与 DB Writer |
| BL-SCHED-007 | 运行部署与可观测性 | P1 | 已完成 | 已验收 | 结果缓冲本地 Spool 治理增强 |
| BL-AUTO-003 | 指纹与自动识别 | P1 | 已完成 | 已验收 | 自动识别 Web 请求 async 化 |
| BL-AUTO-004 | 指纹与自动识别 | P1 | 已完成 | 已验收 | 自动识别指纹与漏洞验证并发治理 |
| BL-AUTO-006 | 指纹与自动识别 | P1 | 已完成 | 已验收 | 自动扫描任务多输入来源（上传/历史文件/引擎/引擎历史） |
| BL-AUTO-008 | 指纹与自动识别 | P1 | 已完成 | 已验收 | 全局资产检索页面 |
| BL-AUTO-009 | 指纹与自动识别 | P1 | 已完成 | 已验收 | 资产检索与 standalone 结果页共用漏洞/CVE 聚类与 vuln 检索 |
| BL-AUTO-010 | 指纹与自动识别 | P1 | 已完成 | 已验收 | 聚类统计展开后分批 40 条异步加载与“更多”按钮 |
| BL-AUTO-011 | 指纹与自动识别 | P1 | 已完成 | 已验收 | 自动扫描任务拆分 HTTP 并发与漏扫线程数配置 |
| BL-AUTO-012 | 指纹与自动识别 | P1 | 已完成 | 已验收 | 自动扫描新增 favicon 与 HTTPS 证书采集、检索与指纹支持 |
| BL-FINGER-002 | 指纹与自动识别 | P2 | 已完成 | 已验收 | 指纹调试页现有指纹模糊选择回填 |
| BL-FINGER-003 | 指纹与自动识别 | P2 | 已完成 | 已验收 | 指纹调试页修复 4xx/5xx 响应展示与旧请求头误判 |
| BL-FINGER-004 | 指纹与自动识别 | P2 | 已完成 | 已验收 | 指纹调试页全库匹配结果列表 |
| BL-BATCH-008 | 任务执行 | P1 | 已完成 | 已验收 | 批量任务全局预算与结果事件治理 |
| BL-TASK-002 | 任务执行 | P1 | 已完成 | 已验收 | 单任务 exploadTask 去留审计与迁移决策 |

### 依赖顺序建议

1. `BL-SCHED-001` → `BL-SCHED-002`
2. `BL-SCHED-002` → `BL-AUTO-002` / `BL-BATCH-007`
3. `BL-AUTO-002` + `BL-BATCH-007` → `BL-SCHED-005`
4. `BL-SCHED-005` → `BL-SCHED-006`
5. `BL-SCHED-006` → `BL-AUTO-003` / `BL-AUTO-004` / `BL-BATCH-008`
6. `BL-SCHED-006` → `BL-SCHED-007`
7. `BL-SCHED-001` → `BL-TASK-002`

## Mode H 共识状态

`BL-SCHED-001`、`BL-SCHED-002`、`BL-AUTO-002`、`BL-BATCH-007`、`BL-SCHED-005`、`BL-SCHED-006` 已完成高风险共识规划（Planner → Architect → Critic），最终 verdict 为 APPROVE。

共识硬约束：

- DB 是业务终态真源，Redis 是运行态/协调真源；旧内存字典仅本机句柄缓存。
- 执行顺序：`BL-SCHED-001` → `BL-SCHED-002` → `BL-AUTO-002` / `BL-BATCH-007` → `BL-SCHED-005` → `BL-SCHED-006`。
- `BL-SCHED-002` 必须启用 DB 连接预算硬闸门，预算超限时 worker 启动失败且 dispatch 被拒。
- `BL-AUTO-002` / `BL-BATCH-007` 必须自带最小 stop bridge。
- `BL-SCHED-006` 必须包含最小 append-only spool；`BL-SCHED-007` 只做增强治理。
- 终态写库必须通过 `dispatch_token/owner` CAS，旧 token、重复消息、迟到 stop 不得覆盖终态。

## 阶段二验收证据摘要

| 维度 | 结果 |
|---|---|
| Django 系统检查 | 0 issues |
| 测试用例 | 后端已维护全量 `app_cybersparker.tests` 回归测试，覆盖 CAS/Lua lease/result dedup/spool/Celery dispatch/async HTTP/stop bridge/budget/历史测绘输入源等；数量以 `manage.py test` 输出为准 |
| 代码文件 | 6 个 service + celery.py + tasks.py，全部存在 |
| Plan 文件 | 11/11 存在 |
| 验收条件 | 73/73 条件有可复查证据（2 个小缺口不阻塞） |

### 审计发现的小缺口

| # | 问题 | 处理 |
|---|---|---|
| 1 | BL-BATCH-007 / BL-SCHED-005 backlog 文件状态滞后 | 已同步 |
| 2 | BL-TASK-002 统计缺"最近使用时间" | 记录到后续开发事项 |
| 3 | PostgreSQL 测试环境不可达 | 基础设施限制，代码层面已验证 |

### 共识硬约束（来自 Mode H）

- DB 是业务终态真源，Redis 是运行态/协调真源；旧内存字典仅本机句柄缓存。
- 终态写库必须通过 `dispatch_token/owner` CAS，旧 token、重复消息、迟到 stop 不得覆盖终态。
- `BL-SCHED-002` 连接预算硬闸门：超限时 worker 启动失败且 dispatch 被拒。

## 架构收口（2026-05-31）

React 巨石拆分 + 侧边栏接管 + 鉴权收口完成：

| 指标 | 旧 | 新 |
|------|---|---|
| 文件 | 2（main.tsx 3788行 + styles.css） | 36（types/api/hooks/components/18pages） |
| 最大文件 | 3788 行 | 369 行（TaskResultsPage） |
| 侧边栏 | Django index.html 模板渲染 | React SidebarLayout（antd） |
| 路由 | data-react-page 手动分发 | react-router-dom BrowserRouter |
| 鉴权 | Django 中间件 302 跳 /login | React AuthGuard → /login?next= 回跳 |
| UI 库 | 自定义 CSS | antd 6.x |
| 代码拆分 | 无 | Vite 自动 chunk |

验证：Vite build ✓ / Django check ✓。当时旧 Django 模板仍保留可回退；2026-06-03 后旧后台模板已删除，仅保留 `login.html`，当前回退路径是 React 壳页兜底。

## 架构收口（2026-06-03）

Nginx 部署 + Vite 构建 HTML 入口完成，前端完全不经过 Django：

| 指标 | 旧 | 新 |
|------|---|---|
| 壳页提供 | Django render → `{% static %}` 加载 | Vite build 产出 index.html，Nginx 直接返回 |
| 路径前缀 | DEV_MODE 条件切换 basename | 统一 `/react-shell/`（Vite base + Nginx alias） |
| 批量删除 | 无 | 9 个列表页全覆盖（插件/字典/字典组/代理/引擎/指纹/自动扫描/批量任务/目录扫描） |
| 时间显示 | `14:30:25` | `14:30`（去秒，不换行） |
| 页面标注 | badge + 说明段落 | 已全部删除 |
| UI 设计 | 暖灰 + 系统字体 | 冷灰 + Plus Jakarta Sans + 5 级阴影 |
| 部署文档 | 无 | `deploy/nginx/react-shell.conf` + `deploy/nginx/README.md` |

验证：Vite build ✓ / Django check ✓ / Nginx 配置语法正确。

## 后续建议

1. 在可连通 PostgreSQL + Redis 的环境中运行完整测试套件，收集运行时证据。
2. 补充 BL-TASK-002 统计中的"最近使用时间"字段。
3. 将 Attact/cmd_input 能力迁移到批量任务（见 `docs/后续开发事项.md`），完成后归档单任务模块。
4. 阶段一运行时端到端测试仍然待补充。

- 2026-05-18：已新增《自动扫描与数据库异常复盘》，记录本轮自动扫描卡进度、结果页空白、writer backlog、NUL 写库、测绘输入 target 为空、外部 TRUNCATE 清库等问题的完整排查与修复证据。

---

## 阶段三：目录扫描 + 资产增强

阶段目标：新建目录扫描模块（独立任务类型），增强资产检索的跳转路径展示，漏洞结果改为追加留存。

**阶段三实现已完成。** 13/13 backlog item 全部通过代码验证（django check 0 issues）。2026-05-23 补齐最后 2 项遗漏：JS 跳转接入（BL-DIRSCAN-005）、uri_path 前端展示（BL-DIRSCAN-010），其余 4 项 backlog 状态同步。

### 新模块

| 模块 | 模块文档 | Backlog |
|------|----------|---------|
| 07-目录扫描 | `docs/modules/07-目录扫描模块.md` | `docs/backlog/07-目录扫描.md` |

### 阶段三 Backlog 总览

| Backlog ID | 模块 | 优先级 | 状态 | 验收 |
|------------|------|--------|------|------|
| BL-DIRSCAN-001 | 目录扫描 | P0 | 已完成 | 已验收 |
| BL-DIRSCAN-002 | 目录扫描 | P0 | 已完成 | 已验收 |
| BL-DIRSCAN-003 | 目录扫描 | P0 | 已完成 | 已验收 |
| BL-DIRSCAN-004 | 目录扫描 | P0 | 已完成 | 已验收 |
| BL-DIRSCAN-005 | 目录扫描 | P1 | 已完成 | 已验收 |
| BL-DIRSCAN-006 | 目录扫描 | P0 | 已完成 | 已验收 |
| BL-DIRSCAN-007 | 目录扫描 | P0 | 已完成 | 已验收 |
| BL-DIRSCAN-008 | 目录扫描 | P0 | 已完成 | 已验收 |
| BL-DIRSCAN-009 | 目录扫描 | P0 | 已完成 | 已验收 |
| BL-DIRSCAN-010 | 目录扫描 | P1 | 已完成 | 已验收 |
| BL-DIRSCAN-011 | 目录扫描 | P0 | 已完成 | 已验收 |
| BL-AUTO-013 | 指纹与自动识别 | P1 | 已完成 | 已验收 |
| BL-RESULT-002 | 结果管理 | P1 | 已完成 | 已验收 |
| BL-TASK-003 | 任务执行 | P2 | 已完成 | 已验收 |
| BL-AUTO-014 | 指纹与自动识别 | P1 | 已完成 | 已验收 | 资产检索页面性能优化（favicon 瘦身 + 并行加载） |
| BL-FINGER-005 | 指纹与自动识别 | P1 | 已完成 | 已验收 | mmh3 favicon hash 支持 |
| BL-FINGER-006 | 指纹与自动识别 | P1 | 已完成 | 已验收 | check_rule != 操作符补充 |
| BL-FINGER-007 | 指纹与自动识别 | P1 | 已完成 | 已验收 | 指纹批量导入命令（28,700 条） |

### 执行顺序建议

1. **BL-DIRSCAN-001**（字典管理）与 **BL-DIRSCAN-009**（4→3 元组迁移 + dir_products 字段）可并行
2. BL-DIRSCAN-009 → **BL-DIRSCAN-002**（任务模型，展开资产时唯一键已是 3 元组）
3. BL-DIRSCAN-002 → **BL-DIRSCAN-003**（随机化引擎）
4. BL-DIRSCAN-003 → **BL-DIRSCAN-004**（Web 扫描阶段）
5. BL-DIRSCAN-004 → **BL-DIRSCAN-005**（JS 跳转共享函数）→ **BL-AUTO-013**（自动扫描复用共享函数）
6. BL-DIRSCAN-004 → **BL-DIRSCAN-006**（漏洞扫描阶段）
7. BL-DIRSCAN-004 → **BL-DIRSCAN-007**（状态机）
8. BL-DIRSCAN-004 + BL-DIRSCAN-009 → **BL-DIRSCAN-008**（dir_products 回写）
9. BL-DIRSCAN-009 + BL-AUTO-013 → **BL-DIRSCAN-010**（uri_path 展示）
10. **BL-RESULT-002**（漏洞结果追加留存，P1）可独立进行
11. **BL-TASK-003**（Attact 迁移，P2）可延后

### 设计文档

- `docs/modules/资产检索与目录扫描设计.md`（两表架构、性能分析、随机化引擎、字典管理、JS 跳转、执行流程）
- `plans/2026-05-23-资产检索与目录扫描设计收口.md`
- `plans/2026-05-23-资产检索JS跳转和uri_path展示.md`
- `plans/2026-05-23-漏洞结果留存设计收口.md`

---

## 阶段四：检索语句导入任务输入源

阶段目标：为自动扫描任务、目录扫描任务、批量漏洞任务新增"从检索语句导入"输入来源，用户用资产检索页面的查询语法直接圈定任务输入范围。

### Backlog 总览

| Backlog ID | 模块 | 优先级 | 状态 | 验收 |
|------------|------|--------|------|------|
| BL-AUTO-016 | 指纹与自动识别 | P1 | 已完成 | 已验收 |
| BL-BATCH-009 | 任务执行 | P1 | 已完成 | 已验收 |
| BL-DIRSCAN-012 | 目录扫描 | P1 | 已完成 | 已验收 |
| BL-DIRSCAN-013 | 目录扫描 | P1 | 已完成 | 已验收 |
| BL-DIRSCAN-014 | 目录扫描 | P1 | 已完成 | 已验收 | 阻断响应记录 + content_length 字段 |

### 执行顺序

1. **BL-AUTO-016**（共享基础：parser 收口 + migration + auto_scan_tasks 改造）
2. **BL-BATCH-009** 和 **BL-DIRSCAN-012** 可并行（依赖 BL-AUTO-016 的共享基础）

### 设计文档

- `plans/2026-05-27-检索语句导入任务输入源.md`（需求澄清、核心决策、实现方案、审计记录）

---

## 阶段六：EXP severity + tags 分类与任务过滤

阶段目标：给 EXP 加 severity（危害等级）和 tags（标签）两个分类字段，批量漏洞任务可以按这两个维度筛选要运行的漏洞模板，自动扫描执行阶段自动跳过非漏洞类探测模板（severity=info）。

### Backlog 总览

| Backlog ID | 模块 | 优先级 | 状态 | 验收 |
|------------|------|--------|------|------|
| BL-BATCH-012 | 任务执行 | P1 | 已完成 | 已验收 |
| BL-PLUGIN-005 | 插件管理 | P0 | 已完成 | 已验收 |
| BL-PLUGIN-006 | 插件管理 | P1 | 已完成 | 已验收 |
| BL-PLUGIN-007 | 插件管理 | P1 | 已完成 | 已验收 |
| BL-BATCH-010 | 任务执行 | P0 | 已完成 | 已验收 |
| BL-BATCH-011 | 任务执行 | P0 | 已完成 | 已验收 |
| BL-AUTO-017 | 指纹与自动识别 | P1 | 已完成 | 已验收 |

### 执行顺序

1. **BL-PLUGIN-005**（模型 + 迁移，无依赖）
2. **BL-PLUGIN-006**（后台页面） + **BL-PLUGIN-007**（导入解析+补数据）可并行，依赖 005
3. **BL-BATCH-010**（任务模型+表单，依赖 005+006）
4. **BL-BATCH-011**（执行引擎，依赖 010）
5. **BL-AUTO-017**（自动扫描过滤，依赖 005+007）

### 设计文档

- `plans/2026-05-27-EXP-severity-tags分类与任务过滤.md`（方案设计、独立审计通过）
- `plans/2026-05-28-batch-pause-resume.md`（批量任务 pause 状态、续跑与 stop 收口）
- `plans/2026-05-29-nuclei-unsupported-template-cleanup.md`（不支持协议模板清理、导入过滤与文档同步）

---

## 阶段五：task_id → 多对多关系表

阶段目标：`auto_scan_indentify_result` 的 `task_id` 从单值字段改为多对多关系表，一个资产被多个任务扫描后，每个任务都能在自己的结果页查到它。

### Backlog 总览

| Backlog ID | 模块 | 优先级 | 状态 | 验收 |
|------------|------|--------|------|------|
| BL-RESULT-003 | 结果管理 | P0 | 已完成 | 已验收 |
| BL-RESULT-004 | 结果管理 | P0 | 已完成 | 已验收 |
| BL-RESULT-005 | 结果管理 | P0 | 已完成 | 已验收 |

### 执行顺序

1. **BL-RESULT-003**（模型 + 迁移）
2. **BL-RESULT-004**（写入路径，依赖 003）
3. **BL-RESULT-005**（查询路径 + 索引 + 验证，依赖 003 + 004）

### 设计文档

- `plans/2026-05-27-资产表task_id改为多对多关系表.md`（方案设计、独立审查通过）
