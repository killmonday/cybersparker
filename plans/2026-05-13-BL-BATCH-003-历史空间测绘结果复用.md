# BL-BATCH-003 历史空间测绘结果复用

## 做什么

新增 `input_type=5`，让用户创建批量任务时复用以往空间测绘引擎查询获得的目标文件。

## 改动点

| # | 文件 | 改动 |
|---|------|------|
| 1 | `app_cybersparker/models.py:126-131` | `inputType_choices` 增加 `(5, "history cyberspace results")` |
| 2 | `app_cybersparker/migrations/` | 新增 migration（alter input_type choices）|
| 3 | `app_cybersparker/views/expload/task_manage/batch_exp_task.py` | 新增 `list_history_engine_results()` 视图函数；`add()`/`edit()`/`resolve_target_source()` 支持 `input_type=5` |
| 4 | `cybersparker/urls.py` | 新增路由 `batch_exploadTask/history_engine_results` |
| 5 | `app_cybersparker/templates/project/expload/task_manage/bath_exp_task_list.html` | 新增 `input_type=5` 的表单区域（历史引擎结果列表） |
| 6 | `app_cybersparker/static/project2/expload/js/batch_Expload_Task.js` | 新增 `input_type=5` 的前端逻辑 |

## 风险

- Low。单文件内改，不涉及 schema 变更（仅 choices 枚举扩展）。
- `input_type=4` 历史数据可能为空，需处理空列表情况。

## 验证

1. Django 系统检查 0 issues ✓（W004/W008 为预存在 TLS 警告）
2. 数据库迁移正常应用 ✓（0006_add_input_type_5_history_cyberspace_results 已应用）
3. URL 路由解析通过 ✓
4. 模型 choices 验证通过 ✓（含 (5, "history cyberspace results")）
5. 视图函数可调用验证通过 ✓（list_history_engine_results, history_engine_results, get_engine_history_files_from_request, collect_targets_from_engine_history_files）

## 实现结果

6 files changed:
- `app_cybersparker/models.py:126-131` → input_type choices 增加 (5)
- `app_cybersparker/migrations/0006_*.py` → migration
- `app_cybersparker/views/expload/task_manage/batch_exp_task.py` → view + 3 helper functions + input_type=5 处理
- `cybersparker/urls.py` → 新增路由
- `app_cybersparker/templates/.../bath_exp_task_list.html` → input_type=5 表单区域
- `app_cybersparker/static/.../batch_Expload_Task.js` → engine results 前端逻辑
