# 2026-05-31 文档与代码同步审计报告

> Mode R 只读审计。仅记录发现，不修改代码。

## 审计范围

- `docs/项目控制台.md`（核心入口）
- `docs/当前实现总览.md`（能力地图）
- `docs/modules/01-插件管理模块.md`
- `docs/modules/08-前后端分离模块.md`
- `docs/backlog/08-前后端分离改造.md`
- `frontend/src/App.tsx`（React 路由真源）
- `frontend/src/pages/`（21 个 .tsx 文件）
- `cybersparker/urls.py`（Django 路由真源）
- `CHANGELOG.md` vs `git log --oneline -10`

---

## 一、已确认代码问题

### 1.1 TaskResultsPage.tsx 是死代码 — 高

- **位置**：`frontend/src/pages/TaskResultsPage.tsx`（16.5K，369 行）
- **现象**：该文件存在于 pages/ 目录，但 App.tsx 中完全未 import，也无任何其他文件引用。且文档多处错误地将其列为任务结果页的组件（见 2.8）
- **影响场景**：GitNexus 索引识别到该组件有 `loadPortOverview`、`loadMorePortOverview`、`openVulnResult` 等调用链，但这些代码永远无法被执行。如果后续有人修改这个文件以为在改线上代码，改动不会生效
- **建议**：确认是否可以删除。如果已由 `TaskResultStandalone.tsx`（包装 GlobalAssetSearchPage）替代，应删除旧文件

### 1.2 AppLayout.tsx 是死代码 — 中

- **位置**：`frontend/src/components/AppLayout.tsx`
- **现象**：该文件存在但从未被任何文件 import。项目实际使用的是 `SidebarLayout.tsx`
- **建议**：删除

### 1.3 useApiList.ts 是死代码 — 中

- **位置**：`frontend/src/hooks/useApiList.ts`
- **现象**：导出 `useApiList` 和 `usePolling` 两个 hook，但项目中无任何文件 import 它们
- **建议**：删除或接入实际使用

### 1.4 Django 路由重复注册 — 高（真实 bug）

- **位置**：`cybersparker/urls.py` 第 183 行 和 第 191 行
- **现象**：同一路由 `path('react-shell/dirscan-tasks', react_dirscan_task_list, name="react_shell_dirscan_tasks")` 被定义了两次，名称参数完全相同
- **影响场景**：Django 启动时可能报 warning 甚至 error（取决于 Django 版本）。路由重复是不应该存在的
- **建议**：删除其中一个

### 1.5 项目控制台阶段十二与实际代码冲突 — 高

- **位置**：`docs/项目控制台.md` 第 52-56 行
- **现象**：文档写"阶段十二：调试工作台线 → 不做 React 迁移，插件调试页/指纹调试页保留旧模板"
- **实际代码**：
  - `App.tsx:156` — `<Route path="exp-debug" element={<ExpDebugPage .../>} />`
  - `App.tsx:157` — `<Route path="fingerprint-debug" element={<FingerprintDebugPage .../>} />`
  - Django 路由有对应的 `react-shell/exp-debug` 和 `react-shell/fingerprint-debug`
  - 路由表第 17-18 行也列出了这两个页面
- **影响场景**：读者会认为调试页还是旧模板，但实际已经是 React 页面了。决策文档跟代码现实完全相反
- **建议**：删除或更新阶段十二的决策描述，改为"已完成 React 迁移"

---

## 二、文档与代码不一致

### 2.1 页面数量矛盾 — 高

- **位置**：`docs/项目控制台.md` 第 7 行
- **现象**：文档写"**16 个页面**已完成 React 迁移"
- **实际**：
  - 项目控制台路由表（第 59-79 行）列出了 **18 个**页面
  - App.tsx 中实际注册了 **20 个** Route 元素
  - pages/ 目录有 **20 个**有效页面文件（+1 个死代码 TaskResultsPage）
- **差异来源**：路由表漏了 `/react-shell/plugins/:id`（PluginDetailPage）和 `/react-shell/ip-detail`（IpPortDetailPage），这两个页面是最近提交加入的
- **建议**：将"16 个"改为实际数字 20，或在路由表补上缺失页面后统一为 20

### 2.2 路由表缺失 PluginDetailPage — 中

- **位置**：`docs/项目控制台.md` React 页面路由总览表（第 59-79 行）
- **现象**：路由表未列出 `/react-shell/plugins/:id`（PluginDetailPage）
- **实际代码**：`App.tsx:141` — `<Route path="plugins/:id" element={<PluginDetailPage />} />`
- **影响场景**：开发者看文档找不到插件详情页在哪里
- **建议**：在路由表中增加"插件详情 | `/react-shell/plugins/:id` | 是"

### 2.3 路由表缺失 IpPortDetailPage — 中

- **位置**：`docs/项目控制台.md` React 页面路由总览表
- **现象**：路由表未列出 `/react-shell/ip-detail`（IpPortDetailPage）
- **实际代码**：`App.tsx:131` — `<Route path="ip-detail" element={<IpPortDetailPage />} />`
- **补充**：此页面无专用 Django 壳页路由，依赖 `react-shell/<path:subpath>` catch-all（`cybersparker/urls.py:227`）
- **影响场景**：同 IP 端口详情页是最近新增功能（commit `0926b3b`），文档未跟上
- **建议**：在路由表中增加"同IP端口详情 | `/react-shell/ip-detail` | —"

### 2.4 当前实现总览页面表缺失 3 个页面 — 中

- **位置**：`docs/当前实现总览.md` 第 170-189 行
- **现象**：页面表列出 18 个页面，缺失：
  - `PluginDetailPage`（`pages/PluginDetailPage.tsx`）
  - `IpPortDetailPage`（`pages/IpPortDetailPage.tsx`）
  - `TaskResultStandalone`（`pages/TaskResultStandalone.tsx`）
- **实际代码**：App.tsx 中三个页面均正常注册
- **建议**：补全页面表

### 2.5 CHANGELOG.md 近 170 行无日期标题 — 高

- **位置**：`CHANGELOG.md` 第 1-170 行
- **现象**：最后一个有日期的标题是 `## 2026-05-23`（第 173 行）。从第 1 行到第 170 行的所有最新变更（对应 2026-05-24 ~ 2026-05-31 的约 30 次 git commit）没有任何日期分组。第 171 行只有一个无日期的 `# CHANGE` 标题
- **影响场景**：任何想按时间线追溯 2026-05-24~05-31 变更的人都只能靠 git log 推断。CHANGELOG 丧失了时间线导航的核心功能
- **建议**：补齐日期标题（至少 `## 2026-05-31`、`## 2026-05-28` 等）

### 2.6 后续开发事项.md 多项状态严重滞后 — 高

- **位置**：`docs/后续开发事项.md`
- **现象**：
  - 第 26 行：Attact/cmd_input 能力迁移标记为"**未开始**"，但代码（`batch_EXPTask.task_type` / `cmd_input` 字段）和 CHANGELOG 均已确认完成
  - 第 38 行：写"**10 个页面** React 迁移完成"，实际已 20 个
  - 第 38 行：写"调试工作台线...评估后决定不做迁移"，紧接着第 40-45 行又写"两个调试页 React 化实际已完成"，同文件自相矛盾
- **影响场景**：开发者看后续开发事项会以为 Attact 迁移还没做、只完成了 10 个页面、调试页没迁移——三个判断全错
- **建议**：全面更新后续开发事项.md

### 2.7 模块文档 08-前后端分离 严重过时 — 高

- **位置**：`docs/modules/08-前后端分离模块.md`
- **现象**：整个文档还停留在"二期评估"阶段的语言：
  - 说"不做"调试工作台线 → 实际已完成
  - 说"不做"任务页、资产检索页核心交互 → 实际已完成
  - 说"二期评估结论"建议调试工作台线放最后 → 实际已完成
  - "首批建议接口"只列了 8 个 → 实际 `/api/v1` 已有 40+ 个接口
  - React 路由表已完全缺失（未列出任何实际 React 路由）
  - 页面风险分层（第 99-121 行）仍将调试页标为"第三层：高风险页面"、导出模块标为未做
- **影响场景**：新成员看模块文档会严重误解当前架构状态，以为大量页面还是旧模板
- **建议**：全面更新此模块文档，反映 2026-05-31 实际架构收口状态

### 2.8 模块文档 07-目录扫描模型标为"待实现" — 高

- **位置**：`docs/modules/07-目录扫描模块.md` 第 11-14 行
- **现象**：4 个 DirScan 模型全部标为"待实现"（DirScanDictGroup / DirScanDict / DirScanTask / auto_scan_directory_result）
- **实际代码**：全部 4 个模型已实现在 `app_cybersparker/models.py`（行 386/395/405/434），且有完整的 API、Celery worker、React 页面
- **建议**：全部改为"已实现"

### 2.9 模块文档 00-模块总览 模块 08 标为"规划中" — 中

- **位置**：`docs/modules/00-模块总览.md` 第 13 行
- **现象**：模块 08（前后端分离）状态列显示"规划中"
- **实际**：已完成 20 个 React 页面迁移，全部 backlog（50 项）已验收，架构收口于 2026-05-31
- **建议**：改为"已实现"

### 2.10 验证证据摘要中的数字已过时 — 中

- **位置**：`docs/项目控制台.md` 第 161 行
- **现象**：
  - "URL 路由解析 | 87 patterns 全部通过" → 实际 `cybersparker/urls.py` 有约 80+ 个 `path()` 调用（加上 react-shell catch-all），不是 87
  - "模型导入 | 11/11 模型可导入" → 实际 models.py 有 20+ 个模型
- **建议**：更新为当前实际数字

### 2.11 Celery 队列数量记录错误 — 中

- **位置**：`docs/当前实现总览.md` 第 112 行
- **现象**：文档写"标准队列：`auto_scan` / `batch_scan` / `result_writer` / `maintenance`"（4 个）
- **实际**：`cybersparker/settings.py` 定义并 `celery.py` 路由了 6 个队列：`auto_scan` / `batch_scan` / `batch_scan_gevent` / `result_writer` / `maintenance` / `dir_scan`
- **缺失**：`batch_scan_gevent`（gevent worker 专用）和 `dir_scan`（目录扫描 worker）
- **建议**：补全队列列表

### 2.12 模块文档 07 列了不存在的路由 — 中

- **位置**：`docs/modules/07-目录扫描模块.md` 第 78 行
- **现象**：接口表列出 `GET /dirscan/task/<id>/result`（任务结果查看），但 `cybersparker/urls.py` 中无此路由
- **建议**：删除或标注为"预留"

### 2.13 当前实现总览模型行号大范围偏移 — 低

- **位置**：`docs/当前实现总览.md` 多处
- **现象**：由于 models.py 后续新增了 Tag、AssetTaskRelation、DirScan 系列、CeyeConfig、ExportTask 等模型，旧模型的引用行号已偏移 1~272 行不等（如 `ProxySetting:247` 实际在 519 行）
- **建议**：更新所有行号引用

### 2.14 Auth 中间件描述不精确 — 低

- **位置**：`docs/当前实现总览.md` 第 165 行
- **现象**：写"AuthMiddleware + BasicAuthMiddleware 放行 `/react-shell/*`、`/api/v1/*`、`/static/*`"
- **实际**：`/api/v1/*` 不是"放行"——未登录请求返回 401 JSON（不重定向），只有 `/api/v1/auth/session` 真正放行
- **建议**：改为"API 路径未登录返回 401 JSON，非 API 路径重定向到 /login"

### 2.15 当前实现总览缺失 8 个模型 — 中

- **位置**：`docs/当前实现总览.md`
- **现象**：以下模型在 models.py 中存在但未在文档中列出：
  - `Tag`（行 25）、`AssetTaskRelation`（行 313）
  - `DirScanDictGroup`（行 386）/ `DirScanDict`（行 395）/ `auto_scan_directory_result`（行 405）/ `DirScanTask`（行 434）
  - `CeyeConfig`（行 546）/ `ExportTask`（行 559）
- **建议**：补全

### 2.16 模块文档 01-插件管理 未提及 React 页面 — 低

- **位置**：`docs/modules/01-插件管理模块.md`
- **现象**：只列了旧 Django 路由（`/expload/list`、`/expload/add` 等），完全没有提及：
  - React 列表页 `PluginListPage` → `/react-shell/plugins`
  - React 详情页 `PluginDetailPage` → `/react-shell/plugins/:id`
  - API 路由 `/api/v1/plugins`、`/api/v1/plugins/<uid>`
- **影响场景**：文档与实际入口脱节，新开发者不知道去哪改插件相关页面
- **建议**：在接口部分增加 React 路由和 API 路由说明

### 2.7 CHANGELOG 与项目控制台路由表不同步 — 中

- **位置**：`CHANGELOG.md` vs `docs/项目控制台.md`
- **现象**：CHANGELOG 最新条目已记录了 IpPortDetailPage 和 PluginDetailPage 等功能，但项目控制台路由表未同步更新
- **影响场景**：两个权威文档对"当前有哪些页面"说法不一致
- **建议**：同步更新

### 2.8 多处文档引用错误的任务结果组件名 — 中

- **位置**：
  - `docs/当前实现总览.md` 第 180 行：路由映射到 `pages/TaskResultsPage.tsx`
  - `docs/项目启动文档.md` 第 243 行：组件名为 `TaskResultsPage`
  - `docs/backlog/08-前后端分离改造.md` 第 365 行：提及 `TaskResultsPage`
- **现象**：三处文档都说任务结果路由用 `TaskResultsPage`，但实际 App.tsx 用的是 `TaskResultStandalone`（13 行包装组件，内嵌 GlobalAssetSearchPage）
- **影响场景**：文档间错误传播——一个写错，其他地方复制粘贴跟着错
- **建议**：全部更正为 `TaskResultStandalone`

### 2.9 架构收口文件数统计有误 — 低

- **位置**：`docs/项目控制台.md` 第 268 行
- **现象**：文档声称 `36（types/api/hooks/components/18pages）`，但实际文件数：
  - types: 8 + api: 2 + hooks: 1（含 1 死代码）+ components: 3（含 1 死代码）+ pages: 20（有效）= **34**（不含死代码）或 **37**（含死代码）
  - 无论怎么数都不是 36
- **建议**：重新统计并更新

---

## 三、代码品位问题

### 3.1 autobuild.py 误放 middleware 目录 — 低

- **位置**：`app_cybersparker/middleware/autobuild.py`
- **现象**：Nuitka 编译脚本，不是 Django 中间件，却放在 middleware/ 与 auth.py/basic.py 同级。项目中散布 6 个 `autobuild.py`
- **建议**：清理冗余，middleware/ 下的移到正确位置

### 3.2 Vite 代理未覆盖部分旧 Django 路径 — 低

- **位置**：`frontend/vite.config.ts`
- **现象**：`/Dashboards`、`/export`、`/TaskResult`、`/identify_exp_result`、`/fingerPrint_debug`、`/batch_exploadTaskResult`、`/exp_detail` 未在 Vite proxy 配置
- **建议**：确认是否需要补充

---

## 四、测试滞后 / 测试夹具过期

### 3.1 新增 API 无测试覆盖 — 中

- **位置**：`cybersparker/urls.py:124` — `GET /api/v1/ip-detail`
- **现象**：这是最近提交 `0926b3b` 新增的 API，但 `app_cybersparker/tests.py` 未被该提交修改
- **影响场景**：新 API 无回归测试保护
- **建议**：补充 ip-detail API 的测试用例

### 3.2 React 新页面无前端测试 — 低

- **位置**：`frontend/src/pages/IpPortDetailPage.tsx`、`PluginDetailPage.tsx`
- **现象**：前端工程未见测试文件
- **影响场景**：新页面无自动化验证
- **建议**：评估是否为关键路径页面需要前端测试

---

## 四、风险点但证据不足

### 4.1 IpPortDetailPage 无专用 Django 壳页路由

- **位置**：`cybersparker/urls.py`
- **现象**：`/react-shell/ip-detail` 没有对应的 `path('react-shell/ip-detail', ...)` 注册，完全依赖 catch-all `react-shell/<path:subpath>`（第 227 行）
- **潜在风险**：如果 catch-all 行为改变或被移除，此页面直接 404。但按照模块文档 08 的设计要求"每个 React 页面都必须先有一个真实 Django 路由作为壳页入口"
- **证据不足**：不确定这是有意为之（因为此页面是新标签页打开，不需要侧边栏壳）还是遗漏
- **建议**：确认设计意图。如果是有意的，在模块文档中记录豁免理由

### 4.2 其他模块文档的 React 迁移描述可能也过时

- **位置**：`docs/modules/02` 到 `07`
- **现象**：模块 01 和 08 都有 React 信息缺失，其他模块文档（任务执行、指纹、结果管理等）的"接口"部分可能同样只列了旧 Django 路由而未列 `/api/v1` 对应路由
- **证据不足**：本次审计未逐一比对所有模块文档
- **建议**：后续对模块 02-07 做一次专项审计

---

## 五、汇总表

| # | 分类 | 等级 | 描述 |
|---|------|------|------|
| 1 | 代码问题 | 高 | `TaskResultsPage.tsx` 死代码（356 行），未被任何文件引用 |
| 2 | 代码问题 | 中 | `AppLayout.tsx` 死代码，从未被 import |
| 3 | 代码问题 | 中 | `useApiList.ts` 死代码（导出 useApiList/usePolling），从未被 import |
| 4 | 文档不一致 | 高 | 阶段十二说"不做 React 迁移"，实际已完成 |
| 5 | 文档不一致 | 高 | "16 个页面"描述错误，实际 20 个 |
| 6 | 文档不一致 | 高 | 模块文档 08-前后端分离严重过时 |
| 7 | 文档不一致 | 中 | 路由表缺失 PluginDetailPage |
| 8 | 文档不一致 | 中 | 路由表缺失 IpPortDetailPage |
| 9 | 文档不一致 | 中 | 当前实现总览页面表缺失 3 个页面 |
| 10 | 文档不一致 | 中 | 3 处文档引用错误组件名（TaskResultsPage → 应为 TaskResultStandalone） |
| 11 | 文档不一致 | 中 | CHANGELOG 与 项目控制台 路由表不同步 |
| 12 | 文档不一致 | 低 | 架构收口文件数"36"与实际不符 |
| 13 | 文档不一致 | 低 | 模块文档 01 未提及 React 页面和 API 路由 |
| 14 | 测试滞后 | 中 | 新增 API `/api/v1/ip-detail` 无测试 |
| 15 | 测试滞后 | 低 | React 新页面无前端测试 |
| 16 | 风险点 | 低 | IpPortDetailPage 无专用 Django 壳页路由 |
| 17 | 风险点 | 低 | 模块 02-07 的 React 描述可能也过时 |

---

## 建议下一步

1. **P0 修复**：更新阶段十二决策描述 + 项目控制台"16 个页面"数字 + 路由表补全
2. **P1 修复**：更新模块文档 08（前后端分离）到 2026-05-31 实际状态
3. **P2 清理**：删除或归档 `TaskResultsPage.tsx` 死代码
4. **P2 补充**：补充 `api/v1/ip-detail` 测试
5. **后续审计**：对模块文档 02-07 做专项 React/API 路由同步审计
