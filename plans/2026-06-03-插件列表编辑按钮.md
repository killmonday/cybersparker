# 插件列表添加编辑按钮

- 日期：2026-06-03
- 模块：插件管理（React）
- 状态：已完成

## 问题

插件列表只读，想编辑插件得记下 ID 再手动打开调试页，流程割裂。

## 方案

复用插件调试页，不做新编辑页。调试页已有完整的代码编辑器和保存闭环。

1. **PluginListPage**：操作列加"编辑"按钮，跳 `/react-shell/exp-debug?plugin_id=<id>`
2. **ExpDebugPage**：读 `plugin_id` URL 参数，自动加载对应插件数据到编辑器

## 验证

- Vite build ✓
- Django check ✓
