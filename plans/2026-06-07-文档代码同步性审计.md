# 文档与代码同步性审计报告

> 日期：2026-06-07 | 审计范围：`docs/` 全部文档 vs 实际代码 + backlog 状态一致性
> 方法：Mode R（只读审计），3 个并行子 agent + 直接检查，不修改任何内容

---

## 审计结论摘要

发现 **28 条差异**：致命 0 / 高 7 / 中 17 / 低 4。

核心问题：项目已演进到前后端分离完成（20 React 页面、100 条 `/api/v1` 路由、Celery/Redis 基础设施），但多数模块文档仍停留在阶段一 Django 视图模式——接口路径、数据模型行号、架构描述均未同步。Backlog 方面，6 个已验收项的"状态"字段未更新（仍为"未开始"）。

---

## 致命（0）

无。

---

## 高（7 条）

### SYNC-01：`docs/设计总览.md` 缺失

**场景**：`project-control-plane` skill 定义该文件为"项目架构入口文档"，引用 8 次作为必维护文档。但从未创建。所有已有的设计决策（Celery 迁移、结果事件缓冲、task_id 多对多、前后端分离架构等）都没有记录到这个本应作为"架构入口"的文档中。

**位置**：`.claude/skills/project-control-plane/SKILL.md`（引用源），文件系统中不存在

**修复方向**：创建 `docs/设计总览.md`，至少覆盖技术栈、模块地图、核心业务流、关键设计决策记录。

---

### SYNC-02：antd 版本号：文档说 5.x，代码用 6.x

**场景**：`docs/前端项目规范.md:17` 和 `docs/modules/08-前后端分离模块.md:15` 声明 `antd 5.x`。实际 `frontend/package.json` 是 `"antd": "^6.4.3"`。antd 5.x → 6.x 是主版本升级，API 有多处不兼容。AI Agent 按文档生成代码会直接编译报错。

**修复方向**：将两份文档的 `antd 5.x` 改为 `antd 6.x`。

---

### SYNC-03：6 个模块文档的接口路由全是已删除的旧 Django URL

**场景**：模块文档 01、02、03、04、05、07 的"接口"章节写的全是 `/expload/list`、`/fingerprint_List`、`/result_List`、`/Identify_task/<uid>/result`、`/dict/group/list` 等旧 Django URL。这些路由在 2026-06-03 已全部删除（80+ 条），实际接口已统一到 `/api/v1/*`。

**涉及文档**：
- `docs/modules/01-插件管理模块.md:34-44` — 11 条旧 URL
- `docs/modules/02-任务执行模块.md` — 引用旧 URL 模式
- `docs/modules/03-指纹与自动识别模块.md:26-32` — 7 条旧 URL
- `docs/modules/04-结果管理模块.md:24-57` — ~15 条旧 URL
- `docs/modules/05-代理与运行时配置模块.md` — 引用旧 URL 模式
- `docs/modules/07-目录扫描模块.md:107-112` — 4 条旧 URL

**修复方向**：每个模块文档的"接口"章节增加 `/api/v1/*` 路由表（模块 06、08 末尾的 React 入口部分可作为参考模板）。

---

### SYNC-04：4 个文档引用已删除的 `single_task_executor.py`

**场景**：该文件在 2026-06-07 提交中作为 dead code 删除（CHANGELOG 已记录），但以下文档仍将其列为"关键代码"：

- `docs/内部调用链参考.md:31,50` — 调用链图将其列为关键参与者，标注 3 个行号引用
- `docs/backlog/02-任务执行.md:12,472` — 两个 backlog item 的"关联代码"字段
- `docs/需求规格说明书初稿.md:22` — FR-2 描述
- `docs/backlog/05-代理与运行时配置.md:175` — 关联代码（但同时在第 185/214 行记录了删除

**修复方向**：清理文档中所有对已删除文件的引用；调用链图用 Celery worker task 路径替代。

---

### SYNC-05：模块 04（结果管理）严重落后于当前架构

**场景**：结果管理是架构演进最大的区域——引入 result_event_service（事件缓冲）、Redis Stream + spool 双通道、Celery result_writer、终态 CAS 保护等。但模块 04 文档完全没有这些描述，且接口全是旧 Django URL。数据模型仍以已弃用的 `EXPTask_result` 为中心描述。

**位置**：`docs/modules/04-结果管理模块.md`

**修复方向**：重写数据流描述（补充 result_event_service / Redis Stream / spool / CAS），更新接口为 `/api/v1/*`，补上 AssetTaskRelation 多对多关系。

---

### SYNC-06：6 个 backlog item 状态为"未开始"但验收为"已验收"，代码已实际实现

**场景**：以下 6 项在阶段六作为批量实现完成，代码已写且验收通过，但"状态"字段未被批量更新为"已完成"：

| Backlog ID | 文件 | 代码证据 |
|-----------|------|---------|
| BL-PLUGIN-005 | `docs/backlog/01-插件管理.md` | EXP.severity (models.py:41), Tag 模型 (:25), tags M2M (:80), migration 0043 |
| BL-PLUGIN-006 | `docs/backlog/01-插件管理.md` | plugin_manage.py 含 severity_filter/tag_filter/all_tags |
| BL-BATCH-010 | `docs/backlog/02-任务执行.md` | batch_EXPTask 含 exp_select_mode/severity_filter/tag_filter |
| BL-BATCH-011 | `docs/backlog/02-任务执行.md` | batch_task_executor.py 含 resolve_exp_filter |
| BL-AUTO-017 | `docs/backlog/03-指纹与自动识别.md` | auto_exp_task.py 含 `severity=="info": continue` 过滤 |
| BL-FE-103 | `docs/backlog/08-前后端分离改造.md` | 结果详情附属能力已 React 化 |

**修复方向**：将这 6 项的"状态"字段批量更新为"已完成"，状态记录追加完成日期。

---

### SYNC-07：模块 03 引用不存在的模型字段 `area` 和 `area_name_zh`

**场景**：`docs/modules/03-指纹与自动识别模块.md:199` 描述了一个已知问题——"`auto_scan_indentify_result` 的 `country`/`area`/`area_name_zh` 字段为 `max_length=6`"。但 `models.py` 中 `area` 和 `area_name_zh` 字段根本不存在，实际有的是 `province`、`city`、`isp`。且 `country` 实际的 `max_length=64` 而非文档声称的 6。这是"幽灵字段"——描述了一个针对已不存在字段的已知问题。

**位置**：`docs/modules/03-指纹与自动识别模块.md:199`

**修复方向**：删除这段过时的已知问题描述，或改为针对 `province`/`city`/`isp` 的实际字段描述。

---

## 中（17 条）

### SYNC-08：模块 05 CeyeConfig 仍标注为"待新建"

**场景**：`docs/modules/05-代理与运行时配置.md:75-78` 写"模型：`CeyeConfig`（待新建）"、"ceye_config 视图，待新建"。实际模型（models.py:543）、视图（ceye_config.py）、React 页面（CeyeConfigPage.tsx）早已全部实现并上线。

**修复方向**：删除"待新建"标注，补充实际的模型位置和接口信息。

---

### SYNC-09：模块 01 未提及 severity/tags 字段及 Tag 模型

**场景**：阶段六（BL-PLUGIN-005）新增的 severity（危害等级）、tags（标签 M2M）、Tag 模型（models.py:25），在模块 01 文档中完全没有记录。

**位置**：`docs/modules/01-插件管理模块.md`（字段表 + 数据模型表）

**修复方向**：EXP 字段表增加 severity/tags；数据模型表增加 Tag。

---

### SYNC-10：模块 02 说旧 Django 模板"保留可用"，与事实矛盾

**场景**：`docs/modules/02-任务执行模块.md:267` 写"旧 Django 模板 `/Identify_task/list`、`/batch_expload_Task/list`、`/dirscan/task/list` 保留可用"。但 2026-06-03 已删除 27 个旧模板（仅保留 login.html）。用户访问这些 URL 会 404。

**修复方向**：删除此段描述，或改为"已删除，由 React 页面替代"。

---

### SYNC-11：模块 08 说"旧 Django 模板全部保留"，与事实矛盾

**场景**：`docs/modules/08-前后端分离模块.md:141` 写"旧 Django 模板全部保留，可通过旧 URL 直接访问作为回退"。但项目控制台说"仅保留 login.html，其余 27 个已删除"。模块 08 更新于 2026-06-01，晚于模板删除操作（06-03）。

**修复方向**：更新为"旧模板已删除，回退路径改为 catch-all 兜底到 React 壳页"。

---

### SYNC-12：模型行号普遍错误

**场景**：所有模块文档中引用的模型行号与实际 `models.py` 偏差从 1 行到 270 行：

| 模型 | 文档行号 | 实际行号 | 偏差 |
|------|---------|---------|------|
| EXP | 24/32 | 31 | -7/-1 |
| fingerPrint | 5/6 | 6 | -1/0 |
| exp_relate_fingerprint | 165/217 | 216 | -51/+1 |
| auto_scan_tasks | 172/224 | 223 | -51/+1 |
| auto_scan_exp_result | 198/299 | 297 | -99/+2 |
| auto_scan_indentify_result | 207/330 | 328 | -121/+2 |
| CyberspaceEngineSetting | 229/501 | 499 | -270/+2 |
| ProxySetting | 247/519 | 516 | -269/+3 |

根因：新增 Tag、AssetTaskRelation、目录扫描等模型把后续模型往下推了几十到几百行。

**修复方向**：批量更新所有模块文档中的模型行号引用。

---

### SYNC-13：模块 03 未提及 AssetTaskRelation 模型

**场景**：`AssetTaskRelation`（models.py:311）是阶段五新增的多对多关系表，关联任务与资产。但模块 03 的"数据模型"表没有列出它。

**位置**：`docs/modules/03-指纹与自动识别模块.md:8-15`

**修复方向**：在数据模型表中增加 AssetTaskRelation。

---

### SYNC-14：模块 06 迁移文件数严重过时（5 个 vs 实际 49 个）

**场景**：`docs/modules/06-运行部署与可观测性模块.md:59-68` 的"迁移历史"表仅列出 5 个迁移（0001-0005），实际 `migrations/` 下 49 个文件。task_type、severity/tags、AssetTaskRelation、目录扫描模型等后续几乎所有迁移都缺失。

**修复方向**：更新迁移历史为 49 个迁移的摘要列表。

---

### SYNC-15：模块 00 总览的关键共享资源过时

**场景**：`docs/modules/00-模块总览.md:78-81` 列出 `conf.proxies` 和进程字典作为关键共享资源。但 `conf.proxies` 已在 2026-06-07 废弃（改为 thread-local sentinel），且完全没有提 Redis、Celery 队列、spool 文件、资源 lease 等实际关键共享资源。

**修复方向**：标记 conf.proxies 废弃，增加 Redis/Celery/spool/lease。

---

### SYNC-16：需求规格说明书初稿严重过时

**场景**：`docs/需求规格说明书初稿.md` 停留在阶段一水平——旧 URL、引用已删除文件、无 Celery/Redis/React/目录扫描/ceye/导出/仪表盘等描述。

**修复方向**：标记为"历史参考，以 docs/当前实现总览.md 为当前状态"。

---

### SYNC-17：内部调用链参考引用过期代码路径

**场景**：`docs/内部调用链参考.md` 的 4 个 Mermaid 时序图全部引用旧代码路径和已删除文件，无 Celery worker task、result_event_service、目录扫描执行链。

**修复方向**：更新或标记为历史参考。

---

### SYNC-18：项目控制台"验证证据摘要"自相矛盾

**场景**：同一文档内：
- 第 169 行："模板文件：14/14 关键模板存在"（阶段一审计结果）
- 第 100 行："旧 Django 模板：仅保留 login.html，其余 27 个已删除"

14 个模板的声称来自阶段一审计，在 06-03 模板已删除后不再准确。

**修复方向**：删除或更新过时的"模板文件"条目。

---

### SYNC-19：项目控制台最后更新日期滞后

**场景**：控制台标注"最后更新：2026-06-03"，但 06-06 和 06-07 有多项重要变更（代理 thread-local 改造、4 模块 CSRF 修复、pause 残留修复、11 项审计修复等）。

**修复方向**：更新日期到 2026-06-07。

---

### SYNC-20：3 个 backlog item 的迁移编号与实际 migration 文件不一致

**场景**：
- **BL-PLUGIN-005**：计划说迁移分两步 0043+0044，实际 migration 0043 将 Tag/severity/M2M 全合在一文件
- **BL-BATCH-010**：计划说迁移 0045，实际为 0044；真正 0045 被 BL-BATCH-012 的 pause/resume 占用
- 缺少迁移编号协调

**修复方向**：更新 backlog 文件中的迁移编号描述与实际的 migration 文件对齐。

---

### SYNC-21：3 个 API 视图文件无独立 backlog 条目

**场景**：以下文件已在代码中存在并运行，但未被任何 backlog 显式引用：
- `app_cybersparker/views/expload/dirscan_task_api.py`
- `app_cybersparker/views/expload/task_manage/auto_scan_task_api.py`
- `app_cybersparker/views/expload/task_manage/batch_exp_task_api.py`

这些作为前后端分离改造的附属产物创建，但缺少显式 ID 映射。

**修复方向**：在相关 backlog 文件中标注这些文件为 BL-FE 系列附属产出。

---

### SYNC-22：模块 05 声称 ProxySetting 仅支持 http，实际也支持 socks5

**场景**：`docs/modules/05-代理与运行时配置模块.md:16` 写"当前仅支持 http 协议（`protocol_choices` 仅有 `(1, "http")`）"。实际 models.py:517-522 中 protocol_choices 含有 `(1, "http")` 和 `(4, "socks5")` 两项。

**修复方向**：更新协议约束描述为同时支持 http 和 socks5。

---

### SYNC-23：Backlog 重复 ID：BL-FINGER-005 被使用两次

**场景**：`docs/backlog/03-指纹与自动识别.md` 中：
- 第 797 行：BL-FINGER-005 — 指纹调试页 favicon/cert/JS 跳转增强
- 第 1006 行：BL-FINGER-005 — mmh3 favicon hash 支持

两个不同的功能用了同一个 ID。第二个应为独立的 ID（如 BL-FINGER-008）。

**修复方向**：重命名第二个 BL-FINGER-005 为唯一 ID。

---

### SYNC-24：模块 01 和 03 内部章节引用失效

**场景**：
- `docs/modules/01-插件管理模块.md:31` — "接口详见上方 §1.1 和 §1.2 的接口表格"，但文档内不存在这些章节
- `docs/modules/03-指纹与自动识别模块.md:156` — "接口详见上方 §3.1 和 §3.2 的接口表格"，同样不存在

**修复方向**：删除失效的章节引用或补充对应内容。

---

## 低（4 条）

### SYNC-25：前端文档遗漏 HistoryFilePicker 组件 + 引用不存在的文件

**场景**：
- `HistoryFilePicker.tsx`（5.8K，被 BatchTaskListPage/DirscanTaskListPage 引用）在两份文档中均未列出
- `AppLayout.tsx` 和 `useApiList.ts` 在模块 08 文档中列出但实际磁盘上不存在（前者已删除，后者从未创建）
- 模块 08 的壳页文件 `react_shell_standalone.html`/`react_shell.html` 描述已不适用当前 Nginx 架构

**修复方向**：增补 HistoryFilePicker，删除 AppLayout/useApiList 和过时壳页描述。

---

### SYNC-26：交接文档 `docs/交接-2026-05-31-阶段十三.md` 状态过时

**场景**：写于 2026-05-31，描述 6 个"待完成"页面。但实际上这 6 个页面已全部完成并验收。作为历史文档没问题，但"待完成"标记可能误导不了解当前状态的人。

**修复方向**：在文档顶部加说明"截至 2026-06-07 以下全部完成，本文档仅作历史参考"。

---

### SYNC-27：模块 08 的 API 契约表未列 12 条 batch-delete 端点

**场景**：`cybersparker/urls.py` 中有 12 条 `batch-delete` 路由，但模块 08 的 API 契约表一条都没列出。

**修复方向**：补充 batch-delete 端点。

---

### SYNC-28：`docs/modules/00-资产检索与目录扫描设计.md` 编号 00 与模块总览冲突

**场景**：该文件是专项设计文档（33.2K），但放在 `docs/modules/` 下且用 `00-` 前缀，与 `00-模块总览.md` 编号冲突。建议移到 `docs/` 或改为不带编号前缀的文件名。

**修复方向**：改名或移出 modules/ 目录。

---

## 无问题的区域

- **20 个 React 页面路由**：控制台列表 vs App.tsx 实际路由 → 完全一致
- **前端页面文件**：20 个 .tsx 全部存在
- **侧边栏菜单 vs 路由**：17 个菜单项全部对应实际 Route
- **数据模型存在性**：文档引用的所有模型在 models.py 中全部存在
- **CHANGELOG**：已更新至 2026-06-07
- **login.html**：唯一保留的模板，与控制台声称一致
- **核心代码文件**：当前实现总览列出的所有文件全部存在
- **阶段四、五、六 code-to-backlog 映射**：BL-AUTO-016、BL-BATCH-009、BL-DIRSCAN-012、BL-RESULT-003/004/005、BL-BATCH-012 等代码均与 backlog 一致

---

## 问题分布热力图

| 文档 | 高 | 中 | 低 | 总 |
|------|---|---|---|---|
| docs/设计总览.md | 1 | 0 | 0 | 1（缺失） |
| docs/modules/04-结果管理模块.md | 1 | 0 | 0 | 1 |
| docs/modules/01-插件管理模块.md | 1 | 2 | 0 | 3 |
| docs/modules/02-任务执行模块.md | 1 | 1 | 0 | 2 |
| docs/modules/03-指纹与自动识别模块.md | 1 | 2 | 0 | 3 |
| docs/modules/05-代理与运行时配置模块.md | 1 | 2 | 0 | 3 |
| docs/modules/08-前后端分离模块.md | 0 | 2 | 2 | 4 |
| docs/modules/00-模块总览.md | 0 | 1 | 1 | 2 |
| docs/modules/06-运行部署与可观测性.md | 0 | 1 | 0 | 1 |
| docs/modules/07-目录扫描模块.md | 1 | 0 | 0 | 1 |
| docs/前端项目规范.md | 1 | 0 | 1 | 2 |
| docs/项目控制台.md | 0 | 2 | 0 | 2 |
| docs/需求规格说明书初稿.md | 0 | 1 | 0 | 1 |
| docs/内部调用链参考.md | 0 | 1 | 0 | 1 |
| docs/backlog/01-插件管理.md | 1 | 1 | 0 | 2 |
| docs/backlog/02-任务执行.md | 1 | 1 | 0 | 2 |
| docs/backlog/03-指纹与自动识别.md | 1 | 1 | 1 | 3 |
| docs/backlog/08-前后端分离改造.md | 1 | 0 | 0 | 1 |
| docs/交接-2026-05-31-阶段十三.md | 0 | 0 | 1 | 1 |

---

## 建议修复优先级

1. **P0（立即可修，2 条）**：SYNC-02（antd 版本号，2 文件修改）— 直接影响 AI Agent 代码生成正确性
2. **P1（本周内，8 条）**：SYNC-01（创建设计总览）+ SYNC-03（更新模块接口路由）+ SYNC-05（重写模块 04）+ SYNC-06（backlog 状态批量更新）+ SYNC-04（清理 dead file 引用）+ SYNC-07（删除幽灵字段）+ SYNC-10/11（模板保留声明修正）
3. **P2（近期，13 条）**：SYNC-12（模型行号）+ SYNC-14（迁移表）+ SYNC-20（迁移编号）+ SYNC-08/09/13/15/22（模块文档字段补充）+ SYNC-16/17（更新或标记过时参考文档）+ SYNC-18/19（项目控制台更新）+ SYNC-24（章节引用修正）
4. **P3（随缘，5 条）**：SYNC-23（重复 ID）+ SYNC-25（组件列表）+ SYNC-26（交接文档）+ SYNC-27（batch-delete 端点）+ SYNC-28（00 编号冲突）

---

## 根因分析

1. **文档更新滞后于代码迭代**：所有 28 条发现最终都可归结为这一根因。项目在阶段二到十三之间进行了大量架构变更，但模块文档作为"设计阶段的产物"没有同步更新。
2. **文档"双写"问题**：项目控制台（docs/项目控制台.md）准确描述了当前架构状态（最新 2026-06-03），但模块文档（docs/modules/）仍保留着阶段一的旧接口描述。
3. **SKILL.md 引用从未创建的文档**：`设计总览.md` 被 skill 定义为关键维护目标但从未创建。
4. **Backlog 状态批量更新遗漏**：阶段六批量实现了 6 个 backlog item，代码已写好验收通过，但"状态"字段未被批量更新为"已完成"。
