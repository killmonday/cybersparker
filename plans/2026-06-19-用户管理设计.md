# 用户管理功能设计（修正版 v3）

> 状态：待用户确认 | 2026-06-19 | 审查通过

## 做什么

给后台新增用户管理体系：多角色账户、角色级权限控制、前端页面适配。

## 当前状态

- 登录硬编码：`app_cybersparker/views/login.py:44`，只认 `admin` + MD5 密码
- Session（生产）：存 `{'id': 'admin'}`（字符串，无角色字段）
- Session（测试）：12 个 fixture 存 `{'username': 'admin'}`（与生产格式不一致）
- AuthMiddleware 只检查 `session.get("info")` 是否存在
- 前端 AuthGuard 只取 `authenticated` 布尔值，不存用户信息
- Django `auth.User` 虽在 INSTALLED_APPS 但从未使用

## 角色权限矩阵

### 用户管理权限

| 操作 | 超级管理员 | 普通管理员 | 普通用户 |
|------|:---:|:---:|:---:|
| 创建新用户 | admin / user | 仅 user | ✗ |
| 删除用户 | 除自己外所有人 | 仅 user（不能是自己） | ✗ |
| 修改他人密码 | 所有人 | 仅 user | ✗ |
| 修改他人角色 | ✓ | ✗ | ✗ |
| 修改自己密码 | ✓ | ✓ | ✓ |

### 功能模块权限

| 模块 | 操作 | 超级管理员 | 普通管理员 | 普通用户 |
|------|------|:---:|:---:|:---:|
| 仪表盘 | 查看 | ✓ | ✓ | ✓ |
| 插件管理 | 查看列表/详情 | ✓ | ✓ | ✗ |
| 插件管理 | 新增/编辑/删除/调试 | ✓ | ✓ | ✗ |
| 指纹管理 | 查看列表/详情/调试 | ✓ | ✓ | ✗ |
| 指纹管理 | 新增/编辑/删除 | ✓ | ✓ | ✗ |
| 自动扫描任务 | 查看 | ✓ | ✓ | ✓ |
| 自动扫描任务 | 新增/编辑/删除/启停 | ✓ | ✓ | ✗ |
| 批量任务 | 查看 | ✓ | ✓ | ✓ |
| 批量任务 | 新增/编辑/删除/启停 | ✓ | ✓ | ✗ |
| 目录扫描 | 查看 | ✓ | ✓ | ✓ |
| 目录扫描 | 新增/编辑/删除/启停 | ✓ | ✓ | ✗ |
| 字典/字典组 | 查看 | ✓ | ✓ | ✓ |
| 字典/字典组 | 新增/编辑/删除 | ✓ | ✓ | ✗ |
| 代理配置 | 查看 | ✓ | ✓ | ✓ |
| 代理配置 | 新增/编辑/删除 | ✓ | ✓ | ✗ |
| 引擎配置 | 查看 | ✓ | ✓ | ✓ |
| 引擎配置 | 新增/编辑/删除 | ✓ | ✓ | ✗ |
| 资产检索/结果 | 查看 | ✓ | ✓ | ✓ |
| 资产检索/结果 | 漏洞利用按钮 | ✓ | ✓ | ✗ |
| 漏洞利用结果 | 查看 | ✓ | ✓ | ✓ |
| AI模型配置 | 查看 | ✓ | ✓ | ✓ |
| AI模型配置 | 新增/编辑/删除 | ✓ | ✓ | ✗ |
| PoC生成任务 | 查看 | ✓ | ✓ | ✓ |
| PoC生成任务 | 新增/执行/删除 | ✓ | ✓ | ✗ |
| CEYE配置 | 查看 | ✓ | ✓ | ✓ |
| CEYE配置 | 编辑 | ✓ | ✓ | ✗ |
| 导出任务 | 查看 | ✓ | ✓ | ✓ |
| 文件管理 | 查看 | ✓ | ✓ | ✓ |
| 文件管理 | 上传/下载/删除 | ✓ | ✓ | ✗ |
| 文件托管 | 查看/上传/删除/重命名/备注 | ✓ | ✓ | ✗ |
| 文件托管 | 下载（公开链接 + 需登录的） | ✓ | ✓ | ✓ |
| 用户管理 | 全部 | ✓ | 仅管理 user | ✗ |

**权限边界一句话**：普通用户能看到所有菜单、所有页面，但调用 API 时——插件/指纹直接 403，其他模块 GET 正常、写操作 403。

## 数据模型

```python
# app_cybersparker/models.py 新增

class UserProfile(models.Model):
    ROLE_CHOICES = [
        ('super_admin', '超级管理员'),
        ('admin', '普通管理员'),
        ('user', '普通用户'),
    ]
    user = models.OneToOneField('auth.User', on_delete=models.CASCADE, related_name='profile')
    role = models.CharField(max_length=16, choices=ROLE_CHOICES, default='user')

# post_save 信号 — 确保每个 User 创建时自动创建 UserProfile
@receiver(post_save, sender=User)
def create_user_profile(sender, instance, created, **kwargs):
    if created:
        UserProfile.objects.create(user=instance)
```

用 Django 内置 `auth.User` 存用户名/密码/is_active，Profile 只存 role。

## 初始化数据（迁移中处理）

不写自定义 hasher。Migration 的 `RunPython` 直接做：

```python
def init_super_admin(apps, schema_editor):
    User = apps.get_model('auth', 'User')
    if not User.objects.filter(username='admin').exists():
        user = User.objects.create_user(
            username='admin',
            password='admin',          # Django set_password 自动 PBKDF2
            is_active=True,
        )
        # post_save 信号会在此项目启动后自动创建 UserProfile
        # 但 migration 中信号不触发，需手动创建
        UserProfile = apps.get_model('app_cybersparker', 'UserProfile')
        UserProfile.objects.create(user=user, role='super_admin')
```

账号 `admin` / 密码 `admin`，管理员首次登录后自己改密码。

## 登录改造

```python
# app_cybersparker/views/login.py 改写

from django.contrib.auth import authenticate, login as auth_login, logout as auth_logout

def get_user_role(user):
    try:
        return user.profile.role
    except User.profile.RelatedObjectDoesNotExist:
        return 'user'

def login(request):
    # GET → 同现在
    # POST → 解析 username / password
    user = authenticate(request, username=username, password=password)
    if user and user.is_active:
        request.session["info"] = {
            "id": user.id,
            "username": user.username,
            "role": get_user_role(user),
        }
        auth_login(request, user)
        return JsonResponse({"status": True, "next": next_url})
    return JsonResponse({"status": False, "message": "用户名或密码错误"}, status=401)

def logout(request):
    auth_logout(request)
    request.session.flush()
    return redirect('/login')

def session_status(request):
    info = request.session.get("info")
    if not info:
        return JsonResponse({...}, status=401)
    return JsonResponse({"authenticated": True, "user": info})
```

不保留旧 session 兼容代码。迁移后用户手动重新登录即可。

## 权限执行体系

### 后端：`app_cybersparker/permissions.py`（新文件）

```python
from functools import wraps
from django.http import JsonResponse

def get_role(request):
    return request.session.get("info", {}).get("role", "")

def require_role(*roles):
    def decorator(view_func):
        @wraps(view_func)
        def wrapper(request, *args, **kwargs):
            if get_role(request) not in roles:
                return JsonResponse(
                    {"status": False, "message": "无操作权限"}, status=403
                )
            return view_func(request, *args, **kwargs)
        return wrapper
    return decorator

def deny_user(view_func):
    """阻止普通用户（super_admin + admin 放行）"""
    return require_role('super_admin', 'admin')(view_func)
```

### API 盘点方法

`views/expload/` 下有约 280 个视图函数。BL-USER-003 执行步骤：

1. 逐模块盘点每个函数：
   - 纯 GET（列表/详情查询）→ 不加装饰器
   - 纯 POST/PUT/DELETE（创建/编辑/删除）→ 加 `@deny_user`
   - 混合方法（同一个函数处理 GET+POST）→ 函数内部对写方法做 `if request.method in ('POST', 'PUT', 'DELETE'): deny_user_check`
   - 非 JSON API → 单独处理
2. 按模块执行：插件 → 指纹 → 任务 → 结果 → 配置 → 文件 → AI PoC
3. `/files/` 下载端点不做角色限制——所有已登录用户均可下载

### 角色变更/用户删除 → 强制 session 失效

```python
from django.contrib.sessions.models import Session

def invalidate_user_sessions(user_id):
    for session in Session.objects.filter(expire_date__gte=timezone.now()):
        data = session.get_decoded()
        if data.get("info", {}).get("id") == user_id:
            session.delete()
```

在 `PUT role` 和 `DELETE user` 操作后调用。

### 前端：AuthContext（全新文件 `frontend/src/contexts/AuthContext.tsx`）

```typescript
import React, { createContext, useContext, useState, useEffect } from 'react'

interface AuthState {
  authenticated: boolean
  username: string
  role: 'super_admin' | 'admin' | 'user'
}

const AuthContext = createContext<AuthState>({ authenticated: false, username: '', role: 'user' })

export const useAuth = () => useContext(AuthContext)

export const AuthProvider: React.FC<{ children: React.ReactNode }> = ({ children }) => {
  const [state, setState] = useState<AuthState>({ authenticated: false, username: '', role: 'user' })
  const [checking, setChecking] = useState(true)

  useEffect(() => {
    fetch('/api/v1/auth/session', { credentials: 'same-origin' })
      .then(async (r) => {
        if (r.status === 200) {
          const data = await r.json()
          if (data.authenticated) {
            setState({
              authenticated: true,
              username: data.user?.username || '',
              role: data.user?.role || 'user',
            })
            return
          }
        }
      })
      .catch(() => {})
      .finally(() => setChecking(false))
  }, [])

  if (checking) return <Spin size="large">...</Spin>
  if (!state.authenticated) {
    window.location.href = '/login?next=' + encodeURIComponent(window.location.pathname)
    return null
  }
  return <AuthContext.Provider value={state}>{children}</AuthContext.Provider>
}
```

App.tsx 组件树：`AuthProvider → SidebarLayout → Routes`

### 前端改造（极简）

**不做的事**：
- 侧边栏菜单不过滤——所有用户看到所有菜单项
- 不加路由守卫——用户直接访问插件/指纹 URL 也能看到页面框架

**做的事**：
- 页面内按钮用 `useAuth().role !== 'user'` 条件显示/隐藏（新增/编辑/删除/启动/停止按钮对普通用户隐藏）
- 插件/指纹页面对普通用户来说，可以看到页面框架，但 API 返回 403，列表为空

## API 设计

全部在 `/api/v1/users` 下，需登录。

| 方法 | 路径 | 功能 | super_admin | admin | user |
|------|------|------|:---:|:---:|:---:|
| GET | `/api/v1/users` | 用户列表 | 所有人 | 仅 role=user 的记录 | — |
| POST | `/api/v1/users` | 创建用户 | 可传 role=admin/user | 仅可传 role=user | — |
| DELETE | `/api/v1/users/<id>` | 删除用户 | 除自己外所有人（删后清 session） | 仅 user（不能是自己） | — |
| PUT | `/api/v1/users/<id>/role` | 修改角色 | ✓（改后清 session） | — | — |
| PUT | `/api/v1/users/<id>/password` | 修改他人密码 | 所有人 | 仅 user | — |
| PUT | `/api/v1/users/me/password` | 修改自己密码 | ✓ | ✓ | ✓ |

## 前端页面

### UserManagementPage.tsx（新页面）

- 路由：`/react-shell/users`
- 侧边栏：一级菜单"用户管理"，所有人可见（普通用户看到菜单但点进去 API 返回 403，页面友好提示）
- 列表列：用户名、角色（Tag 颜色）、最后登录时间、操作按钮
- **super_admin**：新增（角色下拉 admin+user）、操作列（删除/改角色/重置密码）
- **admin**：新增（角色固定 user）、删除仅在目标 role=user 时显示、重置密码仅在目标 role=user 时显示
- **user**：API 返回 403，页面显示无权限提示

## Backlog 拆分

### BL-USER-001：用户模型与认证改造（P0）

**范围**：UserProfile 模型 + migration（含 admin/admin 超级管理员初始数据）+ login/logout/session_status 改写 + post_save 信号 + AuthContext 创建

**验收**：
- [ ] migration 后 `python manage.py shell -c "from django.contrib.auth import authenticate; print(authenticate(username='admin', password='admin'))"` 返回 User 对象
- [ ] admin 用户的 profile.role='super_admin'
- [ ] `session["info"]` 包含 `{id: 1, username: 'admin', role: 'super_admin'}`
- [ ] logout 清除 Django session + 自定义 info
- [ ] 现有 12 个测试的 `session['info']` 更新后通过
- [ ] 前端 AuthContext 加载后可取到 role

### BL-USER-002：用户管理 API（P0）

**范围**：`/api/v1/users` 全套 CRUD + 角色边界校验 + session 失效

**验收**：
- [ ] super_admin 创建 admin/user 成功
- [ ] admin 创建 user 成功，传 role=admin 返回 403
- [ ] super_admin 删除任何用户（除自己外）成功
- [ ] admin 删除 super_admin 或其他 admin 返回 403
- [ ] user 调用任何用户管理接口返回 403
- [ ] 改角色后目标用户 session 被清除
- [ ] 删除用户后目标用户 session 被清除

### BL-USER-003：后端权限执行（P0）

**范围**：新建 `app_cybersparker/permissions.py`，对 `views/expload/` 下约 280 个 API 视图按方法盘点并加以权限

**执行步骤**：
1. 盘点每个 views 文件：函数 → HTTP 方法分类 → 施加权限
2. 按模块顺序执行：插件 → 指纹 → 任务 → 结果 → 配置 → 文件 → AI PoC

**验收**：
- [ ] 插件/指纹所有 API（含调试）→ user 返回 403
- [ ] 其他模块 GET → user 正常访问
- [ ] 其他模块 POST/PUT/DELETE → user 返回 403
- [ ] 文件托管公开下载（`/files/`）→ 所有已登录用户均可下载
- [ ] admin 和 super_admin 访问所有 API 不受限

### BL-USER-004：前端用户管理页 + 按钮权限适配（P0）

**范围**：UserManagementPage + 各已有页面按钮的写操作条件渲染

**不做**：侧边栏菜单过滤、路由守卫

**验收**：
- [ ] `/react-shell/users` 页面可访问，侧边栏有入口
- [ ] super_admin 视角：新增（角色下拉 admin+user）、删除、改角色、重置密码
- [ ] admin 视角：新增（角色固定 user）、仅能删/改密码 user
- [ ] user 视角：页面显示无权限提示
- [ ] 所有功能页面的写操作按钮对普通用户隐藏

## 不做

- 不做细粒度对象级权限
- 不做密码复杂度/过期策略
- 不做用户头像、邮箱验证、密码找回
- 不改造现有功能模块内部逻辑
- 不做操作审计日志
- 不做侧边栏菜单过滤 / 路由守卫（用户能看到所有页面，API 层拦截）
- 不做旧 session 兼容

## 风险

| 风险 | 等级 | 说明 |
|------|------|------|
| API 视图函数盘点遗漏 | 中 | 约 280 个函数需逐个区分，按执行步骤逐模块覆盖 |
| 测试 fixture 更新 | 低 | 12 处 `session['info']` 需更新 |
| `user.profile.role` 无 Profile | 低 | post_save 信号 + get_user_role() try/catch 降级 |
