# BL-ZONE-001 模型与迁移

- 日期：2026-06-20
- 状态：进行中
- 关联 backlog：docs/backlog/13-扫描区域与资产域.md (BL-ZONE-001)

## 做什么

数据库层面建立扫描区域和根资产绑定的基础设施：
1. 新增 AssetZone + AssetRootBinding 模型
2. 5 张表加 zone FK + 历史数据回填 public
3. auto_scan_indentify_result / auto_scan_directory_result 改唯一键
4. zone 删除 API（五重引用检查）

## 怎么做

TDD 顺序：先写测试 → 确认失败 → 实现模型 → 生成 migration → 执行 migration → 测试通过。

## 风险

- 唯一键切换需要删除旧约束 + 创建新约束，可能锁表
- 历史数据可能有唯一键冲突（理论上都在 public 下无冲突，但需要验证）

## 验证

- `python manage.py check` 0 issues
- 新增测试全部通过
- 全量回归测试通过
