# 资产域功能场景走查

## 场景 1：创建自动扫描任务 → 写资产时 zone 正确

用户操作：创建自动扫描任务，选 zone=内网1，上传目标文件，启动扫描。
链路：前端表单 → POST /api/v1/identify-tasks/create → startTask → handler init → save_indentify_to_db → event payload → writer → 资产写入

### 前端
- 文件: frontend/src/pages/AutoScanTaskListPage.tsx
- 检查: zone Select 组件存在？提交时 zone_id 在 FormData 中？
- 结论:

### 后端
- auto_scan_task.py add(): form.instance.zone 来自前端 POST
- auto_scan_task.py startTask(): row_dict["zone_id"] = task_obj.zone_id
- auto_exp_task.py handler init: self.zone_id = int(raw_zone)
- auto_exp_task.py save_indentify_to_db: zone_id=self.zone_id
- result_event_service.py build_identify_event_payloads: zone_id 参数
- result_event_service.py _write_identify_event: match_keys 含 zone_id

## 场景 2：fscanx 导入到内网1，不覆盖公网

## 场景 3：批量任务 input_type=2，只消费 auto_scan_exp_result

## 场景 4：全局检索按 zone 过滤

## 场景 5：删除有资产引用的 zone 被拒绝

## 场景 6：删除任务时锚点资产保留

## 场景 7：input_type=4 强制公网
