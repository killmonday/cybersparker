# SQL 性能反模式批量修复

- 日期：2026-06-24
- 状态：已完成

## 验证

- 迁移 0082 已应用（3 个字段 db_index）
- 全量 447 测试通过（2 个预存失败不相关）

## 修复清单

| ID | 项 | 文件 | 改动 |
|----|----|------|------|
| H1 | status 字段缺索引 | models.py | auto_scan_tasks.status + batch_EXPTask.status 加 db_index=True |
| H2 | task_id 缺索引 | models.py | EXPTask_result.task_id 加 db_index=True |
| H3 | dirscan_worker N+1 | dirscan_worker.py | _finalize_host_products 批量查询 |
| H4 | fscanx_views N+1 | fscanx_views.py | 删除时批量检查引用 |
| M1 | 批量删除逐条删 | 10+文件 | 改用 filter(id__in=uids).delete() |
| M2 | fscanx 列表逐条计数 | fscanx_views.py | 用 Subquery annotation |
| M3 | plugin_manage 缺 prefetch | plugin_manage.py | 加 prefetch_related('tags') |
