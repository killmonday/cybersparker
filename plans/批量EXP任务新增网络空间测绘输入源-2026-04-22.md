# 批量EXP任务新增网络空间测绘输入源-2026-04-22

## 任务目标
在不改动现有扫描执行主链路（`batch_task_executor.py` 继续按 `target_file` 执行）的前提下，为批量任务新增第4类输入源“网络空间测绘引擎”，并补齐配置管理、运行语义与文件安全清理能力。

## 实施计划（本次执行）
1. **模型与迁移**
   - 为 `batch_EXPTask` 增加测绘相关字段。
   - 新增 `CyberspaceEngineSetting` 模型。
   - 生成并应用迁移。
2. **配置管理页面**
   - 新增 `cyberspace_engine_setting` 的 list/add/edit/delete/detail 视图、模板与路由。
   - 在左侧菜单增加入口。
3. **批量任务前后端扩展**
   - 表单增加 input_type=4 对应字段（engine_type/query/max_assets/proxy_mode/proxy）。
   - 前端根据 input_type 切换显示 engine 区域。
   - `detail` 回填新增字段。
4. **服务层与适配器层**
   - 新增统一服务层，负责拉取、代理决策、去重、落盘和安全删除。
   - 新增 5 个引擎适配器（fofa/zoomeye/quake/hunter/shodan）。
5. **运行语义接入**
   - 在 `operate` 启动前调用 `prepare_engine_target_before_start`。
   - 实现 stop 继续复用、finish 重跑重拉。
6. **验证与收口**
   - `makemigrations`、`migrate`、`check`、`py_compile`、`makemigrations --check`。

## 实际完成结果
### 一、模型与迁移
- 已修改：`app_cybersparker/models.py`
  - `batch_EXPTask.input_type` 新增 `(4, "cyberspace engine")`。
  - 新增字段：`engine_type`、`engine_query`、`engine_max_assets`、`engine_proxy_mode`、`engine_proxy`。
  - 新增模型：`CyberspaceEngineSetting`。
- 已生成并应用迁移：
  - `app_cybersparker/migrations/0004_batch_exptask_engine_max_assets_and_more.py`
  - `python manage.py migrate` 执行成功。

### 二、配置管理页面
- 新增后端：`app_cybersparker/views/expload/cyberspace_engine_setting.py`
- 新增模板：`app_cybersparker/templates/project/expload/cyberspace_engine_setting.html`
- 新增路由：`cybersparker/urls.py`
  - `cyberspace_engine_setting`、`add`、`edit`、`delete`、`detail`
- 菜单入口已接入：`app_cybersparker/templates/project/index.html`

### 三、批量任务前后端改造
- 后端核心文件：`app_cybersparker/views/expload/task_manage/batch_exp_task.py`
  - 新增：`clear_engine_fields`、`validate_engine_source_fields`、`parse_progress_value`、`prepare_engine_target_before_start`。
  - `resolve_target_source` 新增 `input_type==4` 分支并做参数校验。
  - `detail` 返回并支持回填 engine 字段。
  - `delete` 按任务类型分流删除（engine 文件走安全删除）。
  - `operate`（单任务与批量）启动前接入 engine 目标准备逻辑。
- 前端模板：`app_cybersparker/templates/project/expload/task_manage/bath_exp_task_list.html`
  - 新增 `.engine-source-field` 渲染块。
- 前端脚本：`app_cybersparker/static/project2/expload/js/batch_Expload_Task.js`
  - `toggleInputTypeUI()` 增加 type=4 显隐控制。

### 四、服务层与适配器
- 新增包与文件：
  - `app_cybersparker/services/__init__.py`
  - `app_cybersparker/services/cyberspace_engine_service.py`
  - `app_cybersparker/services/cyberspace_engine_adapters.py`
- 关键能力：
  - 统一入口 `fetch_and_dump_targets(task_obj)`。
  - 代理解析 `resolve_engine_proxy(task_obj, config_obj)`。
  - 测绘资产路径校验与安全删除：`is_engine_asset_target`、`remove_engine_asset_file`。
  - 固定目录：`EXP_input/engine_assets`。
  - 资产上限保护：`MAX_FETCH_ASSETS = 5000`。

### 五、运行语义达成
- 已实现以下规则：
  - **stop后继续**：优先复用已有 engine 资产文件。
  - **finish后重跑**：删除旧 engine 资产文件并重新拉取。
  - **首次运行**：无有效 target 时拉取并生成新文件。

## 执行验证结果
- `python manage.py makemigrations`：已生成 0004 迁移。
- `python manage.py migrate`：成功。
- `python manage.py check`：无系统问题。
- `python -m py_compile ...`：通过（核心改动 Python 文件语法正常）。
- `python manage.py makemigrations --check`：No changes detected。
- 测试现状：仓库当前未发现测试用例（`python manage.py test` 为 0 tests）。

## 当前状态与后续建议
### 当前状态
本次代码改造与迁移已完成，静态与框架层校验通过。

### 待补充验证（未在本次自动完成）
1. 真实引擎 API 联调（5个适配器请求/响应字段与目标环境一致性）。
2. 页面手工冒烟：
   - 配置页 CRUD；
   - 批量任务 input_type=4 的新增、编辑、回填；
   - 运行 stop 继续 / finish 重跑。
3. 如需提高可回归性，补充自动化测试用例（service + operate 分支）。
